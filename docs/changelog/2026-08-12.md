# 2026-08-12

## 10:52 — Treat last punch as Time Out when tagged In

**Why:** Employees may end the day with an In punch (missing Out). Payroll should still use first In + last log as the work window.

**Files:**
- `app/Services/PayrollBreakService.php`
- `app/Services/FacultyLoadPayrollService.php`
- `tests/Unit/PayrollBreakServiceTest.php`

**Summary:**
- Session: **first In** + **last punch of the day as Out** (even if `is_in`)
- Does not count Out → final dangling In as a lunch/break segment
- Abrantes-style logs (5:48 In, 5:50 Out, 6:18 In) → session **5:48–6:18**
- Unit tests for dangling last In and duplicate morning Ins

## 10:55 — Payroll Register Excel: one worksheet per campus

**Why:** Split Excel download so each campus has its own sheet; unlisted campuses roll into Cainta.

**Files:**
- `config/payroll_register_layout.php`
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `app/Services/Reports/PayrollRegisterExcelExporter.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`
- `tests/Unit/PayrollRegisterExcelExporterTest.php`

**Summary:**
- Excel sheets: Antipolo, Binangonan, Cogeo, San Mateo, Sumulong, Taytay, Cainta
- Mapping by campus code (UA/BI/CO/SA/SU/TA); CA + Angono/Digital/etc. → Cainta
- Uses employee campus / primary campus assignment; empty sheets still created
- Row `#` restarts per worksheet

## 10:57 — Payroll Register Excel: skip empty campus worksheets

**Why:** Do not create Excel tabs for campuses with no employees in the register.

**Files:**
- `app/Services/Reports/PayrollRegisterExcelExporter.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `tests/Unit/PayrollRegisterExcelExporterTest.php`

**Summary:**
- Only campuses with at least one row get a worksheet
- If the whole register is empty, one placeholder “Payroll Register” sheet remains

## 11:04 — Desktop build 0.1.52 (macOS + Windows)

**Why:** Ship last-punch-as-Out payroll fix and campus-split Payroll Register Excel.

**Files:**
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.52`)
- `dist/Pulse-0.1.52-arm64.dmg`
- `dist/Pulse-0.1.52-setup.exe`

**Summary:**
- Built macOS DMG + Windows EXE for **0.1.52**
- Uploaded to S3; deleted 0.1.51 — prefix keeps only 0.1.52

## 13:30 — Manual overtime ignores timekeeping policy

**Why:** Allow Add Overtime even when policy is “Disregard excess hours” / before-after OT flags are off.

**Files:**
- `app/Services/EmployeeOvertimeApprovalService.php`
- `resources/views/payroll/transaction/_batch-employee-add-overtime-modal.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-overtime-form.blade.php`

**Summary:**
- Manual OT no longer checks policy excess-as-OT, before/after flags, min minutes, or OT equivalents
- Still requires: logs for the date, shift schedule, OT inside punch window, OT overlapping time outside scheduled duty
- Process pay uses the same policy-independent minutes from approved filings

## 13:34 — Manual OT auto-fills excess window

**Why:** Users could not save OT because 12:30–12:30 (inside shift) was entered instead of the excess range.

**Files:**
- `app/Services/EmployeeOvertimeApprovalService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_batch-employee-add-overtime-form.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-overtime-modal.blade.php`
- `resources/js/app.js` (+ `npm run build`)

**Summary:**
- Work date change loads excess preview and auto-fills OT Start/End (before + after shift windows)
- Hint shows available excess ranges and minutes
- Same start/end time is rejected with a clear error; overlap errors list available windows

## 13:50 — Upload Adjustments: Overtime tab

**Why:** Allow bulk overtime filing via CSV/TSV upload, same pattern as Shift Code.

**Files:**
- `config/payroll_transaction.php`
- `database/migrations/2026_08_12_134500_create_raw_payroll_overtimes_table.php`
- `app/Models/RawPayrollOvertime.php`
- `app/Models/RawPayrollTransaction.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`

**Summary:**
- New Upload Adjustments tab **Overtime** (`upload=overtime`, type id 7)
- Template columns: emp_num, full_name, work_date, ot_start, ot_end
- Validates against time logs + excess outside shift (policy ignored); upserts `tbl_employee_overtime_approvals`
- Raw audit rows in `raw_payroll_overtimes`; Process/Reprocess applies to pay

## 14:00 — Employee detail: Minutes column on Incomes + Net Pay

**Why:** Show minutes alongside hours for payroll income lines.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`

**Summary:**
- Incomes table: Minutes column between Description and Hours (derived from hours × 60)
- Net Pay tab: shows minutes next to hours for income and late/undertime deduction lines

## 14:02 — Minutes column moved to Deductions (not Incomes)

**Why:** Minutes belongs on late/undertime deductions, not income lines.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`

**Summary:**
- Removed Minutes from Incomes table
- Deductions table: Minutes between Description and Hours (LTDE/UTDE)
- Net Pay: minutes only on deduction lines with hours

## 14:10 — Clickable Late / Undertime / Overtime day details

**Why:** Show which work dates make up late, undertime, and overtime totals.

**Files:**
- `app/Services/PayrollAttendanceDayBreakdownService.php`
- `app/Services/EmployeeOvertimeApprovalService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_batch-employee-attendance-breakdown-modal.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- LTDE / UTDE / OVRT rows are clickable when day details exist (“View days”)
- Nested modal lists work date, shift code, schedule, time in/out, minutes (OT also shows OT window)

## 14:34 — Offset absent tardiness with overtime

**Why:** When late equivalent marks absent but the day has approved OT, keep full regular hours and reduce OT by late minutes.

**Files:**
- `database/migrations/2026_08_12_143100_add_is_offset_absent_tardiness_with_ot_to_timekeeping_policies.php`
- `app/Models/TimekeepingPolicy.php`
- `app/Support/TimekeepingPolicy.php`
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `app/Services/EmployeeOvertimeApprovalService.php`
- `app/Services/PayrollAttendanceDayBreakdownService.php`

**Summary:**
- Policy checkbox **Offset absent tardiness with overtime** on Tardiness & Undertime settings
- If late → absent AND approved OT that day: not absent/late, full scheduled hours, OT reduced by raw late minutes
- Example: 6:00–15:00, in 6:35 / out 18:00 with 3h OT → 8h basic, OT ≈ 2h 25m (180−35)

## 14:37 — Absent+OT offset uses 1 hour, not raw late minutes

**Why:** Mark-as-absent means 1 hour absent for OT offset (3h OT − 1h = 2h), not the 35-minute clock late.

**Files:**
- `app/Services/TimeLogsPayrollService.php`
- `app/Support/TimekeepingPolicy.php`
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`

**Summary:**
- OT offset amount = 60 minutes (or equivalent on the absent row if set > 0)
- Sample: 3h OT − 1h absent = **2h OT**, full 8h basic, no late

## 14:42 — Move offset checkbox above Tardiness Equivalent table

**Why:** Checkbox should sit on top of the Tardiness Equivalent (Late)s card.

**Files:**
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`

**Summary:**
- Moved **Offset absent tardiness with overtime** to the top of the Tardiness Equivalent card
- Main Save Settings form preserves the setting via hidden field

## 15:01 — Desktop build 0.1.53 (macOS + Windows)

**Why:** Ship manual OT, overtime upload, attendance day breakdown, and absent+OT offset policy.

**Files:**
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.53`)
- `dist/Pulse-0.1.53-arm64.dmg`
- `dist/Pulse-0.1.53-setup.exe`

**Summary:**
- Built macOS DMG + Windows EXE for **0.1.53**
- Uploaded to S3; deleted 0.1.52 — prefix keeps only 0.1.53

## 20:28 — Cloud DB backup S3 keys use year/month folders

**Why:** Organize Pulse database backups on S3 by year then month instead of a flat prefix.

**Files:**
- `app/Services/DesktopCloudBackupService.php`
- `config/backup.php`
- `.env.example`
- `resources/views/database/index.blade.php`
- `tests/Unit/DesktopCloudBackupServiceTest.php`

**Summary:**
- S3 object key is now `payroll-backups/{YYYY}/{MM}/{filename}.sql.gz`
- Example: `payroll-backups/2026/08/pulse-db-Kents-MacBook-Pro-2026-08-12-10-00-00.sql.gz`
- Database page copy notes the year/month folder layout

## 20:34 — Desktop build 0.1.54 (macOS + Windows)

**Why:** Ship year/month S3 folder layout for daily cloud database backups.

**Files:**
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.54`)
- `dist/Pulse-0.1.54-arm64.dmg`
- `dist/Pulse-0.1.54-setup.exe`

**Summary:**
- Built macOS DMG + Windows EXE for **0.1.54**
- Uploaded to S3; deleted 0.1.53 — prefix keeps only 0.1.54
