## 11:55 — Employee list filters: Campus, Dept/College, Employment Category

**Why:** Employee Management needs more list filters beside Status and Compliance.

**Files:**
- `app/Http/Controllers/EmployeeController.php`
- `resources/views/employees/_filters.blade.php`
- `resources/views/employees/index.blade.php`

**Summary:**
- Added Campus, Department/Colleges, and Employment Category (Faculty/Staff) filters next to Compliance
- Lahat may **All** na default; live table AJAX keeps filter params in the URL
- Campus filters `campus_id`; Dept/College matches college or department name; category filters employment `user_type`

## 11:59 — Employee filter bar layout

**Why:** Five filters looked uneven (four on one row, one orphaned) beside the search box.

**Files:**
- `resources/views/employees/_filters.blade.php`
- `resources/views/partials/live-data-table.blade.php`

**Summary:**
- Employee filters in a 5-column grid on large screens (2/3 cols on smaller breakpoints)
- Search moved to its own column with label, aligned with filter row
- Live table filter area uses flex-1 so filters and search share one row cleanly

## 12:05 — Employee list performance (24s load fix)

**Why:** Employee index took ~24s because each page loaded full salary trees for every row.

**Files:**
- `app/Http/Controllers/EmployeeController.php`
- `resources/views/partials/live-data-table.blade.php`

**Summary:**
- List view loads only campus + employment position/type (no salary/previous salary eager loads)
- Stats use one aggregate query instead of four separate counts
- AJAX filter refresh skips reloading campus/college/department dropdown data
- Search moved below filter row so five filters fit on one line

## 12:12 — Employee list filter speed (policy + Skolaris defer)

**Why:** Filter changes still felt slow; each refresh ran 50+ submodule DB queries and ISKOLARIS sync on page load.

**Files:**
- `app/Policies/EmployeePolicy.php`
- `app/Http/Controllers/EmployeeController.php`
- `resources/views/employees/_results.blade.php`
- `resources/views/employees/index.blade.php`
- `resources/js/app.js`

**Summary:**
- Memoize employees submodule lookup in `EmployeePolicy` (was queried per row action)
- List actions use one permission check instead of `@can` per employee row
- Employment category filter uses `whereExists` instead of `whereHas`
- ISKOLARIS Approve badge loads after 2s defer, not blocking initial page/filter AJAX

## 12:22 — Unique colleges + faster employee filter

**Why:** Dept/College filter listed the same college once per campus (112 rows / 8 names), and filter changes waited on debounce plus a second COUNT query.

**Files:**
- `app/Http/Controllers/EmployeeController.php`
- `resources/js/app.js`
- `tests/Feature/EmployeeListFilterTest.php`

**Summary:**
- College dropdown now lists each college name once (seed still stores one row per campus)
- Filter dropdowns reload immediately instead of waiting 300ms debounce (search still debounced)
- List query selects only columns needed for the table and reuses the stats COUNT for pagination
- Employment category `whereExists` now hits `tbl_employee_employment_information` (wrong plural table name before)

## 12:32 — Employee filter no longer blocked by ISKOLARIS sync

**Why:** Filter changes still hung ~20–30s because deferred ISKOLARIS Approve fetch (60s API timeout) monopolized the dev server and session lock.

**Files:**
- `app/Http/Middleware/ReleaseSessionLockForReadOnlyAjax.php`
- `bootstrap/app.php`
- `resources/js/app.js`

**Summary:**
- Removed auto ISKOLARIS pending fetch 2s after page load; badge loads only when Approve modal opens
- GET AJAX/JSON requests release the session lock early so live-table filter refreshes can run in parallel
- Rebuilt frontend assets (`npm run build`)

## 12:41 — People360 1.0.1 desktop build

**Why:** Ship employee list filters and filter-speed fixes in macOS + Windows installers.

**Version:** `NATIVEPHP_APP_VERSION=1.0.1`

**Installers:**
- `dist/People360-1.0.1-arm64.dmg`
- `dist/People360-1.0.1-setup.exe`

## 13:57 — Public GitHub repo for desktop auto-update

**Why:** Installed apps on other PCs stayed on v1.0.0 because private repo required a GitHub token on each client.

**Files:**
- `.env.example`

**Summary:**
- `kenjiex01/pulse` visibility changed to **public** on GitHub
- `GITHUB_PRIVATE=false` for future builds (no client token needed)
- Rebuild **1.0.2** with public updater config + publish GitHub release

**Version:** `NATIVEPHP_APP_VERSION=1.0.2`

**Installers:**
- `dist/People360-1.0.2-arm64.dmg`
- `dist/People360-1.0.2-setup.exe`

## 15:22 — Company Documents (Memo Form Builder)

**Why:** Port Skolaris SRS Form Builder into People360 as HR **Company Documents** for memo templates, submissions, and approvals.

**Files:**
- `database/migrations/2026_08_28_150000_create_company_document_form_builder_tables.php`
- `app/Models/CompanyDocument*.php`
- `app/Http/Controllers/CompanyDocument*.php`
- `app/Services/CompanyDocumentFormBuilderService.php`
- `app/Policies/CompanyDocumentFormPolicy.php`
- `resources/views/company-documents/**`
- `resources/js/company-document-designer.js`
- `routes/web.php`
- `database/seeders/SubModuleSeeder.php`
- `database/seeders/CompanyDocumentSampleMemoSeeder.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Card-based template list (Skolaris-style) under Human Resource → **Company Documents**
- Drag-and-drop designer page for memo fields + approval routing
- Submissions (fill memo) and **Document Approvals** queue with approve/reject
- Sample template `hr_internal_memo` seeded; submodule permissions wired for admin
- MySQL-safe short FK/index names; migration + seeds run; `npm run build`

## 15:53 — Company Documents wired into bootstrap seeders

**Why:** New module must appear in sidebar after normal `db:seed` / desktop bootstrap without manual seeder commands.

**Files:**
- `database/seeders/DesktopBootstrapSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `database/seeders/DatabaseSeeder.php`
- `database/seeders/SubModuleSeeder.php`

**Summary:**
- `DesktopBootstrapSeeder` now runs `RoleModuleSeeder` after `SubModuleSeeder` (permissions sync on upgrade)
- `CompanyDocumentSampleMemoSeeder` included in `ReferenceDataSeeder` (full dev seed path)
- HR menu order: **Company Documents** / **Document Approvals** moved near top (sort 2–3, after Employees)
- Ran `DesktopBootstrapSeeder` locally to apply

## 15:54 — Fix Company Documents designer ParseError

**Why:** Designer page crashed — Blade could not parse multi-line `@json()` with arrow functions inside HTML attributes.

**Files:**
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `resources/views/company-documents/designer.blade.php`

**Summary:**
- Moved `initialElements` and `initialSteps` JSON prep to controller
- Blade now uses simple `@json($initialElements)` / `@json($initialSteps)` in data attributes

## 15:59 — Skolaris-style Company Documents designer UI

**Why:** Designer looked like a basic admin form editor; user expects Skolaris 3-panel builder (palette, live preview, properties).

**Files:**
- `resources/views/company-documents/designer.blade.php`
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `app/Support/CompanyDocumentElementCatalog.php`

**Summary:**
- Dark left palette with BASIC/TEXT/DATE/CHOICE/ADVANCED tabs + search
- Center canvas shows live field preview (radio, date, text, etc.) in a form card
- Selected field: blue ring + drag / settings / delete floating buttons
- Dark right properties panel (label, width, required, placeholder, choices)
- Gradient top bar with Build / Approvals tabs; `npm run build` run

## 16:01 — Designer label alignment (LEFT / RIGHT / TOP)

**Why:** Properties panel was missing Skolaris **Label Alignment** control.

**Files:**
- `resources/js/company-document-designer.js`

**Summary:**
- Added Label Alignment section in properties (left / right / top)
- Canvas preview respects `settings.label_align` on input fields
- Default alignment is `top` for new elements

## 16:07 — Signature draw or upload on memo submit

**Why:** Author Signature (and approver signature) should allow drawing on canvas or uploading an image file.

**Files:**
- `resources/js/signature-pad.js`
- `resources/js/company-documents.js`
- `resources/views/company-documents/submissions/_field.blade.php`
- `resources/views/company-documents/submissions/_signature-pad.blade.php`
- `resources/views/company-documents/approvals/index.blade.php`
- `resources/js/company-document-designer.js`

**Summary:**
- Signature fields use Draw / Upload tabs with canvas pad (touch + mouse)
- On submit, drawn signature exports to PNG and posts as file upload
- Wired on memo submission form and optional approver e-signature
- Multi-signature forms sync all pads before submit; `npm run build` run

## 16:12 — Remove approval from memo creation

**Why:** Memo submit should complete immediately — no approval routing for memo document type.

**Files:**
- `app/Models/CompanyDocumentForm.php`
- `app/Http/Controllers/CompanyDocumentSubmissionController.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `database/seeders/CompanyDocumentSampleMemoSeeder.php`
- `resources/views/company-documents/index.blade.php`
- `resources/views/company-documents/designer.blade.php`
- `resources/views/company-documents/submissions/create.blade.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Memo submissions mark status `completed` on submit; no approval rows seeded
- Sample memo seeder clears approval steps and updates success message
- Designer Approvals tab hidden for memo templates; index copy updated
- Feature test confirms memo completes without approval queue

## 16:15 — Remove New submission button from template card

**Why:** Memo templates should not expose a quick New submission action on the Company Documents card grid.

**Files:**
- `resources/views/company-documents/_card.blade.php`

**Summary:**
- Removed the + (New submission) icon button from each template card

## 16:16 — Remove New Submission from submissions list header

**Why:** User should not start a new memo from the submissions list page either.

**Files:**
- `resources/views/company-documents/submissions/index.blade.php`

**Summary:**
- Removed header **New Submission** action from the submissions index page

## 16:17 — Remove submission list from Company Documents

**Why:** Company Documents is template design only — no submission list, create flow, or Document Approvals sidebar entry.

**Files:**
- `resources/views/company-documents/_card.blade.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `routes/web.php`
- `database/seeders/SubModuleSeeder.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Removed submissions count and clipboard button from template cards
- Unregistered submission and approval web routes
- Deactivated **Document Approvals** sub-module in seeder

## 16:20 — Interactive field input in form designer

**Why:** Designer canvas should allow typing sample values while building the memo layout (live preview fill).

**Files:**
- `resources/js/company-document-designer.js`

**Summary:**
- Replaced disabled preview controls with editable inputs (text, date, textarea, dropdown, radio, checkbox, file, signature)
- Preview values persist while editing properties or reordering fields
- Clicking inside a field no longer selects the field chrome; use the gear button or empty area
- Improved drag reorder handle (`dataTransfer` + corrected move logic); `npm run build` run

## 16:22 — Fix field drag reorder in designer

**Why:** HTML5 drag on `<button>` handles was unreliable; fields could not be repositioned.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`

**Summary:**
- Replaced native drag with pointer-based reorder (mousedown + move + blue drop line)
- Drag handle is now a `div` (not button) for consistent grab behavior
- Drop targets use full field wrapper; `npm run build` run

## 16:25 — Free-form X/Y field positioning in designer

**Why:** User needs to drag fields anywhere on the canvas, not just reorder in a vertical list.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`

**Summary:**
- Canvas uses absolute positioning with saved `settings.pos_x` / `settings.pos_y`
- Drag field body or 6-dot handle to move freely; palette drop places at cursor
- Properties panel shows X/Y inputs; dotted grid background for alignment; `npm run build` run

## 16:29 — Fix field positions lost after save/reload

**Why:** Empty `settings_json` was stored as JSON array `[]`; JS assigned `pos_x`/`pos_y` on arrays which `JSON.stringify` dropped on save.

**Files:**
- `resources/js/company-document-designer.js`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Normalize settings to plain objects on load; always save explicit `pos_x` / `pos_y` in settings
- PHP normalizes element settings for designer load and storage
- Feature test confirms positions persist after save

## 16:34 — Designer image upload and free resize

**Why:** User needs to upload an image on the canvas and resize it freely (width/height in pixels).

**Files:**
- `app/Services/CompanyDocumentFileService.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `routes/web.php`
- `resources/views/company-documents/designer.blade.php`
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Upload PNG/JPEG/WebP via designer endpoint; assets served from authorized template folder
- Image element shows upload dropzone or preview; corner handle + properties W/H for free resize
- `image_width` / `image_height` and `options.path` persist on save; feature test added; `npm run build` run

## 16:39 — Designer image opacity

**Why:** Image fields needed an opacity control so logos and photos can be faded on the canvas.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Properties panel has an Opacity slider (0–100%) that updates the image live
- `image_opacity` is saved with width/height and clamped on the server
- Feature test asserts opacity persists after save

## 16:44 — Designer image save + PNG transparency

**Why:** Uploaded stamp images disappeared after Save, and PNGs still showed a white/black box.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `app/Services/CompanyDocumentFileService.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Image `options.path` is always saved as a plain object (empty JSON arrays were dropping the upload path)
- Uploaded images render without a white card/border so PNG alpha shows the form behind the stamp
- Paletted PNGs are converted to truecolor RGBA on upload so transparency survives
- Feature test checks PNG corner pixels stay transparent

## 16:45 — Designer image rotation

**Why:** Image fields needed a rotation control for stamps and logos on the canvas.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Properties panel has a Rotation slider (0–359°) plus -90° / +90° / Reset
- Rotation updates the image live via CSS transform and saves as `image_rotate`
- Feature test asserts rotation persists after save

## 16:47 — Remove designer submit preview

**Why:** The faded Submit Memo button on the canvas is not part of the memo layout.

**Files:**
- `resources/views/company-documents/designer.blade.php`
- `resources/js/company-document-designer.js`

**Summary:**
- Removed the dummy submit button from the designer canvas
- Real submit button stays on the fill-out form

## 16:51 — Designer paragraph text wrap

**Why:** Paragraph text stayed on one line and overflowed the canvas instead of wrapping.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `resources/views/company-documents/submissions/_field.blade.php`

**Summary:**
- Paragraph, heading, and section blocks use full width with word-break inside the positioned canvas box
- Canvas wrappers get `min-w-0` / `max-width` so absolute layout respects the field width
- Submission form paragraph also wraps with `whitespace-pre-wrap`

## 16:54 — Designer full width edge-to-edge

**Why:** Fields set to Full still had side gaps and did not reach the canvas edge.

**Files:**
- `resources/js/company-document-designer.js`

**Summary:**
- Full-width fields now span 100% of the canvas with X locked to 0
- Width is measured from the canvas container instead of subtracting side insets
- Half/third fields keep free X positioning; full-width X is read-only in properties

## 16:55 — Designer paragraph new lines

**Why:** Enter in a paragraph field did not insert a new line because the properties editor was a single-line input.

**Files:**
- `resources/js/company-document-designer.js`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `database/migrations/2026_08_28_150000_create_company_document_form_builder_tables.php`
- `database/migrations/2026_08_28_165500_change_company_document_element_label_to_text.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Paragraph properties use a textarea; Enter starts a new line and the canvas keeps `whitespace-pre-wrap`
- Element `label` is now TEXT (up to 5000 chars) so multi-line body copy can save
- Feature test asserts paragraph newlines persist after save

## 17:05 — Designer free field width and height

**Why:** Every field should be freely resizable, not limited to Full / Half / Third or image-only handles.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `resources/views/company-documents/designer.blade.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- All fields (text, date, paragraph, signature, etc.) have a corner drag handle plus Width/Height (px) in properties
- Full / Half / Third remain presets; after that you can still drag size freely
- `box_width` / `box_height` persist on save; feature test added
- Full-width fields use 100% of the canvas so they sit edge-to-edge until resized
