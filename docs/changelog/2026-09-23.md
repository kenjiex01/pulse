## 11:35 — People360 1.0.8 desktop build

**Why:** Ship dashboard performance and table skeleton loading to campus desktops and auto-update feed.

**Files:**
- `dist/People360-1.0.8-arm64.dmg`
- `dist/People360-1.0.8-arm64.zip`
- `dist/People360-1.0.8-setup.exe`

**Summary:**
- macOS arm64 DMG + ZIP and Windows x64 NSIS installer built.
- GitHub release `v1.0.8` published with `latest.yml` / `latest-mac.yml` for auto-update.

## 11:20 — Bump desktop app version to 1.0.8

**Why:** Ship dashboard performance and table skeleton loading in a new People360 installer.

**Files:**
- `.env.example`

**Summary:**
- `NATIVEPHP_APP_VERSION=1.0.8` for macOS + Windows desktop build.

## 11:12 — Table skeleton loading across People360

**Why:** Users want the same skeleton placeholder on all data tables while rows load (filters, pagination, AJAX tabs, sync modals).

**Files:**
- `resources/views/partials/table-skeleton.blade.php`
- `resources/views/partials/table-skeleton-rows.blade.php`
- `resources/views/partials/live-data-table-loading.blade.php`
- `resources/js/table-skeleton.js`
- `resources/js/app.js`
- `resources/js/timekeeping-memo.js`
- `resources/css/app.css`
- `resources/views/layouts/app.blade.php`
- `resources/views/employees/_sync-form.blade.php`
- `resources/views/employees/_sync-view.blade.php`
- `resources/views/employees/_form.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`

**Summary:**
- Reusable table skeleton partial + JS helpers for AJAX-loaded tables.
- Live data tables (Employees, Users, Roles, Time Logs, etc.) show skeleton overlay on search/filter/page change.
- Employee sync, profile lazy tabs, and memo modals use skeleton rows instead of plain “Loading…” text.

## 11:10 — Dashboard skeleton while Biometric Collector status loads

**Why:** Plain “Loading…” text felt unfinished; users need a visual placeholder matching the S3 status panel.

**Files:**
- `resources/views/dashboard/_biometric-collector-status-skeleton.blade.php`
- `resources/views/dashboard.blade.php`
- `tests/Feature/DashboardBiometricCollectorStatusTest.php`

**Summary:**
- Pulse skeleton (header, badges, two table columns) shows until AJAX replaces it with live S3 status.

## 11:01 — Faster dashboard load (lazy Biometric Collector status)

**Why:** Dashboard felt slow because it blocked on S3 file listing and a full scan of biometric import history before the page appeared.

**Files:**
- `app/Http/Controllers/DashboardController.php`
- `app/Services/BiometricCollectorDashboardService.php`
- `routes/web.php`
- `resources/views/dashboard.blade.php`
- `resources/js/app.js`
- `tests/Feature/DashboardBiometricCollectorStatusTest.php`

**Summary:**
- Dashboard shell loads first; Biometric Collector S3 status loads via AJAX afterward.
- `lastPulledAtByCampus()` uses grouped SQL instead of chunking every biometric import row.
