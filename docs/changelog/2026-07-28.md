## 09:50 — Fix previous salary not archived on same-date amount change

**Why:** Hybrid (and other) employees lost previous salary when Basic Income changed with the same Effectivity From — e.g. 25000 → 25000.01 left Previous Salary empty because the old row was force-deleted.

**Files:**
- `app/Services/EmployeeSalarySync.php`
- `resources/views/employees/partials/_employee-salary.blade.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/js/app.js`
- `tests/Unit/EmployeeSalaryHistorySyncTest.php`

**Summary:**
- Same effectivity date + salary content change now archives the open row (`date_effective_to = date_effective_from`) and inserts a new current salary — never `forceDelete` the history.
- Later effectivity dates still close the prior period as `new from − 1 day`.
- Hybrid salary sync no longer force-deletes employment salaries when a panel payload is missing; trims with soft delete only.
- Submit prep clears `disabled` via `removeAttribute` so desktop/webview posts active hybrid salary fields reliably.
- Unit test covers hybrid Faculty 25000 → 25000.01 on `2026-07-14` keeping previous + current rows.

## 09:54 — Previous Salary table pagination + Show all

**Why:** Previous Salary list needs paging when history grows, with a Show all option like other Pulse tables.

**Files:**
- `resources/views/employees/partials/_employee-salary-previous.blade.php`
- `resources/js/app.js`

**Summary:**
- Wrapped Previous Salary rows in `data-client-paginate` (10 per page default).
- Reused shared pagination controls: Show 10/25/50/100, Previous/Next, page numbers, **Show all** / **Show pages**.
- Initializes pagination when the salary panel’s Previous tab is set up.

## 09:58 — Always show Previous Salary pagination controls

**Why:** With only 1 previous salary row, controls were hidden so browser looked like it had no pagination.

**Files:**
- `resources/views/employees/partials/_employee-salary-previous.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `data-paginate-always-show` so Previous Salary always shows Show / Show all even with a single row.
- Other paginated tables keep the old hide-when-1-row behavior.

## 10:08 — Fix native:serve empty SQLite (no such table: cache)

**Why:** `php artisan native:serve` login failed with `no such table: cache` because first-launch created a 0-byte `pulse.sqlite` then skipped migrate when `hasPendingMigrations()` crashed without a `migrations` table.

**Files:**
- `app/Providers/AppServiceProvider.php`
- `scripts/reset-pulse-dev-db.sh`

**Summary:**
- Treat 0-byte SQLite as first launch; always run `migrate --force`; reconnect after pointing DB config at desktop path.
- Added `scripts/reset-pulse-dev-db.sh` to wipe/migrate/seed `~/Library/Application Support/pulse-dev/.../pulse.sqlite`.

## 10:16 — Fix Excel report download on desktop (missing XMLWriter)

**Why:** Payroll Register / SSS Excel download crashed with `Class "XMLWriter" not found` — NativePHP bundled PHP has no `ext-xmlwriter`, which PhpSpreadsheet Xlsx requires.

**Files:**
- `app/Support/SpreadsheetDownload.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `app/Services/Reports/SssContributionReportService.php`
- `tests/Unit/SpreadsheetDownloadTest.php`

**Summary:**
- Shared downloader uses `.xlsx` when `xmlwriter` is available; otherwise falls back to Excel 97-2003 `.xls` (works on desktop PHP).
- Payroll Register and SSS Contribution Excel exports use the helper.

## 10:23 — Accept legacy employee upload template headers

**Why:** Desktop upload of `employee_upload_template4.xlsx` failed with “Invalid template header” — file still used old `salary_date_effective` (122 cols) instead of `salary_date_effective_from` / `_to` (124 cols).

**Files:**
- `app/Services/EmployeeUploadService.php`

**Summary:**
- Header matching is now column-map based (not exact full-row equality).
- Legacy `salary_date_effective` / `salary2_date_effective` map to the new `*_from` aliases; missing `*_to` stays blank.
- Requires `employee_number`, `first_name`, and `last_name` to recognize a header row.

## 09:53 — Fix desktop stuck loader after closing report preview

**Why:** Generating an HTML payroll report left the main NativePHP window on the SKOLARIS loading screen after the preview tab was closed.

**Files:**
- `resources/js/app.js`
- `resources/views/payroll/reports/index.blade.php`

**Summary:**
- HTML report submit opens `_blank` without navigating the origin page, but the global form handler still showed the full-screen loader — it never hid.
- Skip full-screen loader for `target="_blank"` (and download) form submits.
- Reports generate form uses `data-no-loader`; HTML path explicitly hides any residual loader after setting `_blank`.

## 09:55 — Fix blank white screen on Excel report download (desktop)

**Why:** Excel report generation posted into the NativePHP webview; the attachment response left a blank white window instead of saving the file.

**Files:**
- `resources/js/app.js`

**Summary:**
- Excel generate now `preventDefault`s, fetches the `.xlsx` as a blob, and triggers a client-side download so the main window stays on Reports.
- Non-attachment / HTML responses (validation errors) are written back into the document so flash/errors still show.
- HTML preview path unchanged (`target="_blank"`).

## 11:10 — Desktop build 0.1.30 (Windows ready; macOS DMG pending hdiutil)

**Why:** Ship salary history, spreadsheet fallback, legacy employee-upload headers, and related fixes as installers.

**Files:**
- `pulse/.env.example` (`NATIVEPHP_APP_VERSION=0.1.30`)
- `scripts/build-desktop.sh`
- `scripts/patches/PrunesVendorDirectory.php`
- `scripts/patches/BuildCommand.php`

**Summary:**
- Bumped version to **0.1.30**.
- Build script: offline vendor prune (skip php-bin re-download), npm install instead of npm ci, fail if installers missing.
- **Windows OK:** `pulse/dist/Pulse-0.1.30-setup.exe`
- **macOS:** `Pulse.app` + `Pulse-0.1.30-arm64.zip` built; DMG blocked in Cursor sandbox (`hdiutil: Device not configured`). Create DMG from Terminal: `pulse/tmp/make-0.1.30-dmg.sh`

## 11:35 — Full Name column on upload adjustment templates

**Why:** Users need employee name on Incomes, Deductions, and Hours Worked CSV templates for reference when filling uploads.

**Files:**
- `config/payroll_transaction.php`
- `app/Services/PayrollTransactionUploadService.php`
- `docs/samples/hours-worked-upload-2026-00003-sample.csv`
- `scripts/capture-hours-worked-sample-image.mjs`

**Summary:**
- Added **Full Name** column after Employee No. on all three upload types (reference-only; not stored on import).
- Download Template from a pay period prefills name from employee master when employee numbers are listed.
- Upload parser accepts the column and ignores it for validation/commit.

## 12:08 — Fix desktop app crash on launch (0.1.31)

**Why:** Installed Pulse 0.1.30/0.1.31 would not open — Laravel boot tried to load dev-only `PailServiceProvider` removed during desktop vendor prune.

**Files:**
- `scripts/patches/PrunesVendorDirectory.php`
- `scripts/rebuild-desktop-package-manifest.php`
- `pulse/.env.example` (`NATIVEPHP_APP_VERSION=0.1.32`)

**Summary:**
- After pruning require-dev packages, rebuild `vendor/composer/installed.json` and `bootstrap/cache/packages.php` without Pail/Pao/Collision.
- **Ship 0.1.32** — use new Windows EXE / macOS ZIP (not 0.1.31).

## 13:25 — Fix mac desktop build (DMG vs Cursor sandbox)

**Why:** DMG step failed inside electron-builder (`hdiutil` blocked in Cursor agent); full mac build reported failure even when ZIP was OK.

**Files:**
- `scripts/patches/electron-builder.js` (mac target ZIP only)
- `scripts/build-desktop.sh` (post-step DMG via `scripts/make-mac-dmg.sh`)
- `scripts/make-mac-dmg.sh`

**Summary:**
- electron-builder produces **ZIP** only; DMG is created afterward with `hdiutil` when the environment allows (Terminal.app).
- Cursor agent cannot run `hdiutil` — use ZIP or run `./scripts/make-mac-dmg.sh` in Terminal for DMG.


**Why:** Release upload-template Full Name column and prior fixes as installers.

**Files:**
- `pulse/.env.example` (`NATIVEPHP_APP_VERSION=0.1.31`)
- `pulse/tmp/make-mac-dmg.sh`

**Summary:**
- **Windows:** `pulse/dist/Pulse-0.1.31-setup.exe`
- **macOS ZIP:** `pulse/dist/Pulse-0.1.31-arm64.zip` (unzip → `Pulse.app`)
- **macOS DMG:** run in Terminal: `pulse/tmp/make-mac-dmg.sh` (Cursor blocks `hdiutil`)
