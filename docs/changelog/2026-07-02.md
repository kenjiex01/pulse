## 21:18 — Payroll batch employee detail: Back to batch modal

**Why:** From the Employee Payroll Detail modal, users need a way to return to the payroll batch modal they came from without closing everything.

**Files:**
- `resources/views/partials/modal.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- Added optional `backUrl` support to the shared modal partial — renders a back arrow in the header (same style as page back headers).
- Employee Payroll Detail modal now passes `backUrl` pointing to the batch view (`view_payroll_batch` only, without `view_batch_detail`), preserving batch employee search and list search query params.

## 21:25 — Modal: fix white strip below overlay on desktop

**Why:** On large screens, `body.modal-open` still allowed page scroll (`lg:overflow-auto`), so the page behind the modal could scroll and expose a white/slate strip at the bottom.

**Files:**
- `resources/css/app.css`

**Summary:**
- Split `body.modal-open` from `body.sidebar-open` — modals now always lock body scroll (`overflow-hidden`) on all breakpoints.
- Modal overlay uses `min-height: 100dvh` and `overflow-y-auto` so the dimmed backdrop covers the full viewport and tall modals scroll inside the overlay.

## 21:23 — Sidebar: full viewport height

**Why:** White/slate strip showed below the sidebar on tall screens because `h-full` did not reliably stretch the fixed aside to the bottom of the viewport.

**Files:**
- `resources/views/partials/sidebar.blade.php`

**Summary:**
- Replaced `top-0 h-full` with `inset-y-0` on `#app-sidebar` so the blue background always reaches the bottom edge of the screen.

## 22:05 — Deduction types: lock govt premiums as always active

**Why:** SSS, Pag-IBIG, and PhilHealth premium deduction types are core government contributions and must not be deactivated.

**Files:**
- `config/payroll_maintenance.php`
- `app/Support/PayrollMaintenance.php`
- `app/Http/Controllers/PayrollMaintenanceController.php`
- `resources/views/payroll/maintenance-table/_results.blade.php`

**Summary:**
- Added `always_active_codes` (`PHIL`, `PIBG`, `SSSP`) for deduction-types.
- Status toggle hidden for those rows; server rejects toggle-status requests.
- Edit remains available for employer amount and other fields.

## 22:24 — Government Tables: remove Withholding Tax Classification tab

**Why:** Withholding tax classification (S/ME/Z codes) is legacy under TRAIN 2023 and no longer used in payroll.

**Files:**
- `config/government_tables.php`
- `app/Support/GovernmentTables.php`
- `routes/web.php`
- `resources/views/payroll/government-tables/index.blade.php`
- `resources/views/payroll/government-tables/_form.blade.php`
- `database/seeders/GovernmentTablesSeeder.php`

**Summary:**
- Removed `wtax-classification` tab, config, route, and seeder entries.
- Government Tables now shows Pag-IBIG, PhilHealth, SSS, and Withholding Tax only.

## 22:38 — Employee Load sample CSV (late scenarios)

**Why:** Provide upload test data with multiple dates where Time In is after the scheduled class start (late), for faculty payroll testing.

**Files:**
- `docs/samples/employee-load-sample-late.csv`

**Summary:**
- New 9-row sample (Jun 2–19, 2026) for employees `2026-00001` and `2026-00003`.
- Mix of on-time, minor late (2 min), moderate late (15–45 min), and major late (1 hr+) rows; remarks document expected late minutes.
- Validated via `EmployeeLoadUploadService` parser (9 valid, 0 errors) for date range `2026-06-01` to `2026-06-30`.

## 22:40 — Payroll batch re-process

**Why:** Processed batches need a way to regenerate income and deduction lines (e.g. after salary or employee load changes) without creating a new batch.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_show-content.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- Added `canReprocessBatch()` (status Processed + has employees) and `reprocessBatch()` — clears all income/deduction lines per employee, regenerates from employee salary, updates processed-by/timestamp.
- New route `POST payroll/transaction/batches/{batch}/reprocess` with **Re-process** button on batch modal (confirmation warns manual adjustments will be lost).
- Posted batches remain non-editable; re-process is not offered once posted.

## 22:47 — Payroll batch: basic income from Employee Load (Time-In/Time-Out)

**Why:** Faculty with Time-In/Time-Out basic computation should earn per days worked from Employee Load, not the full salary template amount.

**Files:**
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `database/seeders/PayrollMaintenanceSeeder.php`

**Summary:**
- New `EmployeeLoadPayrollService` counts unique load session dates with Time In within the pay period, computes BASC as `(salary BASC ÷ days per period) × worked days`.
- Applies on Process/Re-process when employee salary `basic_computation_id` is Time-In/Time-Out; other computation modes unchanged.
- Auto-adds **Late** deduction (`LTDE`) when Time In is after class schedule start — `(late minutes ÷ 60) × hourly rate`.
- Example `2026-00003` in batch 0002 (Jun 1–15): 2 days worked → BASC **12,903.10**; 20 min late → **268.81** Late deduction.

## 22:51 — Employee Profile: remove Team Subordinates Viewing Limit

**Why:** Field is not needed on per-employee Timekeeping Setup (policy-level team settings remain elsewhere).

**Files:**
- `resources/views/timekeeping/employee-profile/_setup-tab-timekeeping.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`

**Summary:**
- Removed Team Subordinates Viewing Limit dropdown from Timekeeping Setup form and view modal.
- Dropped validation/save of `timekeeping_policy_team_setting_id` on employee profile store; removed unused `teamSettings` form option.

## 22:54 — Employee Profile: Employee Load tab

**Why:** Users need to see an employee's uploaded faculty load sessions from the Timekeeping Setup modal.

**Files:**
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`

**Summary:**
- Added **Employee Load** tab (after Attendance View) on setup and view modals.
- Shows session summary (sessions, days worked, with time in) and table of load entries; lazy-loaded in edit modal via `GET .../employee-load`.
- Link to **Open Employee Load** module when user has access.

## 22:58 — Employee Profile: Employee Load table pagination

**Why:** Faculty load sessions can exceed one screen; the Employee Load tab needs paginated rows.

**Files:**
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `resources/js/app.js`

**Summary:**
- Employee load entries paginate at 10 per page (`employee_load_page`); summary cards still show totals across all sessions.
- Added standard pagination controls below the table; AJAX page changes work inside setup/view modals.
- View modal Employee Load tab now lazy-loads (same endpoint as edit modal).

## 22:58 — Employee Profile: Employee Load sort ASC

**Why:** Load sessions should list earliest date and class schedule first.

**Files:**
- `app/Support/TimekeepingEmployeeProfile.php`

**Summary:**
- Employee Load tab query now sorts `session_date` ASC, then `class_schedule` ASC (was newest-first).

## 23:05 — Employee Profile: Late / Undertime / Overtime columns

**Why:** Employee Load tab should show computed tardiness metrics per session, not only free-text remarks.

**Files:**
- `app/Services/EmployeeLoadPayrollService.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`

**Summary:**
- Added **Late**, **Undertime**, and **Overtime** columns — derived from Time In/Out vs class schedule start/end.
- Reused payroll load service helpers (`undertimeMinutesForEntry`, `overtimeMinutesForEntry`, `formatDurationMinutes`).

## 23:11 — Tardiness equivalents (late conversion rules)

**Why:** Users need a place to configure late brackets (e.g. 1–5 min → 5 min, 6–15 → 15 min, 16+ → absent) and have Employee Load / payroll honor them.

**Files:**
- `database/migrations/2026_07_02_120000_add_marks_absent_to_timekeeping_policy_tardiness.php`
- `app/Models/TimekeepingPolicyTardiness.php`
- `config/timekeeping_policy.php`
- `app/Support/TimekeepingPolicy.php`
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`
- `resources/views/timekeeping/policy/_equivalent-form.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`

**Summary:**
- Restored **Tardiness Equivalents** (and Undertime Equivalents) on Policy → **Tardiness and Undertime** tab.
- Added `marks_absent` flag for ranges that should mark a session absent (e.g. 16+ minutes late).
- `resolveTardinessEquivalent()` maps raw late minutes to billable equivalent; Employee Load Late column and payroll LTDE use converted minutes; absent sessions exclude the day from worked days when no other qualifying session exists.
- Employee must have the correct **Policy Group** under Employee Profile → Timekeeping Settings.

## 23:19 — Tardiness equivalent form UI fixes

**Why:** Late equivalent modal had misaligned inputs; users opened Undertime modal and did not see Mark as absent.

**Files:**
- `resources/views/timekeeping/policy/_equivalent-form.blade.php`
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`
- `config/timekeeping_policy.php`
- `resources/js/app.js`

**Summary:**
- Aligned From / To / Equivalent inputs with equal label height and shorter labels.
- **Mark as absent** shown only on **Tardiness Equivalent (Late)** form (highlighted box); Undertime section notes that late rules belong above.
- Checkbox disables equivalent field and sets 0 when absent is selected.

## 23:21 — Hide Undertime Equivalents on policy tab

**Why:** User does not need undertime equivalent brackets on the Tardiness and Undertime policy screen.

**Files:**
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`

**Summary:**
- Removed **Undertime Equivalents** table/modals from Policy → Tardiness and Undertime tab.
- **Tardiness Equivalent (Late)** section remains; undertime rounding/leave type settings in the form are unchanged.

## 23:25 — Payroll batch: full tardiness equivalent on process

**Why:** Tardiness Equivalent rules must drive late minutes, worked days, and LTDE when processing/re-processing payroll batches.

**Files:**
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`

**Summary:**
- Process/Re-process loads employee **Policy Group** (grace period, flexi, tardiness equivalents).
- Late: grace/flexi window → tardiness equivalent brackets → billable minutes for LTDE `(equiv min ÷ 60) × hourly rate`.
- **Mark as absent** ranges: no late deduction; day excluded from worked days if all sessions that day are absent.
- Employee Load tab uses the same policy-aware logic for display.

## 10:51 — Time logs payroll + Net Pay tab on batch employee detail

**Why:** Uploaded time logs were ignored during batch process; employee detail modal needed a Net Pay summary.

**Files:**
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `app/Support/PayrollTransactionModule.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`

**Summary:**
- Process/Re-process now prefers **uploaded time logs** (`raw_timekeeping_inandout`) over Employee Load when salary **Basic Computation** is **Time-In/Time-Out**; shift code `time_in` is the schedule start for late/grace/flexi.
- **Leaves** basic computation still uses full template BASC (no attendance adjustment).
- Auto **LTDE** late deduction when billable late minutes exist after policy equivalents.
- Employee Payroll Detail modal: new **Net Pay** tab (gross income − employee deductions).

## 10:58 — Undertime (UTDE) deduction on batch process

**Why:** Late and undertime were not reducing net pay; undertime was never wired for time logs or employee load.

**Files:**
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `app/Support/TimekeepingPolicy.php`
- `database/seeders/PayrollMaintenanceSeeder.php`

**Summary:**
- Added **UTDE** (Undertime) deduction type; process/re-process auto-adds when billable undertime minutes exist.
- Time logs: compares time out vs shift `time_out`; employee load uses class schedule end (existing helper).
- `resolveUndertimeEquivalent()` applies policy undertime brackets when configured.
- **Basic income** stays `daily rate × worked days`; late/undertime reduce **deductions / net pay**, not BASC (unless marked absent).

## 11:07 — Fix tardiness “Mark as absent” validation

**Why:** Creating a tardiness equivalent with **Mark as absent** failed — disabled Equivalent field was not submitted, so server returned “equivalent is required”.

**Files:**
- `app/Support/TimekeepingPolicy.php`
- `resources/views/timekeeping/policy/_equivalent-form.blade.php`
- `resources/js/app.js`

**Summary:**
- When `marks_absent` is checked, `equivalent` is optional server-side and saved as `0`.
- Form uses `readonly` instead of `disabled` so `0` still posts with the form.

## 11:22 — Net Pay tab breakdown layout (PATHS-style)

**Why:** User wanted Net Pay tab to show income/deduction line breakdowns like legacy PATHS batch detail.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`

**Summary:**
- Two-column layout: left = income lines + **Gross Income**; right = each deduction (e.g. Late, Undertime) + **Total Deductions** as negative amounts.
- Amber footer bar with **Net Pay** in bold red.

## 11:25 — Government deductions on payroll batch process

**Why:** Payroll calendar had SSS/PhilHealth/Pag-IBIG/WHTX checked but batch process never computed them from Government Tables.

**Files:**
- `app/Services/GovernmentDeductionPayrollService.php`
- `app/Services/PayrollBatchService.php`

**Summary:**
- Process/Re-process reads **Payroll Calendar → Deductions** schedule (`tbl_payroll_calendar_deductions`) for govt deduction types.
- Computes **PIBG**, **PHIL**, **SSSP** from seeded government tables using period taxable gross; **WHTX** (Regular) from TRAIN 2023 table by pay type frequency.
- Skips govt types already on employee salary template; attendance **LTDE/UTDE** still added after govt premiums.
