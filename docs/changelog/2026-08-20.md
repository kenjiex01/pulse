# 2026-08-20

## 08:58 — Retry unmatched punches on partially imported S3 log files

**Why:** After a partial S3 pull, newly added employees with campus biometric IDs should still get their punches from the same file.

**Files:**
- `app/Services/BiometricLogsS3PullService.php`
- `tests/Unit/BiometricLogsS3PullServiceTest.php`

**Summary:**
- Re-read S3 files on every pull instead of skipping by filename
- Already-imported punches are skipped as duplicates; only new matches are inserted
- No empty batch is created when a retry has nothing new to insert

## 11:54 — Payroll Register Excel: one worksheet per campus

**Why:** Payroll Register Excel should split employees by campus, not by pay-period tab.

**Files:**
- `app/Services/Reports/PayrollRegisterReportService.php`
- `app/Services/Reports/PayrollRegisterExcelExporter.php`
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `config/payroll_register_layout.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `database/seeders/ReportSeeder.php`
- `tests/Unit/PayrollRegisterExcelExporterTest.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Excel `sheet_group` is campus again (one tab per campus; empty campuses omitted)
- Mapped all ICCT campus codes (AG, UA, BI, CA, CO, DC, SA, SU, TA, GH, ND, WR, O8, F6)
- Unmapped campus names become their own worksheet instead of merging into Cainta
- Header line includes `Campus: {sheet}` with the period covered

## 11:59 — Payroll Register name: Last, First Middle; sort by last name

**Why:** Payroll Register should print employee names as last name first and list rows alphabetically by last name.

**Files:**
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Name column is `Last Name, First Name Middle Name` (e.g. Dela Cruz, Juan Santos)
- Default Sort By is Employee Name; sort uses last name, then first, then middle

## 12:08 — Desktop build Pulse 0.1.59 (macOS + Windows)

**Why:** Ship Payroll Register per-campus Excel worksheets and last-name employee format in paired installers.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.59`)
- `dist/Pulse-0.1.59-arm64.dmg`
- `dist/Pulse-0.1.59-setup.exe`

**Summary:**
- Version bumped 0.1.58 → 0.1.59
- macOS DMG and Windows setup.exe built; uploaded to S3 `payroll_installer/`
- Includes Payroll Register: one worksheet per campus; names as Last, First Middle sorted by last name

## 12:19 — Human Resource report: Employee Credentials

**Why:** Add an HR report listing employees with one column per credential document type, marked Yes when uploaded.

**Files:**
- `app/Services/Reports/EmployeeCredentialsReportService.php`
- `resources/views/payroll/reports/options/employee-credentials.blade.php`
- `config/payroll_reports.php`
- `database/seeders/ReportSeeder.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `tests/Unit/EmployeeCredentialsReportServiceTest.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- New report **Employee Credentials** under Human Resource
- Rows: employees (Last Name, First Name Middle Name), sorted by last name
- Columns: Employee No., Employee Name, then each active document type
- Cell is `Yes` when that document type is already uploaded; blank if not
- Preview / Excel / PDF; optional employee filter

## 12:25 — Employee report includes full employee details

**Why:** The Human Resource Employee report should list all employee data, not credentials only.

**Files:**
- `app/Services/Reports/EmployeeCredentialsReportService.php`
- `resources/views/payroll/reports/options/employee-credentials.blade.php`
- `database/seeders/ReportSeeder.php`
- `tests/Unit/EmployeeCredentialsReportServiceTest.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Report type title is **Employee**
- Columns include personal, government IDs, contact, address, campus assignments, employment, salary (pay type, rates, incomes/deductions), shift code / timekeeping, loans
- Credential document types remain as Yes/blank columns at the end

## 12:32 — Employee report: tag hybrid salary with Faculty/Staff

**Why:** Hybrid employees have two salaries; each value should show which employment it belongs to.

**Files:**
- `app/Services/Reports/EmployeeCredentialsReportService.php`
- `tests/Unit/EmployeeCredentialsReportServiceTest.php`

**Summary:**
- Salary columns append `(Faculty)`, `(Staff)`, or `(Admin)` only when `is_hybrid` is true
- Non-hybrid salary cells stay untagged

## 12:49 — Main assignment checkbox and Employee Assignment upload

**Why:** Let HR pick one main campus per employee and bulk-change that main campus from a prefilled upload template.

**Files:**
- `app/Services/EmployeeCampusAssignmentSync.php`
- `app/Services/EmployeeUploadService.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `config/employee_upload.php`
- `config/employee_assignment_upload.php`
- `resources/views/employees/partials/_campus-assignment-row.blade.php`
- `resources/views/employees/partials/_campus-assignments.blade.php`
- `resources/views/employees/_upload-form.blade.php`
- `resources/views/employees/_upload-preview.blade.php`
- `resources/js/app.js`
- `tests/Unit/EmployeeCampusAssignmentSyncTest.php`
- `tests/Unit/EmployeeUploadRowMapperTest.php`
- `tests/Unit/EmployeeAssignmentUploadTemplateTest.php`

**Summary:**
- Campus assignment cards have a **Main assignment** checkbox; only one can be checked
- Saving uses `is_primary` instead of always treating the first card as primary
- New upload type **Employee Assignment** downloads a live template of all employees with the current main campus code
- Uploading a different assigned campus makes it the new main and unchecks the previous one

## 12:54 — Payroll Register Excel uses Main assignment campus

**Why:** Employees should appear on the Payroll Register worksheet of the campus marked as Main assignment, not a stale employee campus.

**Files:**
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `config/payroll_register_layout.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `database/seeders/ReportSeeder.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Campus worksheet grouping prefers the checked **Main assignment** (`is_primary`)
- Falls back to the first assignment, then the employee campus, then Cainta

## 13:05 — Campus Under Campus and Payroll Register parent sheets

**Why:** Satellite campuses like Greenhills should optionally sit under a parent campus and print on that parent’s Payroll Register worksheet.

**Files:**
- `database/migrations/2026_08_20_125900_add_parent_campus_id_to_tbl_campuses_table.php`
- `app/Models/Campus.php`
- `config/hr_lookups.php`
- `app/Support/HrLookup.php`
- `resources/views/hr-lookups/_form.blade.php`
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Campuses form has optional **Under Campus** dropdown (not required)
- Payroll Register Excel puts employees on the parent campus worksheet when Under Campus is set
- Existing Greenhills (`GH`) is linked under Cainta (`CA`) once on migrate; can be changed later

## 14:34 — Campus minimum wage field

**Why:** HR needs to store the minimum wage amount per campus directly on the campus record.

**Files:**
- `database/migrations/2026_08_20_143300_add_minimum_wage_to_tbl_campuses_table.php`
- `app/Models/Campus.php`
- `config/hr_lookups.php`
- `resources/views/hr-lookups/_form.blade.php`

**Summary:**
- Added nullable `minimum_wage` on campuses
- Add/Edit Campus modal now includes a `Minimum Wage` numeric field
- Campus list config now exposes the saved minimum wage value

## 19:47 — Encrypt API/S3 secrets at rest in .env

**Why:** Keep Skolaris Pulse API key and S3 credentials encrypted on disk; decrypt only in memory when used.

**Files:**
- `app/Support/EncryptedEnv.php`
- `app/Console/Commands/EncryptEnvSecretsCommand.php`
- `app/Providers/AppServiceProvider.php`
- `app/Services/SkolarisApiService.php`
- `.env.example`
- `tests/Unit/EncryptedEnvTest.php`

**Summary:**
- Secrets use `enc:…` values in `.env` (Laravel Crypt + `APP_KEY`)
- Boot decrypts configured paths into memory; Skolaris client also reveals the API key when calling
- Command: `php artisan secrets:encrypt-env`
- Plaintext values still work until encrypted (backward compatible)

## 12:56 — ISKOLARIS Approve: per-row, selected, and Approve all

**Why:** Approvers need a row action, checkbox bulk for selected rows only, and a top-level Approve all.

**Files:**
- `resources/views/employees/_sync-form.blade.php`
- `resources/js/app.js`
- `tests/Feature/EmployeeSkolarisSyncTest.php`

**Summary:**
- Per-row **Approve** still applies that employee only
- **Approve selected** (beside Select all shown) applies checked rows only
- **Approve all** at the top applies every shown row after search, with a confirm prompt

