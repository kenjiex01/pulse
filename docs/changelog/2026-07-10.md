## 09:58 — Income adjustment upload sample file

**Why:** User requested a sample file for uploading income adjustments.

**Files:**
- `docs/samples/income-adjustment-upload-sample.txt`

**Summary:**
- Added tab-delimited sample with template header rows plus two example lines (OVRT taxable, OTHR non-taxable).
- Uses existing employee numbers `2026-00003` and `2026-00001`.

## 10:05 — Build macOS desktop installer (NativePHP)

**Why:** User requested a desktop app build.

**Files:**
- `dist/Pulse-0.1.0-arm64.dmg` (generated)
- `dist/Pulse-0.1.0-arm64.zip` (generated)
- `dist/mac-arm64/Pulse.app` (generated)

**Summary:**
- Ran `scripts/build-desktop.sh mac` (icons, Vite build, `native:build mac arm64`).
- Fresh macOS Apple Silicon DMG/zip produced in `pulse/dist/`.
- Build logged adhoc signing / notarization warnings (no Apple Developer ID on this machine); app still packages for local install/testing.

## 10:15 — Build Windows desktop installer (NativePHP)

**Why:** User requested the Windows build after macOS DMG.

**Files:**
- `dist/Pulse-0.1.0-setup.exe` (generated)
- `dist/win-unpacked/` (generated)

**Summary:**
- Ran `scripts/build-desktop.sh win` (`native:build win x64`).
- Fresh Windows x64 NSIS installer produced in `pulse/dist/`.
- Code signing skipped (no cert on build machine) — OK for internal/testing installs.

## 14:40 — Add Income button on Employee Payroll Detail

**Why:** User wants to add income lines per employee from the batch detail modal, similar to upload income but scoped to the open employee.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Services/GovernmentDeductionPayrollService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Support/PayrollTransactionModule.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-income-form.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-income-modal.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- Added **Add Income** on the Incomes tab when the batch is editable and the employee has processed payroll lines.
- Nested modal form: income type, taxable, non-taxable — posts directly to `trn_payroll_incomes` for that employee/detail.
- Refreshes government deduction lines after add when taxable income changes; writes `sys_logs` on create.

## 14:50 — Preserve manual/uploaded income on re-process

**Why:** Re-process was wiping incomes added via Add Income button or upload; user wants those kept.

**Files:**
- `database/migrations/2026_07_10_140000_add_is_manual_to_trn_payroll_incomes.php`
- `app/Models/PayrollIncome.php`
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_show-content.blade.php`

**Summary:**
- Added `is_manual` on `trn_payroll_incomes`; Add Income button sets `is_manual = true`.
- Re-process clears only system-generated incomes; manual button lines are preserved.
- Uploaded incomes merge from `raw_payroll_incomes` on process/re-process (same pattern as deductions) via `applyUploadedIncomesToDetail`.
- `applyRawUploadToOpenBatches` now applies income uploads immediately to open batches.
- Updated re-process confirm message to reflect preserved manual/uploaded lines.

## 14:54 — Add Deduction button on Employee Payroll Detail

**Why:** User requested Add Deduction on the deductions tab, matching Add Income / upload deduction behavior for the open employee.

**Files:**
- `database/migrations/2026_07_10_145400_add_is_manual_to_trn_payroll_deductions.php`
- `app/Models/PayrollDeduction.php`
- `app/Services/PayrollBatchService.php`
- `app/Services/GovernmentDeductionPayrollService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Support/PayrollTransactionModule.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-deduction-form.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-deduction-modal.blade.php`
- `resources/views/payroll/transaction/index.blade.php`
- `resources/views/payroll/transaction/_show-content.blade.php`
- `resources/js/app.js`

**Summary:**
- Added **Add Deduction** button + nested modal (type, hours for LTDE/UTDE, employee/employer amounts, reference fields).
- Manual deductions use `is_manual = true` and are preserved on re-process; uploaded deductions still re-merge from raw.
- Government deduction types are excluded from manual add; `sys_logs` on create.

## 15:04 — Fix Hours field in Add Deduction modal

**Why:** Hours input was hidden by JS that did not re-init when the nested modal opened; LTDE/UTDE required hours but the field never appeared.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-add-deduction-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Hours field is always visible in the Add Deduction form; asterisk toggles only for LTDE/UTDE.
- Refactored `initBatchAddDeductionForm` and re-run it when the modal opens so `required` state syncs on type change.
- Rebuilt frontend assets.

## 15:11 — Hours field visible only for LTDE/UTDE

**Why:** User requested Hours show only for Late and Undertime, not for all deduction types.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-add-deduction-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Hours wrap hidden by default; shown when deduction type is LTDE or UTDE.
- Moved batch add-deduction JS before modal open handler; added document-level change listener so toggle works reliably in nested modal.
- Clears hours value when switching to a type that does not need hours.

## 15:24 — Philhealth Minimum government table tab

**Why:** User requested a new Government Tables tab for PhilHealth minimum premium (edit-only, seeded at 250).

**Files:**
- `database/migrations/2026_07_10_151900_create_govt_table_philhealth_minimum.php`
- `app/Models/GovtTablePhilhealthMinimum.php`
- `config/government_tables.php`
- `app/Support/GovernmentTables.php`
- `app/Http/Controllers/GovernmentTablesController.php`
- `app/Services/GovernmentDeductionPayrollService.php`
- `routes/web.php`
- `resources/views/payroll/government-tables/index.blade.php`
- `resources/views/payroll/government-tables/_results.blade.php`
- `database/seeders/GovernmentTablesSeeder.php`

**Summary:**
- New **Philhealth Minimum** tab with Minimum Amount column and Edit action only (no Add/Delete).
- Seeded single row: `minimum_amount = 250.00`.
- Payroll PhilHealth computation enforces minimum share (250 ÷ 2 per employee/employer).
