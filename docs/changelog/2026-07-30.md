## 15:12 — Employee upload type dropdown (Master File + Employee Salary)

**Why:** Users need to choose whether they are uploading full employee master data or updating salary records for existing employees.

**Files:**
- `config/employee_upload.php`
- `config/employee_salary_upload.php`
- `app/Services/EmployeeUploadService.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Services/EmployeeSalarySync.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `resources/views/employees/_upload-form.blade.php`
- `resources/views/employees/_upload-preview.blade.php`
- `resources/views/employees/index.blade.php`
- `resources/js/app.js`
- `resources/templates/employee_salary_upload_template.xlsx`
- `scripts/build-desktop.sh`

**Summary:**
- Upload modal now has **Upload Type** dropdown: Master File (existing) and Employee Salary (new).
- Salary upload updates existing employees by employee number + employment slot; separate Excel template and preview columns.
- Preserves salary history via `EmployeeSalarySync::syncForEmployment()`.

## 15:05 — Desktop build 0.1.41 (macOS + Windows)

**Why:** Ship Mac DMG with Install Pulse.command helper, payroll reports fix, and build pipeline fixes from 0.1.40.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.41`)
- `scripts/patches/electron-builder.js`
- `scripts/patches/BuildCommand.php`

**Summary:**
- macOS: `dist/Pulse-0.1.41-arm64.dmg` (includes Pulse.app, Install Pulse.command, README.txt)
- Windows: `dist/Pulse-0.1.41-setup.exe`
- Fixed unsigned Mac build (conditional hardenedRuntime; optional signing env vars only when set).
- Pruned old `Pulse-0.1.38-*` installers.

## 15:00 — Mac install helper + client guide

**Why:** Make unsigned Mac installs easier when Apple Developer notarization is not used.

**Files:**
- `scripts/install-pulse-mac.sh`
- `docs/client-mac-install-guide.md`

**Summary:**
- Install helper now finds Pulse.app next to the DMG script and shows a macOS alert with right-click Open steps.
- Documented that zero-warning installs require Apple Developer notarization — no free alternative exists.

## 14:58 — Mac notarization setup (Gatekeeper fix for other Macs)

**Why:** Unsigned desktop builds show "Apple could not verify Pulse is free of malware" on client Macs; production installs need Developer ID signing + notarization.

**Files:**
- `config/nativephp-internal.php`
- `scripts/setup-mac-notarization.sh`
- `scripts/build-desktop.sh`
- `scripts/patches/BuildCommand.php`
- `scripts/patches/electron-builder.js`
- `scripts/patches/notarize.js`
- `.env.example`
- `docs/desktop-mac-install.md`

**Summary:**
- Wired `.env` Apple credentials into NativePHP build (sign + notarize + staple).
- Added interactive setup script; build skips adhoc re-sign when stapler validates notarized app.
- Build machine still needs Developer ID Application cert in Keychain — run `./scripts/setup-mac-notarization.sh` then rebuild.
- Added free client path: `scripts/install-pulse-mac.sh` + **Install Pulse.command** bundled in DMG (unsigned builds still need one-time right-click Open).

## 14:34 — Desktop build 0.1.40 (macOS + Windows)

**Why:** Ship payroll reports fix, hybrid payroll computation, hourly-rate Time-In/Out, and performance improvements from 0.1.39.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.40`)

**Summary:**
- macOS: `dist/Pulse-0.1.40-arm64.dmg`, `dist/Pulse-0.1.40-arm64.zip`
- Windows: `dist/Pulse-0.1.40-setup.exe`
- Pruned old `Pulse-0.1.37-*` installers from `dist/`.

## 14:30 — Fix Payroll Reports options after SQL restore

**Why:** After restoring a SQL backup, Payroll Reports showed "Unable to load report options" because the batch options query selected a non-existent `id` column on `tbl_payroll_calendar`.

**Files:**
- `app/Services/Reports/ReportBatchOptionsService.php`

**Summary:**
- Corrected eager-load column lists: use `payroll_calendar_id` (not `id`) on PayrollCalendar and `payroll_batch_status` (not `status`) on PayrollBatchStatus.
- `/payroll/reports/{id}/options` no longer returns HTTP 500 when loading processed batch dropdown.

## 14:45 — Staff Time-In/Out respects Use Basic Income as Hourly Rate

**Why:** Staff Time-In/Time-Out payroll still divided Basic Income by Days Per Period (75÷12×days = 68.75) even when "Use Basic Income as Hourly Rate" was checked — faculty load path had this fix but time logs did not.

**Files:**
- `app/Services/TimeLogsPayrollService.php`

**Summary:**
- When `use_basic_income_as_hourly_rate` is true, basic = sum of punch hours × hourly rate (75), not daily rate × worked days.
- Added `punchHoursForSession()` and `totalPunchHoursForSessions()`; return `computed_hours` on time-log payroll results for batch income Hours column.

## 14:35 — Hybrid payroll: separate Faculty + Staff computation per calendar category

**Why:** When Payroll Calendar Category includes both Faculty and Staff, hybrid employees must be computed twice — faculty income from teaching loads and staff income from staff salary (Leaves fixed or Time-In/Out logs), not merged into one BASC line zeroed by the faculty path.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Services/EmployeeSalaryResolverService.php`

**Summary:**
- Added `payrollComputationSlices()`: hybrid + calendar has Faculty **and** Staff → two passes (faculty salaries, then staff salaries).
- Faculty slice uses teaching-load path only; staff slice uses Time-In/Out logs or Leaves fixed basic — faculty flag no longer blocks staff computation.
- Calendar scoped to one category (e.g. Staff only) filters to that employment's salaries only.
- Added `salariesForPeriodByUserType()` on the salary resolver.

## 14:20 — Hybrid staff Leaves basic preserved in payroll batch

**Why:** Hybrid employees (Faculty + Staff) with Staff salary on Leaves were getting ₱0 Basic Income when faculty teaching loads were missing — faculty attendance path overwrote the entire BASC line, including the fixed Staff portion.

**Files:**
- `app/Services/PayrollBatchService.php`

**Summary:**
- Added `proratedLeavesBasicFromSalaries()` to prorate BASC only from salaries with Basic Computation = Leaves.
- When `is_hybrid` and faculty/load attendance runs, Staff Leaves basic is added on top of faculty attendance basic (or paid alone when attendance basic is zero).
- Example: Mejarse (2026-SA0176) should get ₱75 non-taxable from Staff Leaves even without teaching loads; faculty portion still requires Employee Load.

## 15:22 — Auto compute Excess as OT (employee timekeeping setting)

**Why:** Per-employee control over whether excess regular hours are computed as overtime (OVRT) during payroll batch processing.

**Files:**
- `database/migrations/2026_07_30_152100_add_is_auto_compute_excess_as_ot_to_timekeeping_employee_setup.php`
- `app/Models/TimekeepingEmployeeSetup.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/employee-profile/_setup-tab-timekeeping.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `app/Services/PayrollBatchService.php`

**Summary:**
- New checkbox **Auto compute Excess as OT** below **Auto populate attendance** in Employee Profile timekeeping settings.
- Column `is_auto_compute_excess_as_ot` on `tbl_timekeeping_employee_setup` (default off).
- Payroll batch skips excess-hours OT (OVRT income) unless the employee flag is enabled; policy OT rules still apply when enabled.

## 15:30 — Exclude excess from BASC when Auto compute Excess as OT is off

**Why:** Users saw 101.17 BASC hours even with the new checkbox unchecked — the flag only gated OVRT, while Basic Income still used full punch span (IN→OUT).

**Files:**
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/timekeeping/shift-codes/_form.blade.php`
- `database/migrations/2026_07_30_153000_fix_am4_shift_duty_schedule_times.php`

**Summary:**
- When **Auto compute Excess as OT** is unchecked, BASC `computed_hours` clips to duty schedule (Time In–Time Out minus breaks) instead of raw punch hours; excess before/after schedule is not paid in BASC or OVRT.
- Restored shift code labels to **Time In / Time Out** (duty schedule for late, UT, excess).
- Fixed AM4 shift data from mistaken 14:00–15:00 break window to 10:00–19:00 duty per description.

## 15:35 — Shift code duty schedule section restored

**Why:** Form labels were renamed to Break In/Out (2026-07-21), so users entered break times into duty schedule fields and thought schedule setup was removed.

**Files:**
- `resources/views/timekeeping/shift-codes/_form.blade.php`
- `resources/views/timekeeping/shift-codes/_results.blade.php`
- `app/Support/ShiftCode.php`
- `resources/js/app.js`
- `tests/Unit/ShiftCodeValidationTest.php`

**Summary:**
- Added clear **Duty Schedule** section with **Time In / Time Out** (required for fixed shifts).
- **Breaks** remain in the table below (Break Minute + Paid Break?) — same as legacy PATHS.
- List columns renamed from Break In/Out to Time In/Out.
- `npm run build` run for JS toggle updates.

## 15:40 — Shift code Break Out/In window fields

**Why:** Users expect both duty schedule (Time In/Out) and break window (Break Out/In) like legacy PATHS — not break times stored in duty schedule fields.

**Files:**
- `database/migrations/2026_07_30_154000_add_break_window_times_to_shift_code_breaks.php`
- `app/Models/ShiftCodeBreak.php`
- `app/Support/ShiftCode.php`
- `resources/views/timekeeping/shift-codes/_form.blade.php`
- `resources/js/app.js`
- `app/Services/PayrollBreakService.php`
- `tests/Unit/ShiftCodeValidationTest.php`

**Summary:**
- **Duty Schedule:** Time In / Time Out (shift start/end).
- **Breaks table:** Break Out, Break In, Break Minute (optional if window set), Paid Break.
- Break minutes auto-derived from Break Out → Break In when minutes blank.

## 15:37 — One-time sync shift Time In/Out from Description

**Why:** Shift codes had break-window times in duty schedule fields; descriptions already state the correct duty hours (e.g. AM = 7:00 am to 4:00 pm).

**Files:**
- `app/Support/ShiftCodeDescriptionParser.php`
- `database/migrations/2026_07_30_154500_sync_shift_duty_times_from_descriptions.php`
- `tests/Unit/ShiftCodeDescriptionParserTest.php`

**Summary:**
- One-time migration parses duty hours from `description` and updates `time_in` / `time_out` (24-hour).
- Previous misplaced duty values moved to first break row as Break Out / Break In where they differed from parsed schedule.
- AM → 07:00–16:00; AM2 → 08:00–17:00; … PM3 → 14:00–23:00; FLXI unchanged.

## 15:49 — Payroll upload hours column (Incomes + Deductions)

**Why:** Users need to upload hours beside income/deduction type; required for BASC/OVRT and Late/Undertime, optional but saved for other types.

**Files:**
- `config/payroll_transaction.php`
- `database/migrations/2026_07_30_160000_add_hours_to_raw_payroll_incomes.php`
- `app/Models/RawPayrollIncome.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`

**Summary:**
- Incomes template: new `hours` column after `income_type`; required for BASC and OVRT; optional for others but persisted when provided.
- Deductions: existing LTDE/UTDE hours-required rule unchanged; optional hours on other types still saved.
- Uploaded income hours flow to `raw_payroll_incomes` and merge into payroll batch income lines.

## 15:52 — Employee Profile bulk upload

**Why:** Users need to bulk-update holiday group, shift code, policy, rest days, and flags instead of one employee at a time.

**Files:**
- `config/employee_profile_upload.php`
- `app/Services/TimekeepingEmployeeProfileUploadService.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/index.blade.php`
- `resources/views/timekeeping/employee-profile/_upload-form.blade.php`
- `resources/views/timekeeping/employee-profile/_upload-preview.blade.php`

**Summary:**
- Upload button on Employee Profile list (requires `employee-profile.update`).
- CSV template pre-filled with all employees and current setup values.
- Validates holiday group code, policy name, shift code, rest days (1/0), and three boolean flags.
- Preview → commit updates `tbl_timekeeping_employee_setup` and rest days per row.

## 15:55 — Payroll batch Days column (Incomes + Deductions)

**Why:** Employee Payroll Detail should show days worked beside hours on income and deduction lines.

**Files:**
- `database/migrations/2026_07_30_160500_add_days_to_payroll_income_and_deduction_tables.php`
- `app/Models/PayrollIncome.php`, `PayrollDeduction.php`, `RawPayrollIncome.php`, `RawPayrollDeduction.php`
- `config/payroll_transaction.php`
- `app/Services/PayrollBatchService.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/EmployeeLoadPayrollService.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`

**Summary:**
- Added `days` column on payroll income/deduction lines and raw upload staging tables.
- BASC auto-fills days from `worked_days`; LTDE/UTDE from late/undertime day counts.
- Upload template accepts optional `days`; merges to batch on load.
- Employee Payroll Detail modal shows Days column next to Hours on Incomes and Deductions tabs.

## 15:58 — Net Pay tab days display

**Why:** Net Pay summary should show days beside hours on each income and deduction line.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`

**Summary:**
- Income and deduction lines on Net Pay tab now show `{hours} hrs` and `{days} days` when present, before the amount.

## 15:59 — SSS report Gross Income column

**Why:** SSS Monthly Contribution report needs Gross Income before Grand Total.

**Files:**
- `app/Services/Reports/SssContributionReportService.php`
- `resources/views/payroll/reports/_preview.blade.php`
- `resources/views/payroll/reports/_print-document.blade.php`

**Summary:**
- Added **Gross Income** column (taxable + non-taxable payroll income for the month) before Grand Total.
- Updated preview, print, and Excel export headers/rows.

## 16:01 — Upload required Days for BASC/OVRT and LTDE/UTDE

**Why:** Days column on income/deduction uploads should be required for the same types as Hours.

**Files:**
- `config/payroll_transaction.php`
- `app/Services/PayrollTransactionUploadService.php`

**Summary:**
- Incomes: **Days** required for BASC and OVRT (> 0); optional for other types but saved when provided.
- Deductions: **Days** required for LTDE and UTDE (> 0); optional for other types but saved when provided.
- Template hints updated; hours and days validation errors both reported per row.

## 16:03 — Manual Add Income Hours and Days

**Why:** Manual income entry should capture hours and days for Basic Pay and Overtime Pay, matching upload rules.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-add-income-form.blade.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Services/PayrollBatchService.php`
- `resources/js/app.js`

**Summary:**
- Add Income modal now shows **Hours** and **Days** fields when income type is BASC or OVRT.
- Both fields are required (> 0) for Basic Pay and Overtime Pay; optional but saved for other types.
- Backend validation and persist in `addIncomeToDetail()`; JS toggles visibility/required on type change.

## 16:06 — Manual Add Deduction reference date, auto days, batch totals

**Why:** Late/undertime manual deductions need a reference date per entry, auto 1 day each, and summed totals on the batch detail.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-add-deduction-form.blade.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/js/app.js`

**Summary:**
- **Reference Date** required for LTDE and UTDE; each entry auto-saves **1 day**.
- Multiple late/undertime lines can be added (one per date); each saved as its own row.
- Deductions tab **Total** row now sums hours and days (grouped by type, amounts/hours/days aggregated).

## 16:15 — Reports PDF output option

**Why:** Users need a PDF download option alongside on-screen preview and Excel for payroll reports.

**Files:**
- `composer.json`, `composer.lock`
- `app/Models/ReportFileType.php`
- `app/Support/ReportPdfDownload.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `config/payroll_reports.php`
- `database/seeders/ReportSeeder.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `resources/views/payroll/reports/pdf-document.blade.php`
- `resources/views/payroll/reports/_print-document.blade.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `resources/views/payroll/reports/options/sss.blade.php`
- `tests/Unit/ReportPdfDownloadTest.php`

**Summary:**
- Added **PDF** to Output dropdown for Payroll Register and SSS Monthly Contribution.
- Uses `dompdf/dompdf` to render report tables and stream a `.pdf` download.
- Wide reports auto-use landscape orientation; SSS layout always landscape.

## 16:19 — Desktop build v0.1.42

**Why:** Release desktop installers with today's payroll/report changes (manual income/deduction hours/days, PDF reports, SSS gross income, etc.).

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.42`)

**Summary:**
- Built macOS + Windows installers at **0.1.42**.
- `pulse/dist/Pulse-0.1.42-arm64.dmg` (342 MB)
- `pulse/dist/Pulse-0.1.42-arm64.zip` (288 MB)
- `pulse/dist/Pulse-0.1.42-setup.exe` (222 MB)
- Pruned `Pulse-0.1.39-*` from dist (keeps 3 latest versions).

## 16:23 — Employee History details tab

**Why:** Edit Employee needs a read-only audit tab showing what changed and previous data snapshots.

**Files:**
- `resources/views/employees/_tabs-nav.blade.php`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/partials/_history-results.blade.php`
- `app/Http/Controllers/EmployeeController.php`
- `app/Services/EmployeeHistoryService.php`
- `app/Models/Employee.php`
- `config/employee_history.php`
- `routes/web.php`
- `tests/Unit/EmployeeHistoryServiceTest.php`

**Summary:**
- New **History details** tab on Edit Employee (lazy-loaded from `sys_logs`).
- Lists create/update/delete entries with user, timestamp, and field-level **Previous** vs **New** values.
- Expanded `Employee::logSnapshot()` to capture personal, contact, address, extended profile, assignments, employment, and salary data on save.

## 16:26 — HR Historical Data report

**Why:** Reports module needs a Human Resource classification with Historical Data export from employee change logs.

**Files:**
- `database/seeders/ReportSeeder.php`
- `config/payroll_reports.php`
- `app/Services/Reports/HistoricalDataReportService.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `resources/views/payroll/reports/options/historical-data.blade.php`
- `resources/views/payroll/reports/_preview.blade.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `resources/js/app.js`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Activated **Human Resource** report classification with **Historical Data** report type.
- Filters: employee(s), actions (create/update/delete), date range; output Preview / Excel / PDF.
- Report rows show date, user, action, employee, field, previous value, new value, and remarks from `sys_logs`.

## 16:30 — Payroll Register ICCT per-hour Excel layout

**Why:** Payroll Register Excel export should match the ICCT sample worksheet layout (`3.16-03.31.26 (PER HOUR)`).

**Files:**
- `config/payroll_register_layout.php`
- `config/payroll_register_layout_columns.json`
- `app/Services/Reports/PayrollRegisterExcelExporter.php`
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `.env.example`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Excel output now uses ICCT-style title rows, 83-column header (A–CE), yellow highlight columns, and data starting row 8.
- Maps Pulse payroll data to key columns: BASC hours/pay, holiday, tardiness, overtime, gross, WHT, Pag-IBIG, SSS, PhilHealth, total deductions, net pay.
- Company name/subtitle configurable via `PAYROLL_REGISTER_COMPANY_NAME` and `PAYROLL_REGISTER_SUBTITLE`.
- On-screen preview stays as simplified table; **Excel** uses the full ICCT layout.

## 16:31 — Fix Payroll Register Collection type error

**Why:** Report generation failed because `groupBy()` returns a Collection, not an array.

**Files:**
- `app/Services/Reports/PayrollRegisterRowBuilder.php`

**Summary:**
- Updated `sumIncomeCodes()` / `sumDeductionCodes()` to accept `Collection` from Eloquent `groupBy()`.

## 16:38 — Map Job Order columns to Overtime (OVRT)

**Why:** ICCT payroll register uses Job Order columns for overtime hours and amount, not a separate job-order income type.

**Files:**
- `config/payroll_register_layout_columns.json`

**Summary:**
- **AF (Job Order)** → `overtime_hours` from OVRT income lines.
- **AG (Total Job Order)** and **AL (Total Job Order Amnt)** → `overtime_amount`.
- Data source unchanged in `PayrollRegisterRowBuilder`; columns AQ/AR (Overtime) still map the same OVRT totals.

## 16:42 — Unify Payroll Register preview, Excel, and PDF layout

**Why:** On-screen preview still used the old flat income/deduction columns while Excel used the ICCT layout.

**Files:**
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `resources/views/payroll/reports/_icct-payroll-register-table.blade.php`
- `resources/views/payroll/reports/_preview.blade.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `app/Support/ReportPdfDownload.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Preview, Excel, and PDF now share the same ICCT column headers and mapped values from `register_rows`.
- Added `buildLayoutTable()` and `sortRegisterRows()`; removed legacy flat-table builder from report service.
- PDF uses landscape for the wide ICCT register layout.

## 16:46 — Desktop build v0.1.43

**Why:** Release installers with unified Payroll Register preview/Excel/PDF layout and Job Order → Overtime mapping.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.43`)

**Summary:**
- Built macOS **Pulse-0.1.43-arm64.dmg** (350 MB) + ZIP, and Windows **Pulse-0.1.43-setup.exe** (222 MB).
- Output: `pulse/dist/`; pruned v0.1.40 installers (keeps 3 latest versions).
