# 2026-08-19

## 11:09 — Pulse + Collector weekly Progress & Targets Word report

**Why:** Produce the last-week progress and this-week targets pack (including web S3 documents → desktop viewing) as a Word file.

**Files:**
- `docs/Pulse-and-Collector-Progress-and-Target-Report-2026-08-11-to-08-22.md`
- `docs/Pulse-and-Collector-Progress-and-Target-Report-2026-08-11-to-08-22.docx`
- `docs/generate-progress-report-docx.php`

**Summary:**
- Progress Aug 11–18 for Pulse (through 0.1.58) and Biometric Collector (through 0.1.9)
- Targets Aug 19–22 include San Mateo collector UAT, S3 pull logs, and pulling web-uploaded S3 documents for Pulse desktop viewing

## 15:48 — Rebuild Word report so Word can open it

**Why:** Word (including iOS) failed to open the first `.docx` from PhpWord.

**Files:**
- `docs/generate-progress-report-docx.py`
- `docs/Pulse-and-Collector-Progress-and-Target-Report-2026-08-11-to-08-22.docx`

**Summary:**
- Regenerated a valid OOXML `.docx` with python-docx (zip-tested)
- Same report content; ASCII-safe punctuation so Word does not choke on the file

## 16:42 — Employee Management Sync from ISKOLARIS

**Why:** Let HR pull employee profiles from ISKOLARIS into Pulse, with a count, review of changes, and per-row or bulk apply.

**Files:**
- `app/Services/EmployeeSkolarisSyncService.php`
- `app/Http/Controllers/EmployeeSkolarisSyncController.php`
- `app/Services/SkolarisApiService.php`
- `app/Policies/EmployeePolicy.php`
- `resources/views/employees/index.blade.php`
- `resources/views/employees/_sync-form.blade.php`
- `resources/views/employees/_sync-view.blade.php`
- `resources/views/partials/page-header.blade.php`
- `resources/js/app.js`
- `routes/web.php`
- `tests/Feature/EmployeeSkolarisSyncTest.php`

**Summary:**
- Added a Sync button (with pending count) next to Upload on Employee Management
- Uses Local Employee Updates (`GET /local-employee-updates`, then `POST .../mark-applied`)
- Matches Pulse `employee_id` from the desktop app — no extra mapping column
- View shows Pulse vs queued ISKOLARIS fields; Sync per row or Sync selected
- Does not overwrite salary, credentials, or loans
- Pulse API key is stored in local `.env` as `SKOLARIS_PULSE_API_KEY` (not committed)

## 17:02 — Sync uses Pulse employee_id + Local Employee Updates API

**Why:** Pending ISKOLARIS changes are queued per Pulse employee ID; do not add a separate Skolaris ID column.

**Files:**
- `app/Services/EmployeeSkolarisSyncService.php`
- `app/Services/SkolarisApiService.php`
- `tests/Feature/EmployeeSkolarisSyncTest.php`
- `resources/js/app.js`
- `resources/views/employees/_sync-form.blade.php`

**Summary:**
- Lookup is `tbl_employees.employee_id` = API `employee_id`
- Removed the extra `skolaris_employee_id` column/migration

## 17:08 — Sync count badge as red notification corner

**Why:** Match a notification-style badge (red circle, top-right overlap) on the Sync button.

**Files:**
- `resources/views/employees/index.blade.php`
- `resources/views/partials/page-header.blade.php`
- `resources/js/app.js`

**Summary:**
- Sync count is a red badge on the top-right corner of the button
- Hidden when the pending count is 0

## 17:10 — Always show Sync row checkboxes

**Why:** Select all / Sync selected need a checkbox on every pending row, including Not in Pulse.

**Files:**
- `resources/js/app.js`

**Summary:**
- Per-row checkbox and Sync button are always shown

## 17:16 — Approve label + employee full name on ISKOLARIS pending list

**Why:** Name column should show the employee full name, and user-facing Sync actions should read Approve.

**Files:**
- `app/Services/EmployeeSkolarisSyncService.php`
- `app/Http/Controllers/EmployeeSkolarisSyncController.php`
- `resources/views/employees/index.blade.php`
- `resources/views/employees/_sync-form.blade.php`
- `resources/js/app.js`
- `tests/Feature/EmployeeSkolarisSyncTest.php`

**Summary:**
- Name uses Pulse `full_name`, then ISKOLARIS `employee_name`/`full_name`, then queued first/middle/last name
- Also match Pulse by `employee_number` when `employee_id` is missing
- Header, modal, row, and bulk buttons now say Approve / Approve selected

## 17:22 — Resolve ISKOLARIS full name for pending employee IDs

**Why:** Local employee updates only send `employee_id` (e.g. 287, 344), so the Name column still showed "Employee ID 344".

**Files:**
- `app/Services/SkolarisApiService.php`
- `app/Services/EmployeeSkolarisSyncService.php`
- `tests/Feature/EmployeeSkolarisSyncTest.php`

**Summary:**
- For unmatched IDs, load `full_name` from timekeeping attendance (`data.employee`)
- Also rematch Pulse using that card's `employee_number` (344 → 8670)
