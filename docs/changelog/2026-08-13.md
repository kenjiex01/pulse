# 2026-08-13

## 11:36 — Pull biometric logs from S3 on Time Logs

**Why:** Import gzipped attendance JSON from `biometric_logs/` on S3 and attach punches to employees by campus biometric ID.

**Files:**
- `app/Services/BiometricLogsS3PullService.php`
- `config/biometric_logs.php`
- `app/Http/Controllers/TimeLogsController.php`
- `app/Support/TimeLogs.php`
- `routes/web.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_s3-pull-form.blade.php`
- `resources/views/partials/modal-form-actions.blade.php`
- `resources/js/app.js`
- `.env.example`
- `tests/Unit/BiometricLogsS3PullServiceTest.php`

**Summary:**
- Time Logs header: **Pull logs** button beside **Upload** (Time In/Out and Timelogs DTR tabs)
- Modal pulls `biometric_logs/{YYYY}/{MM}/{collector}/*.json.gzip` via `backup-s3` / `DB_BACKUP_S3_*`
- Parses attendance JSON; maps `user_id` → campus `biometric_id`, `CheckIn`/`CheckOut` → in/out punches
- Optional campus + collector folder filters; already-imported S3 keys are skipped
- Creates `raw_timekeeping_transactions` / `raw_timekeeping_inandout` batches for payroll

## 11:54 — Match paired page-header action button styles

**Why:** Pull logs and Upload looked different (outline vs solid); header action pairs should share one design.

**Files:**
- `resources/views/partials/page-header.blade.php`

**Summary:**
- Secondary header action (e.g. Pull logs, Upload) now uses `btn-primary` like the main action

## 11:56 — Match Add Shift Code / Overtime / Income button styles

**Why:** Incomes tab action buttons mixed outline and solid styles.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`

**Summary:**
- Add Shift Code, Add Overtime, and Add Income all use `btn-primary`

## 11:57 — Match payroll batch toolbar button styles

**Why:** Batch detail toolbar mixed outline and solid buttons (Re-process, Remove Selected, Search vs Post / Add Employees).

**Files:**
- `resources/views/payroll/transaction/_show-content.blade.php`

**Summary:**
- Re-process, Post, Add Employees, Remove Selected, and Search all use `btn-primary`

## 11:58 — Remove Selected uses danger (red) style

**Why:** Destructive remove action should read as red, not primary blue.

**Files:**
- `resources/views/payroll/transaction/_show-content.blade.php`

**Summary:**
- Batch **Remove Selected** button uses `btn-danger`

## 11:59 — Fill btn-danger solid red

**Why:** Remove Selected looked outline-only; destructive actions should match primary’s solid fill style in red.

**Files:**
- `resources/css/app.css`

**Summary:**
- `.btn-danger` is now solid red fill + white text (same weight/shadow pattern as `.btn-primary`)
- Disabled state uses opacity so Remove Selected still reads clearly when none selected

## 12:08 — Employee Profile Loans tab

**Why:** Port PATHS HR employee loans into Pulse profile tabs after Credentials.

**Files:**
- `database/migrations/2026_08_13_120535_create_employee_loans_tables.php`
- `app/Models/PaymentScheme.php`
- `app/Models/EmployeeLoan.php`
- `app/Models/Employee.php`
- `app/Http/Controllers/EmployeeLoanController.php`
- `app/Http/Controllers/EmployeeController.php`
- `database/seeders/PayrollMaintenanceSeeder.php`
- `routes/web.php`
- `resources/views/employees/_tabs-nav.blade.php`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/partials/_loans-tab.blade.php`
- `resources/views/employees/partials/_loan-form.blade.php`
- `resources/views/employees/partials/_loans-modals.blade.php`
- `resources/js/app.js`

**Summary:**
- Added **Loans** tab after Credentials (before History details)
- `tbl_employee_loans` + `lu_payment_schemes` (User Entered Amortization / Base on # of Payments)
- Modal CRUD: list, add, edit, soft-delete with balance / amortization rules from PATHS
- Loan types reuse existing `tbl_loan_types`; payment schemes seeded via PayrollMaintenanceSeeder
- sys_logs on create/update/delete

## 13:34 — Employee Profile Calendar View tab

**Why:** Show first Time In / last Time Out per day in a month calendar; click a day to list every raw punch.

**Files:**
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Services/EmployeeAttendanceLogService.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-calendar-view.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/views/timekeeping/employee-profile/_attendance-log-edit-form.blade.php`
- `resources/views/timekeeping/employee-profile/_attendance-log-edit-modal.blade.php`
- `resources/css/app.css`
- `resources/js/app.js`
- `tests/Unit/EmployeeAttendanceLogServiceTest.php`

**Summary:**
- New **Calendar View** tab beside Attendance View / Employee Load
- Per-day cell: first IN + last OUT from raw `raw_timekeeping_inandout`
- Day click opens detail table of all logs for that date (edit/original modals stacked)
- Month prev/next via lazy panel reload

## 13:52 — Allow blank Grace Period on Timekeeping Policy

**Why:** Policy Tardiness settings should allow no grace period (empty input), not force a positive value.

**Files:**
- `app/Support/TimekeepingPolicy.php`
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`
- `resources/views/timekeeping/policy/_settings-breaks.blade.php`
- `resources/views/timekeeping/policy/_equivalent-section.blade.php`

**Summary:**
- Grace Period (and break grace) can be left blank → stored as `null` (no grace)
- Empty / `0` normalized to null; deduct-grace flag clears when there is no grace
- Helper text: “Leave blank if none”

## 15:42 — Master File upload: disable required + upsert

**Why:** Allow partial Master File imports and update existing employees matched by Employee Number + Email.

**Files:**
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Services/EmployeeUploadService.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `resources/views/employees/_upload-form.blade.php`
- `resources/views/employees/_upload-preview.blade.php`
- `tests/Unit/EmployeeUploadRowMapperTest.php`

**Summary:**
- New **Disable required fields** checkbox on Master File upload (Employee Number + Email still required)
- Match key: Employee Number + Email → both match = **Update**; else **Create**
- Duplicate email / number-with-different-email conflicts are rejected
- Partial rows only validate/save filled columns; employment/campus/salary sync only when that section has data
- Preview shows Create vs Update action column
- Unit tests cover partial create, upsert match, and conflict cases

## 15:47 — Desktop build v0.1.55 (macOS + Windows)

**Why:** Ship current Pulse features (calendar view, grace blank, master-file upsert) as paired installers.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.55`)
- `dist/Pulse-0.1.55-arm64.dmg`
- `dist/Pulse-0.1.55-setup.exe`

**Summary:**
- Bumped version 0.1.54 → **0.1.55**
- Built both platforms via `./scripts/build-desktop.sh all`
- Uploaded to S3 `payroll_installer/`; `latest.json` points to 0.1.55

## 15:53 — Document Types HR maintenance CRUD

**Why:** Add a simple Document Types lookup under Human Resource for future employee document attachment.

**Files:**
- `database/migrations/2026_08_13_154900_create_tbl_document_types_table.php`
- `app/Models/DocumentType.php`
- `config/hr_lookups.php`
- `database/seeders/DocumentTypeSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `database/seeders/DesktopReferenceDataSeeder.php`
- `resources/views/partials/sidebar-icon.blade.php`

**Summary:**
- New `tbl_document_types` (code, name, description, sort, active) with soft deletes
- Registered as HR lookup `document-types` → `/hr/document-types` (shared modal CRUD)
- Sidebar icon + submodule/role seed; starter types (Resume, Contract, ID, TOR, Certificate, NBI)

## 15:54 — Document Types: Required checkbox

**Why:** Mark which document types are mandatory.

**Files:**
- `database/migrations/2026_08_13_155400_add_is_required_to_tbl_document_types_table.php`
- `app/Models/DocumentType.php`
- `config/hr_lookups.php`
- `resources/views/hr-lookups/_form.blade.php`

**Summary:**
- Added `is_required` boolean (default false) on Document Types
- Shown in list + create/edit form checkbox (unchecked by default on create)

## 15:58 — Seed Document Types checklist (all required)

**Why:** Populate Document Types with the HR onboarding document checklist; all marked required.

**Files:**
- `database/seeders/DocumentTypeSeeder.php`

**Summary:**
- Seeded 12 document types (1x1 ID, gov IDs, address/proof of billing, cellphone, birth/marriage/child certs, diploma/TOR, PRC rating, COE, training, TESDA)
- All rows `is_required = true`
- Soft-deleted previous starter types (Resume, Contract, etc.)

## 16:02 — Credentials: Document Type dropdown

**Why:** Link employee credential uploads to Document Types maintenance.

**Files:**
- `database/migrations/2026_08_13_160000_add_document_type_id_to_tbl_employee_credentials_table.php`
- `app/Models/EmployeeCredential.php`
- `app/Models/Employee.php`
- `app/Http/Controllers/EmployeeCredentialController.php`
- `resources/views/employees/partials/_credential-add-form.blade.php`
- `resources/views/employees/partials/_credentials-modals.blade.php`
- `resources/views/employees/partials/_credentials-tab.blade.php`

**Summary:**
- Add Credential modal: required **Document Type** select (active types; shows “(Required)” when flagged)
- Description is optional notes (defaults to type name if blank)
- Credentials list shows Document Type column + Required badge

## 16:03 — Split government ID document types

**Why:** TIN, SSS, PhilHealth, Pag-IBIG, and PRC should be separate required document types, not one combined option.

**Files:**
- `database/seeders/DocumentTypeSeeder.php`

**Summary:**
- Replaced combined `GOV_IDS` with: TIN ID, SSS ID, PhilHealth ID, Pag-IBIG ID, PRC ID (all required)
- Soft-deleted the old combined government IDs type

## 16:16 — Desktop build v0.1.56 (macOS + Windows)

**Why:** Ship Document Types maintenance, credential document-type select, and split gov IDs.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.56`)
- `dist/Pulse-0.1.56-arm64.dmg`
- `dist/Pulse-0.1.56-setup.exe`

**Summary:**
- Bumped version 0.1.55 → **0.1.56**
- Built both platforms via `./scripts/build-desktop.sh all`
- Uploaded to S3 `payroll_installer/`; `latest.json` points to 0.1.56
