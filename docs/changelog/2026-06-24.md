## 11:00 — Rename Upload Transactions tab label

**Why:** User requested the Payroll Transaction tab label read "Upload Adjustments" instead of "Upload Transactions".

**Files:**
- `app/Support/PayrollTransactionModule.php`
- `resources/views/payroll/transaction/index.blade.php`
- `app/Http/Controllers/PayrollTransactionController.php`

**Summary:**
- Tab label changed to **Upload Adjustments** (route key `upload-transactions` unchanged).
- Page description and audit log wording updated to match.

## 11:10 — Remove upload adjustment tabs

**Why:** User requested hiding Income Adjustments, Deduction Adjustments, and Resigned Employees from Upload Adjustments.

**Files:**
- `config/payroll_transaction.php`

**Summary:**
- Removed `income-adjustments`, `deduction-adjustments`, and `resigned-employees` from `upload_types` config.
- Upload Adjustments tab nav now shows only: Incomes, Deductions, Hours Worked, Leaves, Loans.

## 11:17 — Fix upload template employee pre-fill

**Why:** Download Template was not passing the selected pay period, so `emp_num` rows were empty even when the payroll batch had employees.

**Files:**
- `resources/js/app.js`

**Summary:**
- Template download link now updates after pay period is synced (not before pay type/year cascade).
- Click handler refreshes link with current `payroll_calendar_id` before download.
- Modal open re-syncs template link after batch form fields are populated.
- Backend already pre-fills Employee No. from payroll batch details for the selected period.

## 14:11 — Remove amount from Hours Worked upload template

**Why:** Hours Worked uploads should only include hours; amount is computed from employee hourly rate when applied to the payroll batch.

**Files:**
- `config/payroll_transaction.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`

**Summary:**
- Removed `amount` column from Hours Worked CSV template and upload detail view.
- Template columns: Employee No., Day Type, Time Type, No. of Hours only.
- Updated upload description to note amount is computed from hourly rate on batch load.

## 15:06 — Remove Templates tab from Timekeeping Policy

**Why:** User requested hiding the Templates tab from Timekeeping Policy navigation.

**Files:**
- `config/timekeeping_policy.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `resources/views/timekeeping/policy/index.blade.php`

**Summary:**
- Removed `templates` from `module_tabs` config.
- Tab nav now shows: Policy, Shift Code, Time Capturing Settings, Holiday Settings.
- Legacy `/timekeeping/policy/templates` now redirects to `Policy` instead of showing a 404.
