## 15:30 — Government ID length validation on employee profile

**Why:** Catch wrong-length TIN, SSS, PhilHealth, and Pag-IBIG numbers on create/edit before they reach payroll.

**Files:**
- `app/Support/GovernmentIdNumbers.php`
- `app/Rules/GovernmentIdNumber.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `tests/Unit/GovernmentIdNumbersTest.php`
- `tests/Feature/EmployeeGovernmentIdValidationTest.php`

**Summary:**
- Validates digit length when a value is provided: SSS 10, PhilHealth 12, Pag-IBIG 12, TIN 9 or 12 (dashes/spaces allowed).
- Applies on employee wizard create, edit/update, and bulk upload preview.

## 15:35 — Government IDs: digits only (no dashes)

**Why:** Statutory ID fields must be entered as plain numbers without dash separators.

**Files:**
- `app/Support/GovernmentIdNumbers.php`
- `resources/views/employees/_form.blade.php`
- `tests/Unit/GovernmentIdNumbersTest.php`
- `tests/Feature/EmployeeGovernmentIdValidationTest.php`

**Summary:**
- Rejects values containing dashes or non-numeric characters; digits-only with required lengths unchanged.
- Form inputs use numeric input mode for TIN, SSS, PhilHealth, and Pag-IBIG.

## 15:40 — Government ID display dashes (UI only)

**Why:** Show standard dashed formats in forms/view while storing plain digits in the database.

**Files:**
- `app/Support/GovernmentIdNumbers.php`
- `resources/js/government-id-format.js`
- `resources/js/app.js`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `tests/Unit/GovernmentIdNumbersTest.php`
- `tests/Feature/EmployeeGovernmentIdValidationTest.php`

**Summary:**
- UI auto-formats TIN/SSS/PhilHealth/Pag-IBIG with hardcoded dash positions as the user types.
- Backend normalizes to digits-only on save, validation, and upload; view page formats on display.

## 16:00 — Required employee names with No middle name toggle

**Why:** First, middle, and last names must be captured on create/edit; employees without a middle name should explicitly opt out.

**Files:**
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `resources/views/employees/_form.blade.php`
- `resources/js/app.js`
- `tests/Feature/EmployeeNameValidationTest.php`
- `tests/Feature/EmployeeGovernmentIdValidationTest.php`
- `tests/Feature/EmployeeWizardCampusAssignmentTest.php`

**Summary:**
- First name and last name remain required; middle name is required unless **No middle name** is checked.
- Toggle disables and clears the middle name field; backend saves null when selected.
- Edit form auto-checks the toggle when the saved middle name is empty.

## 16:15 — Fix No middle name still triggering browser required

**Why:** Middle name kept HTML `required` after checking No middle name, blocking submit in wizard.

**Files:**
- `resources/views/employees/_form.blade.php`
- `resources/js/app.js`

**Summary:**
- Removed HTML `required` on middle name; backend validation handles required logic.
- Toggle now hides/shows the asterisk in JS and syncs field state on submit.
