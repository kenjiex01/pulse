## 10:13 — Timelogs DTR tab with campus upload

**Why:** User requested a new Time Logs tab for DTR uploads with campus selection (3 ICCT campuses that provide DTR templates).

**Files:**
- `database/migrations/2026_07_11_101300_add_campus_id_to_raw_timekeeping_transactions.php`
- `app/Models/RawTimekeepingTransaction.php`
- `config/time_logs.php`
- `app/Support/TimeLogs.php`
- `app/Services/TimeLogsUploadService.php`
- `app/Http/Controllers/TimeLogsController.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `resources/views/timekeeping/time-logs/_upload-preview.blade.php`
- `resources/views/timekeeping/time-logs/_show-content.blade.php`

**Summary:**
- Added **Timelogs DTR** tab with same batch table layout as Time In / Time Out.
- Upload form shows **Campus** dropdown (San Mateo, Sumulong, Cainta Main) required for DTR tab only.
- Batches store `campus_id` and `timekeeping_transaction_type_id = 2`; upload flow stays tab-aware through preview/commit.

## 10:20 — DTR hardcoded campus parser (no Format Type)

**Why:** Timelogs DTR upload should not use Format Type dropdown; parsing is hardcoded per campus layout.

**Files:**
- `config/time_logs_dtr.php`
- `app/Support/TimeLogsDtr.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `app/Services/TimeLogsUploadService.php`
- `app/Http/Controllers/TimeLogsController.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `resources/views/timekeeping/time-logs/_upload-preview.blade.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_show-content.blade.php`
- `docs/samples/timelogs-dtr-sample.txt`

**Summary:**
- Removed Format Type from DTR upload modal; Campus + file only.
- Added per-campus hardcoded DTR layouts in `config/time_logs_dtr.php` (SA, SU, CA — `employee_number`, `actual_date`, `time_in`, `time_out`).
- New `TimeLogsDtrUploadService` handles parse, staging, and commit for DTR batches (`timecapture_format_id` null).

## 10:21 — DTR campus Excel file types (.xls / .xlsx)

**Why:** User specified Cainta and San Mateo DTR files are `.xls`; Sumulong is `.xlsx` (not tab-delimited text).

**Files:**
- `composer.json`
- `composer.lock`
- `config/time_logs_dtr.php`
- `app/Support/TimeLogsDtr.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `phpoffice/phpspreadsheet` for Excel parsing.
- Per-campus `file_extension`: SA/CA = `xls`, SU = `xlsx`; server rejects wrong extension.
- Upload UI updates accept filter and hint when campus is selected.

## 10:28 — Payroll Calendar Philhealth Minimum schedule + computation

**Why:** User requested Philhealth Minimum checkbox in Loan & Deduction Schedule, mutual exclusivity with Philhealth Premium, and premium computation that offsets prior minimum deductions in the same payroll month/year.

**Files:**
- `database/seeders/PayrollMaintenanceSeeder.php`
- `config/payroll_maintenance.php`
- `app/Support/PhilhealthDeductionTypes.php`
- `app/Services/GovernmentDeductionPayrollService.php`
- `app/Services/PayrollCalendarScheduleService.php`
- `app/Http/Controllers/PayrollCalendarController.php`
- `resources/views/payroll/calendar/_schedule-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Seeded **PHIM** (Philhealth Minimum) deduction type; default schedule attaches PHIL only (PHIM excluded from autofill).
- Schedule modal: PHIL/PHIM checkboxes are mutually exclusive (client JS + server validation).
- Payroll compute: PHIM uses flat minimum share; PHIL premium = bracket share minus prior PHIM deductions from earlier pay periods in same calendar month/year.

## 10:36 — Philhealth Minimum employee/employer amounts

**Why:** User requested Government Tables Philhealth Minimum tab to show separate Employee Amount and Employer Amount instead of a single Minimum Amount field.

**Files:**
- `database/migrations/2026_07_11_103600_split_philhealth_minimum_amounts.php`
- `app/Models/GovtTablePhilhealthMinimum.php`
- `config/government_tables.php`
- `database/seeders/GovernmentTablesSeeder.php`
- `app/Services/GovernmentDeductionPayrollService.php`

**Summary:**
- Replaced `minimum_amount` with `employee_amount` and `employer_amount` columns; migrated existing 250.00 row to 125.00 / 125.00.
- Government Tables UI now lists and edits both share columns.
- Payroll batch PHIM computation reads employee/employer amounts directly from the table (no more divide-by-two).

## 10:39 — Philhealth Minimum seeder defaults 250/250

**Why:** User corrected seeded defaults to ₱250 employee and ₱250 employer shares.

**Files:**
- `database/seeders/GovernmentTablesSeeder.php`

**Summary:**
- Updated `seedPhilhealthMinimum()` to `employee_amount = 250.00`, `employer_amount = 250.00`.
- Re-ran `GovernmentTablesSeeder` to sync the live row.

## 10:52 — PHIM shows as Philhealth Premium in payroll batch

**Why:** User requested Philhealth Minimum (PHIM) deductions to display as "Philhealth Premium" in payroll batch employee detail.

**Files:**
- `app/Support/PhilhealthDeductionTypes.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`

**Summary:**
- Added `payrollBatchLabel()` — PHIM description overridden to "Philhealth Premium" in batch Deductions and Net Pay tabs.
- Deduction type code stays `PHIM`; only the display label changes in payroll batch context.

## 11:12 — Multi-campus assignments + San Mateo Card Report DTR parser

**Why:** User requested multiple campus assignments per employee with per-campus biometric ID, and San Mateo DTR uploads that parse only Card Report worksheets (skip summary/schedule tabs).

**Files:**
- `database/migrations/2026_07_11_110800_create_employee_campus_assignments_table.php`
- `app/Models/EmployeeCampusAssignment.php`
- `app/Models/Employee.php`
- `app/Services/EmployeeCampusAssignmentSync.php`
- `app/Services/EmployeeBiometricResolver.php`
- `app/Services/TimeLogsDtr/SanMateoCardReportParser.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `app/Services/TimeLogsUploadService.php`
- `app/Http/Controllers/EmployeeController.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `config/time_logs_dtr.php`
- `resources/views/employees/partials/_campus-assignments.blade.php`
- `resources/views/employees/partials/_campus-assignment-row.blade.php`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `resources/js/app.js`
- `public/build/assets/app-BXaYFZEQ.js`

**Summary:**
- New `tbl_employee_campus_assignments` with campus, biometric ID, college, department, program; migrated existing employee campus data.
- Assignment tab: repeatable rows with Add/Remove; primary assignment syncs back to `tbl_employees.campus_id`.
- `EmployeeBiometricResolver` maps campus + biometric ID to employee for DTR and time-log uploads.
- San Mateo (SA) DTR parser reads all worksheets, skips Schedule/Att Stat/Attendance Record/Exception tabs, parses Card Report blocks only; First time zone with Overtime fallback.
- CA/SU flat DTR parser unchanged.

## 11:18 — Dedupe program dropdown options per campus

**Why:** Program dropdown in campus assignments showed duplicate names (e.g. Associate in Computer Technology twice).

**Files:**
- `app/Services/EmployeeFormOptions.php`

**Summary:**
- `tbl_programs` has multiple active rows with the same `program_name` on one campus; dropdown value is name-only, so duplicates appeared in the searchable select.
- Form options now `unique()` by campus + program name before rendering.

## 11:25 — Fix employee list crash on campus name

**Why:** Back button from employee details to list threw "Attempt to read property campus_name on string".

**Files:**
- `app/Models/Employee.php`
- `resources/views/employees/_results.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `resources/views/employees/partials/_campus-assignment-row.blade.php`

**Summary:**
- `tbl_employees.campus` stores campus code string; it shadows the `campus()` relationship when accessed as `$employee->campus`.
- Added `campus_name` accessor that reads the eager-loaded relation (or falls back to code).
- Updated employee list/show/assignment views to use `$employee->campus_name`.

## 19:30 — Fix San Mateo DTR upload config validation

**Why:** DTR upload for San Mateo failed (419 page / format not configured errors in logs).

**Files:**
- `app/Support/TimeLogsDtr.php`
- `app/Http/Controllers/TimeLogsController.php`
- `bootstrap/app.php`

**Summary:**
- `campusFormat()` incorrectly required `columns` for all campuses; SA Card Report parser has no flat columns.
- DTR upload max file size aligned to shared `uploads.max_file_kb` (15 MB).
- CSRF/session expiry on time-log uploads now redirects back with a clear error instead of a bare 419 page.

## 19:35 — Fix San Mateo Card Report row extraction

**Why:** DTR upload found Card Report sheet but returned "No data rows found" despite employee biometric ID 1 being configured.

**Files:**
- `app/Services/TimeLogsDtr/SanMateoCardReportParser.php`

**Summary:**
- Attendance period dates in Excel serial format were not parsed, so no daily rows were emitted.
- Row scan now uses max row index (not `count()` on sparse arrays).
- Improved biometric ID, On-duty/Off-duty column detection, and inline `ID: 1` cell parsing.

## 19:42 — Parse real San Mateo DTR workbook (64 sheets, WeekDate header)

**Why:** User sample file still returned "No data rows found" after prior parser fixes.

**Files:**
- `app/Services/TimeLogsDtr/SanMateoCardReportParser.php`
- `docs/samples/san-mateo-dtr-sample.xls`

**Summary:**
- Real file uses `WeekDate` (no space) and 64 Card Report tabs; parser only matched `Week Date`.
- Loading all sheets at once exceeded 128MB memory; now reads one sheet at a time with `readDataOnly`.
- Sample file parses 330 rows; biometric ID 1 resolves to employee 2026-00004 (14 valid days).
