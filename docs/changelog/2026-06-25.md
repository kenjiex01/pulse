## 17:35 — Remove TOIL Settings policy tab

**Why:** Hide the TOIL Settings tab from the Timekeeping Policy settings UI.

**Files:**
- `config/timekeeping_policy.php`
- `resources/views/timekeeping/policy/index.blade.php`

**Summary:**
- Removed `toil-settings` from `settings_tabs` so it no longer appears in the policy tab bar.
- Removed the `toil-settings` switch case from the policy index view.
- Direct `/toil-settings` URLs now 404 via existing route tab validation.
