## 12:10 — Clear Max Flexi-time when Allow Flexi-time is No

**Why:** Kapag naka-No ang Allow Flexi-time, dapat walang laman ang Max. Flexi-time (minutes) field.

**Files:**
- `resources/js/app.js`
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`
- `public/build/assets/app-BQkp8fh4.js`

**Summary:**
- `syncFlexiFields()` clears `[data-flexi-field]` value when flexi-time is disabled.
- Blade initial render shows empty max flexi-time value when Allow Flexi-time is No.
- Backend already saves `max_flexi_time` as null when flexi is off.

## 14:01 — Remove Tardiness and Undertime Equivalents UI

**Why:** Tanggalin ang Tardiness/Undertime Equivalents sections sa Tardiness and Undertime tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-tardiness-undertime.blade.php`

**Summary:**
- Inalis ang dalawang `_equivalent-section` includes (Tardiness Equivalents at Undertime Equivalents).
- Settings form lang ang natira sa tab na ito.

## 14:44 — Remove Overtime Equivalents UI

**Why:** Tanggalin ang Overtime Equivalents section sa Overtime tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-overtime.blade.php`

**Summary:**
- Inalis ang `_equivalent-section` include para sa overtime.

## 14:53 — Add Compute Night Differential fields

**Why:** Wala pa sa Pulse ang Compute Night Differential + Start/End Time na meron sa paths-mvc.

**Files:**
- `database/migrations/2026_06_18_150000_add_night_diff_times_to_timekeeping_policies.php`
- `app/Models/TimekeepingPolicy.php`
- `app/Support/TimekeepingPolicy.php`
- `resources/views/timekeeping/policy/_settings-night-differential.blade.php`
- `resources/js/app.js`
- `scripts/check_timekeeping_policy.php`
- `public/build/assets/app-BK9lklRo.js`

**Summary:**
- Dinagdag ang `night_diff_start` at `night_diff_end` columns.
- Night Differential tab: Compute checkbox, Start Time, End Time (hh:mm), at Deduct Break.
- Kapag naka-off ang compute, na-clear ang times sa save (paths-mvc behavior).

## 14:56 — Remove ND deduct break checkbox

**Why:** Tanggalin ang `Deduct Break from Night Differential` sa Night Differential tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-night-differential.blade.php`

**Summary:**
- Inalis ang checkbox para sa `nd_deduct_break` sa Night Differential UI.

## 14:59 — Remove Night Differential Equivalents UI

**Why:** Tanggalin ang Night Differential Equivalents section sa Night Differential tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-night-differential.blade.php`

**Summary:**
- Inalis ang `_equivalent-section` include para sa night differential.

## 15:03 — Remove Break Tardiness Equivalents UI

**Why:** Tanggalin ang Break Tardiness Equivalents section sa Breaks tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-breaks.blade.php`

**Summary:**
- Inalis ang `_equivalent-section` include para sa break tardiness.

## 15:20 — Add Timekeeping Policy module tabs

**Why:** Dapat may 5 tabs sa Timekeeping Policy submodule: Policy, Shift Code, Time Capturing Settings, Templates, at Holiday Settings.

**Files:**
- `config/timekeeping_policy.php`
- `app/Support/TimekeepingPolicy.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `routes/web.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `resources/views/timekeeping/policy/_module-tabs-nav.blade.php`
- `resources/views/timekeeping/policy/_module-placeholder.blade.php`
- `resources/views/timekeeping/policy/_policy-list.blade.php`

**Summary:**
- Dinagdag ang 5 module-level tabs sa Timekeeping Policy page.
- `Policy` tab: existing policy list at CRUD.
- `Shift Code`, `Time Capturing Settings`, `Templates`, `Holiday Settings`: placeholder pages (coming soon).
- Inayos ang routing: `timekeeping/policy/{tab}` para sa module tabs; policy settings naka-`/{policy}/{tab}` pa rin.

## 17:04 — Port Shift Code maintenance from paths-mvc

**Why:** I-port ang shift code CRUD logic (with breaks) sa Shift Code tab ng Timekeeping Policy.

**Files:**
- `database/migrations/2026_06_18_160000_create_shift_code_tables.php`
- `app/Models/ShiftCode.php`, `app/Models/ShiftCodeBreak.php`
- `app/Support/ShiftCode.php`
- `app/Http/Controllers/ShiftCodeController.php`
- `resources/views/timekeeping/shift-codes/_list.blade.php`
- `resources/views/timekeeping/shift-codes/_results.blade.php`
- `resources/views/timekeeping/shift-codes/_form.blade.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `routes/web.php`
- `resources/js/app.js`
- `database/seeders/ShiftCodeSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `public/build/assets/app-AjbKQOrA.js`

**Summary:**
- `tbl_shift_codes` + `tbl_shift_code_breaks` tables (dynamic breaks, replace-on-edit).
- List/search/pagination, modal Add/Edit, delete with in-use check stub.
- Validation: unique uppercase shift code, 24h time format, break minutes > 0.
- Sample seed data (0001, 0002, 0015).

---

## 17:30 — Remove Shift Code seeder

**Why:** User requested no seeder for shift codes — records should be created via UI only.

**Files:**
- `database/seeders/ReferenceDataSeeder.php`
- `database/seeders/ShiftCodeSeeder.php` (deleted)

**Summary:**
- Removed `ShiftCodeSeeder` from `ReferenceDataSeeder` call list.
- Deleted `ShiftCodeSeeder.php`; shift code CRUD remains, no default seed data.

---

## 18:00 — Soft deletes for all module/submodule deletes

**Why:** User requested all delete actions across modules and submodules use soft delete only.

**Files:**
- `database/migrations/2026_06_18_180000_add_soft_deletes_to_module_tables.php`
- `app/Support/ValidationRules.php`
- `app/Models/*` (37 CRUD + child models — `SoftDeletes` trait)
- `app/Support/HrLookup.php`, `PayrollMaintenance.php`, `GovernmentTables.php`, `RateDefinition.php`, `ShiftCode.php`
- `app/Services/EmployeeEmploymentSync.php`, `EmployeeSalarySync.php`
- `app/Http/Controllers/RateDefinitionController.php`
- `app/Http/Requests/User/*`, `Role/StoreRoleRequest.php`, `Employee/Concerns/EmployeeFormRules.php`
- `.cursor/rules/soft-deletes.mdc`

**Summary:**
- Added `deleted_at` to all module CRUD tables (users, roles, employees, HR lookups, payroll, government tables, shift codes, timekeeping policy equivalents, and child tables).
- All destroy endpoints now soft-delete via Eloquent `SoftDeletes`; parent models cascade soft-delete to children on delete.
- Unique validation ignores soft-deleted rows (`whereNull('deleted_at')`).
- Internal sync-on-edit paths (breaks, pivots, employment/salary replace) use `forceDelete()` only for row replacement, not user delete.
- `sys_logs` remains append-only.
