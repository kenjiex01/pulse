## 11:35 — PATHS-style Attendance View on Employee Profile

**Why:** Replace raw-log list in Timekeeping → Attendance View with daily PATHS-like summary; OT/late come from processed payroll batches; keep per-day add/edit/delete of punches.

**Files:**
- `app/Services/EmployeeAttendanceViewService.php`
- `app/Services/EmployeeAttendanceLogService.php`
- `app/Services/EmployeeShiftResolver.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/views/timekeeping/employee-profile/_attendance-day-modal.blade.php`
- `resources/views/timekeeping/employee-profile/_attendance-day-content.blade.php`
- `tests/Feature/EmployeeAttendanceLogCrudTest.php`

**Summary:**
- Attendance View tab now shows monthly daily rows: Date, Day Type, Shift, Time In/Out, Basic, Excess, OT, SOT, NDiff, NDOT, NDSOT, Late, Undertime, Break Late
- Walang Process Attendance button — OT/late/etc. lalabas lang kapag covered ang araw ng processed payroll batch
- Pencil per day opens modal para mag-add / edit / delete ng raw logs
- Fixed create: `reference_number` is INT — manual logs use null (not string `MANUAL`)
- Fixed infinite loop in `EmployeeShiftResolver::loadOverridesForRange` with CarbonImmutable (`$cursor = $cursor->addDay()`)
- Feature test covers add → edit → delete + attendance view load

## 11:56 — Attendance View shift label uses parentheses

**Why:** Match preferred shift display format (code in parentheses, not pipes).

**Files:**
- `app/Services/EmployeeAttendanceViewService.php`

**Summary:**
- Shift column format: `08:00 to 17:00 (AM2)` instead of `08:00 to 17:00 | AM2 |`

## 11:57 — Enlarge Employee Profile modals

**Why:** Attendance View table needs more width; modal was too narrow (columns cut off).

**Files:**
- `resources/views/timekeeping/employee-profile/_results.blade.php`

**Summary:**
- View + Edit Timekeeping Settings modals: `max-w-3xl` / `max-w-4xl` → `max-w-7xl`

## 11:59 — Fix modal width (Attendance View)

**Why:** `.modal-panel` hard-coded `max-w-lg` was overriding `max-w-7xl`, so Edit Timekeeping Settings stayed narrow.

**Files:**
- `resources/css/app.css`
- `resources/views/partials/modal.blade.php`
- `resources/views/timekeeping/employee-profile/_results.blade.php`

**Summary:**
- Removed fixed `max-w-lg` from `.modal-panel`; default width via `panelClass` (`max-w-lg` if omitted)
- Employee Profile view/setup modals use `max-w-[96vw]` for near-full width
- Ran `npm run build` for production assets

## 12:01 — Attendance View pagination

**Why:** Monthly attendance table needs page size control so long months are easier to scan.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `tests/Feature/EmployeeAttendanceLogCrudTest.php`

**Summary:**
- Added client pagination under Attendance View: Show dropdown (10/25/50/100), page nav, and Show all
- Default 10 rows per page; day edit modals stay available for all dates even when rows are paged

## 12:04 — Desktop stays usable offline

**Why:** Pulse desktop must run without internet; cloud work should wait until the machine is online.

**Files:**
- `app/Support/DesktopConnectivity.php`
- `app/Services/DesktopInstallerUpdateService.php`
- `app/Services/DesktopCloudBackupService.php`
- `tests/TestCase.php`
- `tests/Unit/DesktopConnectivityTest.php`

**Summary:**
- Core app (SQLite, payroll, timekeeping, UI) is already local — no internet required to launch or use
- Fast connectivity probe before S3 installer-update check and daily cloud backup
- Offline: skip those calls (no 60s page hang); retry automatically once online
- Manual actions that need the net (Skolaris pull, biometric S3 pull, installer download, LibreOffice download) still run only when the user starts them

## 12:07 — Hide SOT and NDSOT on Attendance View

**Why:** Those columns are not needed on the Employee Profile daily attendance table.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `tests/Feature/EmployeeAttendanceLogCrudTest.php`

**Summary:**
- Removed Sot and Ndsot columns; kept Ot, Ndiff, and Ndot

## 12:13 — Attendance View OT follows processed payroll batch

**Why:** Aug 10 showed 3.00 OT in Attendance View vs 2.00 (120 min) in the processed batch Overtime Details.

**Files:**
- `app/Services/PayrollAttendanceDayBreakdownService.php`
- `app/Services/EmployeeAttendanceViewService.php`
- `tests/Feature/EmployeeAttendanceViewPayrollOtTest.php`

**Summary:**
- Attendance View OT now uses the same per-day minutes as Process Batch (approved window ∩ excess, minus tardiness-OT offset)
- Late 40 min marked absent claws 1 hour from the 3-hour OT window → 2.00 hours, matching the batch modal

## 12:22 — Attendance View PDF download

**Why:** Employee Profile Attendance View needs a PDF export of the monthly daily table.

**Files:**
- `app/Services/EmployeeAttendanceViewService.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Support/ReportPdfDownload.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `tests/Feature/EmployeeAttendanceLogCrudTest.php`

**Summary:**
- Added **PDF** button next to month navigation
- Download is landscape and matches on-screen columns (no SOT / NDSOT)
- Filename: `Attendance_View_{employee-number}_{YYYY-MM}.pdf`
- Feature test checks the file is a PDF and that employee, month, date, and time in/out appear in the content

## 12:36 — Attendance View report (date range + employees)

**Why:** Users need a Reports PDF of Attendance View for a chosen date range and selected employees, not only one person from Employee Profile.

**Files:**
- `app/Services/EmployeeAttendanceViewService.php`
- `app/Services/Reports/AttendanceViewReportService.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `config/payroll_reports.php`
- `database/seeders/ReportSeeder.php`
- `resources/views/payroll/reports/options/attendance-view.blade.php`
- `resources/views/payroll/reports/_attendance-view-document.blade.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `resources/views/payroll/reports/_preview.blade.php`
- `resources/views/payroll/reports/pdf-document.blade.php`
- `resources/views/payroll/reports/index.blade.php`
- `resources/js/app.js`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Added **Attendance View** under Reports → Timekeeping (classification now active)
- Options: Date From / Date To, searchable employee multi-select (search bar + select all shown), output HTML / Excel / PDF
- PDF has a section per employee with the same columns as Employee Profile Attendance View
- Date range capped at 92 days
- Ran ReportSeeder and `npm run build`

## 13:02 — BIR 1601-C and 2316 official templates

**Why:** Stamp 1601-C and 2316 onto the current BIR ENCS blanks (Jan 2018 / Sep 2021) instead of the old legal-size PATHS forms.

**Files:**
- `resources/bir-forms/BIR1601c.pdf`
- `resources/bir-forms/BIR2316.pdf`
- `app/Services/Reports/Bir1601cOfficialPdfWriter.php`
- `app/Services/Reports/Bir2316OfficialPdfWriter.php`
- `app/Services/Reports/Bir1601cReportService.php`
- `app/Services/Reports/Bir2316ReportService.php`
- `app/Support/Bir1601cFormMapper.php`
- `tests/Unit/Bir1601cFormMapperTest.php`
- `tests/Feature/BirOfficialPdfWriterTest.php`

**Summary:**
- Replaced blank templates: 1601-C January 2018 ENCS with DPA; 2316 September 2021 ENCS
- Page size is now 215.9 × 330.2 mm (was 215.9 × 355.6)
- 1601-C stamps items 14–36 (MWE SMW, holiday/OT for MWEs, 13th, de minimis, SSS/PHIC/HDMF, WHT)
- 2316 stamps Sep 2021 boxes: items 19–28 summary and 29–52 compensation
- Recalibrated header, TIN, ZIP, checkbox, and signatory coordinates

## 13:11 — Live 1601-C / 2316 check and alignment fixes

**Why:** Generated real reports from posted payroll; item 24 was negative and several stamps sat off the new ENCS boxes.

**Files:**
- `app/Support/Bir1601cFormMapper.php`
- `app/Services/Reports/Bir1601cOfficialPdfWriter.php`
- `app/Services/Reports/Bir2316OfficialPdfWriter.php`
- `tests/Unit/Bir1601cFormMapperTest.php`

**Summary:**
- 1601-C item 14 now uses the same gross as 2316 (includes SSS/PHIC/Pag-IBIG) so items 22–24 no longer go negative
- Split 1601-C pesos vs cents; left the form’s pre-printed WW010 ATC; moved Non-Individual name/title into the signature white space
- 2316 TIN, DOB, and phone now stamp per digit box; item 56 signature recentered
- HTTP generate path (Reports → PDF) verified for Balucan + Francisco on posted batch 4 / year 2026

## 13:34 — BIR 1601-C stamp alignment (month, amounts, checkbox)

**Why:** MM/YYYY digits, amount comb boxes, and Amended “No” X were off the Jan 2018 ENCS white cells.

**Files:**
- `app/Services/Reports/Bir1601cOfficialPdfWriter.php`

**Summary:**
- Month digit X from blank-template white-box centers; Y set so ink sits mid-box
- Amount peso/cent X from comb-tick midpoints; Y offset `bulletY - 2.0` so digits sit in the white band (not on the bottom rule)
- Amended Return “No” X recentered on measured checkbox
- Verified via blank-vs-stamped ink centroids; `BirOfficialPdfWriterTest` passed

## 16:00 — 1601-C item 15 for MWEs (gross less government deductions except WHT)

**Why:** Abrantes is an MWE but item 15 was 0 because basic pay was tagged taxable; item 15 should be gross minus SSS/PHIC/Pag-IBIG, not minus WHT.

**Files:**
- `app/Support/Bir1601cFormMapper.php`
- `app/Support/Bir2316FormMapper.php`
- `tests/Unit/Bir1601cFormMapperTest.php`
- `tests/Unit/Bir2316FormMapperTest.php`

**Summary:**
- 1601-C treats MWE from the salary flag (`is_above_minimum_wage_earner` unticked)
- Item 15 = gross compensation − SSS − PHIC − Pag-IBIG; withholding tax is not subtracted
- MWEs no longer roll into item 23 (that line is for non-MWEs)
- 2316 uses the same MWE flag so taxable-tagged MWE basic goes to items 29–37

## 16:04 — 1601-C item 16 MWE holiday / OT / ND / hazard

**Why:** Item 16 was empty; it should hold Holiday Pay, Overtime Pay, Night Shift Differential Pay, and Hazard Pay for MWEs.

**Files:**
- `app/Support/Bir1601cFormMapper.php`
- `app/Support/Bir2316FormMapper.php`
- `tests/Unit/Bir1601cFormMapperTest.php`
- `tests/Unit/Bir2316FormMapperTest.php`

**Summary:**
- MWE item 16 = 2316 items 30–33 (holiday, OT, ND, hazard)
- Those amounts are subtracted from item 15 so they are not counted twice
- 2316 now classifies HOLI/OVRT/NDIF/HAZD (and similar codes) instead of lumping them into Other

## 16:38 — 1601-C optional whole-year 13th month (item 17)

**Why:** Item 17 should be computed 13th month for the selected batch’s pay year when the new checkbox is on, and 0 when it is off.

**Files:**
- `resources/views/payroll/reports/options/bir-1601c.blade.php`
- `config/payroll_reports.php`
- `app/Support/Bir1601cFormMapper.php`
- `app/Services/Reports/Bir1601cReportService.php`
- `tests/Unit/Bir1601cFormMapperTest.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Added “Include 13th month pay (whole year)” on 1601-C options
- When checked: item 17 = posted BASC for that pay year ÷ 12 (₱90,000 cap per employee)
- When unchecked: item 17 = 0.00
- Live check (Abrantes, batch 4 / 2026): basic 7,181.25 → 13th 598.44; unchecked 17 = 0; official PDF generated

## 19:04 — 1601-C multi-select posted batches (same month)

**Why:** Allow selecting multiple posted payroll batches on BIR 1601-C, require the same pay month/year, and sum amounts into one return.

**Files:**
- `resources/views/payroll/reports/options/_batch-employee-picker.blade.php`
- `resources/views/payroll/reports/options/bir-1601c.blade.php`
- `config/payroll_reports.php`
- `app/Services/Reports/BirFormEmployeeSelection.php`
- `app/Services/Reports/Bir1601cReportService.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `resources/js/app.js`
- `tests/Feature/Bir1601cMultiBatchTest.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Posted Payroll Batch is now multi-select (`payroll_batch_ids[]`) with same pay month/year guard (UI + server)
- Employee list loads the union of selected posted batches
- Generated 1601-C totals sum selected batches per employee
- Tests: same-month proceeds and doubles Abrantes amounts; different-month blocked; npm build run
