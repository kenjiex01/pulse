## 14:30 — Company Documents Send button

**Why:** HR needs to email a company document template to selected employees directly from the template card.

**Files:**
- `app/Services/CompanyDocumentSendService.php`
- `app/Services/TimekeepingMemoEmailService.php`
- `app/Support/MemoPdfFilename.php`
- `app/Policies/CompanyDocumentFormPolicy.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `routes/web.php`
- `resources/views/company-documents/_card.blade.php`
- `resources/views/company-documents/_send-form.blade.php`
- `resources/views/company-documents/index.blade.php`
- `resources/js/company-documents.js`
- `tests/Unit/CompanyDocumentSendServiceTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Added **Send** button on each active company document card (update permission required).
- Send modal lists active employees with search, **Select all**, per-employee checkboxes, and Send action.
- Backend renders PDF per employee, creates a completed submission, and emails the document (uses Memo Setup email copy when linked, otherwise default subject/body).

## 14:40 — Fix company documents send employee query

**Why:** Index page crashed with unknown column `status` on `tbl_employees`.

**Files:**
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Send modal employee list now filters by `employment_status` and `is_active` (matches Employee model).

## 14:45 — Company document send history

**Why:** Users need to see who already received a document after using Send.

**Files:**
- `database/migrations/2026_09_17_143600_create_company_document_send_logs_table.php`
- `app/Models/CompanyDocumentSendLog.php`
- `app/Models/CompanyDocumentForm.php`
- `app/Services/CompanyDocumentSendService.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `resources/views/company-documents/_send-history.blade.php`
- `resources/views/company-documents/_send-form.blade.php`
- `resources/views/company-documents/index.blade.php`
- `tests/Unit/CompanyDocumentSendServiceTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Added `tbl_company_document_send_logs` to record each send (employee, sender, timestamp, submission link).
- Send modal now shows **Send history** table below the employee picker.
- Re-sending the same employee creates a new history row.

## 14:50 — Send history per-employee count and dates

**Why:** Users need to see how many times each employee was sent a document and on which dates.

**Files:**
- `app/Support/CompanyDocumentSendHistorySummary.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `resources/views/company-documents/_send-form.blade.php`
- `resources/views/company-documents/_send-history.blade.php`
- `resources/views/company-documents/index.blade.php`
- `tests/Unit/CompanyDocumentSendHistorySummaryTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Send history is grouped per employee with **Times sent** badge and full **Sent on** date list.
- Employee picker shows `Sent N× · last date` under names that were already sent this template.

## 15:00 — Company document send progress loader

**Why:** Batch send should show live progress (e.g. 27/250) on the full-screen loader while emails are sent.

**Files:**
- `resources/views/partials/full-screen-loader.blade.php`
- `resources/js/app.js`
- `resources/js/company-documents.js`
- `resources/views/company-documents/_send-form.blade.php`
- `resources/views/company-documents/index.blade.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `routes/web.php`

**Summary:**
- Send now posts one employee at a time via AJAX and updates `PulseLoader.showProgress(current, total)`.
- Loader text shows `27 / 250 — Sending to …` with a moving progress bar.
- Added `send-one` and `send-batch-complete` routes for progressive send + audit summary.
