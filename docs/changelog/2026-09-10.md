## 15:46 — Remove anti-cheat quiz demos (flicker + AI watermark)

**Why:** User confirmed AI watermark test still answered by ChatGPT; demos discarded — not pursuing screen-based anti-photo/anti-AI tricks.

**Files:**
- `public/demo/ai-watermark-quiz-demo.html` (deleted)
- `public/demo/flicker-quiz-demo.html` (already deleted)

**Summary:**
- Removed standalone demo HTML files under `public/demo/`.

## 15:42 — AI refusal watermark quiz demo (replaces flicker demo)

**Why:** Remove flicker experiment; try prompt-injection style watermarks so photographed questions may cause AI to refuse answering.

**Files:**
- `public/demo/ai-watermark-quiz-demo.html`
- `public/demo/flicker-quiz-demo.html` (deleted)

**Summary:**
- Standalone demo with student watermark, footer integrity notice, corner stamps, and low-contrast embedded “do not answer” text for OCR/vision models.
- Manual test only — upload phone photo to ChatGPT/Gemini and check if model refuses.

## 11:53 — ICCT Code of Offenses company document templates

**Why:** Seed HR disciplinary memos from the ICCT Colleges Code of Offenses and Table of Penalties (effective February 16, 2011).

**Files:**
- `database/seeders/CompanyDocumentIcctOffensesSeeder.php`
- `database/seeders/DesktopReferenceDataSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Added eight Company Document templates: Notice to Explain, Verbal Reprimand Record, Written Warning, Notice of Suspension, Notice of Dismissal, Return to Work Form (Appendix B), AWOL notice, and Uniform/ID/Nameplate infraction.
- Templates include offense heading, category A–D, frequency, and the official penalty table. Existing designer edits are not overwritten.

## 11:49 — People360 1.0.3 desktop release

**Why:** Ship company-document memos (preview-matching email PDF), payslip email, and pending HRIS work in a paired macOS + Windows build.

**Version:** `NATIVEPHP_APP_VERSION=1.0.3`

**Installers:**
- `dist/People360-1.0.3-arm64.dmg`
- `dist/People360-1.0.3-arm64.zip`
- `dist/People360-1.0.3-setup.exe`

**Summary:**
- GitHub release **v1.0.3** published on `kenjiex01/pulse` (auto-update feed).
- macOS DMG/ZIP and Windows EXE verified after `./scripts/build-desktop.sh`.

## 11:24 — PDF text fields use preview width and height (content-box)

**Why:** Dompdf ignores `box-sizing`, so `height:38px` plus padding made email fields taller and wider than Memo Preview.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `resources/views/shared/company-document-memo-canvas-field-box.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- PDF field boxes are sized as content-box so the painted area stays 38×624px (short) and 96×624px (message body) — same as preview.
- Signature box uses the same conversion so it does not grow past 120px.

## 11:21 — Memo PDF fields match preview size and rounded corners

**Why:** Email PDF used table cells for inputs, so boxes were taller than preview and had square corners.

**Files:**
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `resources/views/shared/company-document-memo-canvas-field-box.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- Text fields and signature use the same div box as Memo Preview: 38px / 96px height, 8px 12px padding, 6px rounded corners.
- Explicit `height` keeps empty PDF fields from collapsing without changing preview layout.

## 11:16 — Email memo PDF matches Memo Preview column and field boxes

**Why:** The emailed PDF stretched fields across the legal page (Dompdf ignored side-margin shorthand and collapsed empty `min-height` boxes), so it did not match Memo Preview.

**Files:**
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `resources/views/shared/company-document-memo-canvas-field-box.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- PDF uses a centered 640px form column (explicit table width) and longhand `@page` left/right margins.
- Empty fields are table cells with a fixed height so they stay tall like the preview inputs.
- Memo Preview HTML is unchanged (rounded divs on the legal sheet).

## 11:12 — Memo email PDF uses a 640px form column (not stretched to 8.5in)

**Why:** The emailed memo stretched input fields across the full legal sheet (16px side padding only), so boxes looked extra wide compared with the designer/preview.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Form content stays at the 640px designer width, centered on legal (8.5 × 14 in) paper.
- Side margins are 88px / 66pt so fields and the SAMPLE stamp keep designer proportions.
- Preview HTML, designer canvas, and email PDF share the same column width.

## 11:08 — Preview and email PDF share the same legal-width layout

**Why:** Memo Preview shrank the paper (`max-width: 100%`) but left the SAMPLE stamp at 436px, so the stamp looked huge in Preview and smaller on the emailed PDF.

**Files:**
- `resources/views/pdf/company-document-memo-document.blade.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Preview sheet stays 8.5in wide (same as the email PDF). If the modal is narrower, the whole page zooms uniformly — stamp and fields shrink together.
- Email PDF is unchanged legal size, so what you preview is what gets sent.

## 11:03 — Email memo PDF uses the same legal sheet as Memo Preview

**Why:** The sent PDF was a short content-sized page. Memo Preview shows an 8.5×14 in sheet with 16px padding — they did not match.

**Files:**
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/timekeeping/memo/_preview-modal.blade.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Email PDF page is legal (8.5×14 in) with the same 16px padding as the HTML preview.
- Canvas has an in-flow spacer so Dompdf keeps the full sheet height.
- Memo Preview modal uses the same wide panel as Company Documents Preview.

## 10:52 — Document Preview shows a full legal sheet (not a PDF thumbnail)

**Why:** The modal iframe loaded a PDF. Chromium/desktop PDF viewer draws the page as a tiny thumbnail in a large white pane.

**Files:**
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/timekeeping/memo/_preview-content.blade.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `app/Services/TimekeepingMemoPreviewService.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `tests/Feature/CompanyDocumentFormTest.php`
- `tests/Feature/TimekeepingMemoTest.php`

**Summary:**
- Company Documents and Timekeeping Memo preview iframes load `preview-html` — a readable 8.5×14 in sheet that fills the modal.
- Email still attaches the legal-size PDF from `renderPdf()`.

## 10:46 — Equal left/right margins; full SAMPLE stamp (not clipped)

**Why:** Designer paper was a fixed 816px inside a padded 816px card, so the right side was clipped — stamp cut off, no right margin.

**Files:**
- `resources/views/company-documents/designer.blade.php`
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- Legal sheet is `width: 100%` with 16px padding on all sides so left and right gutters match.
- PDF uses equal `@page` margins (12pt) instead of a left-only extra margin.
- SAMPLE stamp is no longer clipped (`overflow: visible`, `object-fit: contain`).

## 10:40 — Legal-size canvas for Company Documents and Memo preview

**Why:** Company Documents preview showed a tiny custom-size PDF. Canvas should be legal bond paper (8.5 × 14 in); overflow starts a new page. Same for Timekeeping Memo.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/timekeeping/memo/_preview-content.blade.php`
- `resources/views/company-documents/designer.blade.php`
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`
- `tests/Feature/TimekeepingMemoTest.php`

**Summary:**
- PDF paper is legal (8.5 × 14 in). Content past one page is placed on the next page (`page_break` also starts a new page).
- Company Documents Preview and Timekeeping Memo Preview show that legal-size PDF.
- Designer canvas is a legal-size sheet with page-break guides.

## 10:34 — Keep SAMPLE stamp inside the page (no right overflow)

**Why:** Dompdf painted the 646px stamp PNG at intrinsic size while fields used `width:100%`, so the stamp spilled off the right edge.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- Fields use fixed pixel widths (640px), not `100%`, so they share the stamp’s coordinate space.
- Images that would cross the right edge are shifted or scaled to stay on the 640px canvas.
- Stamp is rendered in a fixed-size table cell; canvas `overflow:hidden`.

## 10:31 — Memo preview is the email PDF (same bytes)

**Why:** Memo module preview rendered browser HTML while email attached Dompdf — stamp size, field heights, and alignment never matched.

**Files:**
- `resources/views/timekeeping/memo/_preview-content.blade.php`
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `app/Support/CompanyDocumentMemoDompdfPreparer.php`
- `tests/Feature/TimekeepingMemoTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Timekeeping Memo and Company Documents preview iframes load `preview-pdf` — same `renderPdf()` bytes as the email attachment.
- PDF fields use styled boxes (not `<input>`/`<textarea>`) so Dompdf does not inflate native form controls.
- Stamp images are baked with object-fit:contain + opacity at designer width/height.

## 10:26 — Unified memo preview + email pipeline (HTML source, custom PDF page)

**Why:** Email PDF had a blank first page (A4 overflow), thick div borders unlike preview, and preview/email used different rendering paths.

**Files:**
- `app/Services/CompanyDocumentMemoRenderService.php`
- `app/Services/TimekeepingMemoPreviewService.php`
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `app/Http/Controllers/TimekeepingMemoController.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/timekeeping/memo/_preview-content.blade.php`
- `routes/web.php`
- `tests/Feature/TimekeepingMemoTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Preview iframe loads `preview-html` — same `renderDocumentHtml()` blade as email PDF input (640px designer canvas, readonly inputs).
- Email uses `renderPdf()` on that same HTML with custom page size (canvas + padding) — no A4 blank/extra pages.
- Fields use readonly `<input>`/`<textarea>` matching designer controls; employee memos verified 1 page.

## 10:22 — PDF margins from designer layout, not fixed 0.5in

**Why:** User requested alignment match preview — spacing should follow designer canvas (640px, CANVAS_INSET, pos_x/pos_y), not a static half-inch @page margin.

**Files:**
- `resources/views/pdf/company-document-memo-document.blade.php`
- `app/Support/CompanyDocumentMemoPdfLayout.php`

**Summary:**
- `@page` margin set to `0`; body padding uses `PREVIEW_BODY_PADDING` (16px) like the preview modal.
- `maxCanvasHeightForPage()` uses full A4 height minus that padding only — element positions drive alignment.

## 10:19 — PDF matches designer layout (640px, stamp, empty fields)

**Why:** Emailed PDF scaled to ~570px, showed em dashes in empty fields, and stretched the SAMPLE stamp — different from People360 HTML preview.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Support/CompanyDocumentMemoDompdfPreparer.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`

**Summary:**
- `buildForPdf()` keeps 640px designer width when canvas fits one page (tolerance for ~1034px templates).
- SAMPLE stamp uses `object-fit: contain`; opacity + rotation baked into PNG for Dompdf.
- Empty fields render blank (like HTML preview), not em dashes.

## 10:15 — Preview/email PDF parity (canvas-only, placeholders)

**Why:** Email PDF duplicated title inside the file, showed field labels as values ("Memo To"), and Company Documents still used HTML preview — all different from People360 preview.

**Files:**
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `app/Services/CompanyDocumentMemoValueResolver.php`
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- PDF attachment is canvas-only (no duplicate title/description — matches modal preview chrome).
- Input fields use `placeholder` when empty, not the field label; only `default_text` resolves merge tags.
- Company Documents preview always embeds the same PDF iframe as memo emails (removed HTML/JS preview path).
- Larger printable canvas area (no header reserve) keeps layout closer to 640px designer width.

## 10:12 — Single-page memo PDF + 0.5in margins

**Why:** Long absence lists made the canvas 1100px tall — Dompdf split to 2 pages and pushed the SAMPLE stamp to page 2; user requested 0.5 inch margins.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`

**Summary:**
- PDF uses `buildForOutput()` again — scales canvas to fit one A4 page (`maxCanvasHeightForPage()` with 0.5in margins + header reserve).
- `@page` margin set to `0.5in`; canvas uses fixed `height` + `page-break-inside: avoid` so overlay images stay on page 1.
- Verified employee `23-LIB250200` absent memo renders as 1 page.

## 10:09 — Memo PDF stamp image + 0.25in margins

**Why:** SAMPLE stamp image disappeared in emailed PDF (Dompdf ignores CSS opacity on divs); content needed uniform 0.25 inch page margins.

**Files:**
- `app/Support/CompanyDocumentMemoDompdfPreparer.php`
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- Bake `image_opacity` into PNG during PDF materialization so overlay stamps render in Dompdf.
- Image elements use `z-index: 30` so stamps stay above form fields.
- PDF `@page` margin set to `0.25in` on all sides.

## 10:03 — Memo preview matches email PDF (WYSIWYG)

**Why:** Company Documents preview used HTML/JS at 640px while email sent a scaled Dompdf PDF — margins and alignment did not match.

**Files:**
- `resources/views/company-documents/_preview-content.blade.php`
- `app/Http/Controllers/CompanyDocumentFormController.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `app/Services/CompanyDocumentMemoValueResolver.php`
- `routes/web.php`
- `tests/Feature/CompanyDocumentFormTest.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- Memo templates in Company Documents now embed a PDF iframe (`preview-pdf` route) — same bytes as email attachments.
- PDF layout uses fixed 640px canvas (`build()`) instead of auto-scaling tall memos (`buildForOutput()`).
- Added `buildSamplePreviewData()` / `renderSamplePdf()` for template preview with sample merge-tag values.
- Timekeeping Memo preview/send pipeline unchanged — still exact PDF iframe + attachment parity.

## 13:32 — Uploaded faculty loading on Teaching Loads tab

**Why:** Port Skolaris “Uploaded Faculty Loading” into People360 Time Logs → Teaching Loads while keeping existing Skolaris pull history.

**Files:**
- `database/migrations/2026_09_10_130000_create_uploaded_faculty_load_tables.php`
- `app/Models/PulseFacultyLoadUpload.php`
- `app/Models/PulseFacultyLoadItem.php`
- `app/Services/PulseTeachingLoadPdfParser.php`
- `app/Services/UploadedFacultyLoadService.php`
- `app/Http/Controllers/TimeLogsController.php`
- `app/Support/TimeLogs.php`
- `routes/web.php`
- `config/filesystems.php`
- `composer.json`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_teaching-loads-source-tabs.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-load-filters.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-loads-results.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-load-upload-form.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-load-detail.blade.php`
- `tests/Feature/UploadedFacultyLoadTest.php`

**Summary:**
- Teaching Loads tab now has sub-tabs: **Skolaris Pulls** (existing) and **Uploaded PDFs** (new).
- Header keeps **Pull from Skolaris** and adds **Upload PDF**; uploads parse ICCT college/SHS faculty loading PDFs via ported parser.
- Uploaded loads list supports search + parse status filter; detail modal shows parsed rows, PDF preview/download, reparse, and soft delete.
- Parsed upload data stays in People360 only — not synced to Skolaris course offerings.

## 13:40 — Uploaded faculty loads: pull from Skolaris only

**Why:** People360 should not upload/parse PDFs locally; it should only pull uploads already added in Skolaris.

**Files:**
- `database/migrations/2026_09_10_140000_add_skolaris_sync_to_uploaded_faculty_loads.php`
- `app/Services/UploadedFacultyLoadService.php`
- `app/Services/SkolarisApiService.php`
- `app/Models/PulseFacultyLoadUpload.php`
- `app/Http/Controllers/TimeLogsController.php`
- `app/Support/TimeLogs.php`
- `routes/web.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-load-pull-form.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-load-filters.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-loads-results.blade.php`
- `resources/views/timekeeping/time-logs/_uploaded-faculty-load-detail.blade.php`
- `tests/Feature/UploadedFacultyLoadTest.php`
- Removed `app/Services/PulseTeachingLoadPdfParser.php`, local upload form, and `smalot/pdfparser` dependency.

**Summary:**
- **Uploaded PDFs** sub-tab now syncs from Skolaris API (`/pulse-uploaded-faculty-loading`) via **Pull from Skolaris**.
- Local cache stores metadata, parsed rows, and PDF copy keyed by `skolaris_upload_id`.
- Removed People360-side PDF upload, parse, and reparse flows.

## 13:55 — Teaching load pull includes Attendance Checker marks

**Why:** When Skolaris Attendance Checker already has schedules/marks, People360 pull should include them.

**Files:**
- `app/Services/SkolarisApiService.php`
- `app/Services/TeachingLoadPullService.php`
- `resources/views/timekeeping/time-logs/_pull-form.blade.php`

**Summary:**
- Skolaris Pulls now also fetches `/attendance-checker/daily` per campus/date and merges P/A/L/U/E/M marks (and any AC-only schedules) into pulled teaching load rows.
- Uploaded PDFs remain a separate sub-tab/pull from `/pulse-uploaded-faculty-loading`.

## 14:51 — ICCT Table of Penalties lookup table

**Why:** Store the ICCT offense penalty matrix (Categories A–D × frequency) in the database for memo templates and future HRIS use.

**Files:**
- `database/migrations/2026_09_10_150000_create_lu_icct_offense_penalties_table.php`
- `app/Models/LuIcctOffensePenalty.php`
- `database/seeders/IcctOffensePenaltySeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `database/seeders/DesktopReferenceDataSeeder.php`
- `database/seeders/CompanyDocumentIcctOffensesSeeder.php`
- `tests/Unit/IcctOffensePenaltyTest.php`

**Summary:**
- Added `lu_icct_offense_penalties` with 13 seeded rows matching the ICCT matrix (empty cells omitted).
- `CompanyDocumentIcctOffensesSeeder` now reads penalty dropdown choices and table text from the lookup table.
- Unit tests verify seeded matrix values and distinct penalty options.
