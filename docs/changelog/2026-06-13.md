# Code Changelog — 2026-06-13

> Daily log of `pulse/` code changes. Append-only.

---

## 14:11 — Government tables seeder

**Why:** Seed government payroll lookup tables for payroll module.

**Files:**
- `database/seeders/GovernmentTablesSeeder.php`

**Summary:**
- Added seeder for SSS, PhilHealth, Pag-IBIG, and withholding tax tables.

---

## 14:18 — Database seeder wiring

**Why:** Align default seed order and admin account with ICCT deployment.

**Files:**
- `database/seeders/DatabaseSeeder.php`

**Summary:**
- Registered Module, SubModule, RoleModule, Campus, EmployeeLookup, and Employee seeders.
- Default admin: `superadmin@icct.edu.ph` with role sync via many-to-many.
- Removed legacy `admin@pulso.local` user.

---

## 14:22 — Employee form options service

**Why:** Centralize dropdown data for employee forms with campus and address cascading.

**Files:**
- `app/Services/EmployeeFormOptions.php`

**Summary:**
- Campus-filtered colleges and programs.
- Region → province → city cascading lookups.
- Legacy option helper for values no longer in lookup tables.

---

## 14:23–14:24 — Searchable select UI component

**Why:** Replace plain `<select>` with searchable dropdowns on employee and lookup forms.

**Files:**
- `resources/css/app.css`
- `resources/js/searchable-select.js`

**Summary:**
- New `SearchableSelect` class with keyboard nav, filter, and disabled state.
- CSS for trigger, panel, options, empty state, and datatable/sidebar utilities.

---

## 14:31 — ICCT college and program seeders

**Why:** Populate lookup tables aligned with Skolaris / icct.edu.ph.

**Files:**
- `database/seeders/CollegeSeeder.php`
- `database/seeders/ProgramSeeder.php`

**Summary:**
- Colleges: CCS, CHS, ISHTM, CoTEd, CoEDT, CAS, CBA, and related ICCT colleges.
- Programs: BSCS, BSIT, BSN, BSHM, BEED, BSCPE, BSA, and related bachelor programs per college.

---

## 14:46–14:47 — Employee employment information refactor

**Why:** Support multiple employment records per employee (hybrid Faculty + Staff) instead of flat columns on `tbl_employees`.

**Files:**
- `database/migrations/2026_06_11_100000_create_employee_employment_information_table.php`
- `app/Models/EmployeeEmploymentInformation.php`
- `app/Models/Employee.php`
- `app/Services/EmployeeEmploymentSync.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Http/Requests/Employee/WizardDetailsRequest.php`
- `app/Http/Controllers/EmployeeController.php`
- `database/seeders/EmployeeSeeder.php`
- `resources/js/app.js`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `resources/views/employees/_wizard-step-review.blade.php`
- `resources/views/employees/partials/_employment-information.blade.php`
- `resources/views/employees/partials/_employment-information-row.blade.php`

**Summary:**
- New `tbl_employee_employment_information` table; migrated data from old employee columns.
- Added `is_hybrid` flag on employees; hybrid shows Faculty + Staff panels.
- `EmployeeEmploymentSync` handles replace-all sync and hybrid record normalization.
- Employee model accessors read from primary employment record; `logSnapshot` includes employment array.
- Validation: `employment_informations` array (min 1, max 2); root `hire_date` prohibited.
- Frontend: hybrid toggle, panel enable/disable, campus-linked select filtering, searchable selects.
- Sample employees in seeder updated to new structure.

---

## 15:00 — Daily code changelog tracking setup

**Why:** User requested automatic tracking of all code changes starting today.

**Files:**
- `.cursor/rules/code-changelog.mdc`
- `pulse/docs/changelog/2026-06-13.md`
- `pulse/docs/changelog/README.md`
- `pulse/scripts/changelog-today.sh`

**Summary:**
- Cursor rule: agent must append to daily changelog after every `pulse/` edit session.
- Baseline log for 2026-06-13 seeded from today's work.
- Helper script for git + filesystem daily snapshots.
