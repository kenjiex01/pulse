## 15:43 — Desktop build v1.0.7

**Why:** Ship the Human Resource Memo report (date range, company documents, offense frequency) in a paired macOS + Windows installer.

**Version:** `NATIVEPHP_APP_VERSION=1.0.7`

**Outputs:**
- `dist/People360-1.0.7-arm64.dmg`
- `dist/People360-1.0.7-setup.exe`

**GitHub:** https://github.com/kenjiex01/pulse/releases/tag/v1.0.7

## 15:33 — Bump desktop version to 1.0.7

**Why:** Ship the Human Resource Memo report (date range, company documents, offense frequency) in a new desktop installer.

**Files:**
- `.env.example`

**Summary:**
- Set `NATIVEPHP_APP_VERSION=1.0.7` for the paired macOS + Windows build.

## 15:18 — Memo report offense frequency column

**Why:** Users need to see which offense number (1st, 2nd, etc.) each memo send represents.

**Files:**
- `app/Services/Reports/MemoReportService.php`
- `app/Services/CompanyDocumentOffenseMemoService.php`
- `tests/Feature/PayrollReportsTest.php`
- `tests/Unit/CompanyDocumentOffenseMemoServiceTest.php`

**Summary:**
- Added **Offense Frequency** column to Memo report output (Excel/HTML/PDF).
- Uses stored `offense_frequency` submission value when available; otherwise derives from prior sends for offense memos.

## 15:15 — Human Resource Memo send log report

**Why:** Users need a downloadable list of memo sends by date range and selected company document templates.

**Files:**
- `app/Services/Reports/MemoReportService.php`
- `resources/views/payroll/reports/options/memo.blade.php`
- `config/payroll_reports.php`
- `database/seeders/ReportSeeder.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `resources/js/app.js`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- New **Memo** report under Human Resource with date range and company document multiselect (Select all).
- Excel/HTML/PDF output lists employee, memo template, sent date/time, and source (Company Documents or Timekeeping).

## 14:58 — Disciplinary action fallback for sparse penalty tiers

**Why:** Category D (e.g. I.1) only has a 1st-offense row; preview/send at 2nd offense left `disciplinary_action` blank while frequency still showed Second Offense.

**Files:**
- `app/Models/LuIcctOffensePenalty.php`
- `tests/Unit/IcctOffensePenaltyTest.php`
- `tests/Unit/CompanyDocumentOffenseMemoServiceTest.php`

**Summary:**
- `penaltyFor()` now uses the highest defined tier at or below the offense frequency (e.g. D + 2nd → Dismissal).

## 14:51 — Fix designer palette duplicate key dropping offense tags

**Why:** `CompanyDocumentDesignerController::show()` passed `palette` twice; PHP kept the second call without `$form`, so offense tags never reached the designer UI.

**Files:**
- `app/Http/Controllers/CompanyDocumentDesignerController.php`

**Summary:**
- Removed duplicate `'palette' => palette()` entry that overwrote the form-aware palette.

## 14:44 — Pin offense tags at the top of the TAGS panel

**Why:** Disciplinary action and Offense frequency were appended after Current datetime, so they sat below the fold and looked missing.

**Files:**
- `app/Support/CompanyDocumentMergeTagCatalog.php`
- `tests/Unit/CompanyDocumentMergeTagCatalogTest.php`

**Summary:**
- Offense tags now render first in the TAGS palette when Nature of Offense is set.

## 14:39 — Show offense tags when memo has offense block fields

**Why:** Verbal Reprimand and other ICCT memos include a Nature of offense form field but no template-level `icct_offense_id`, so TAGS panel stayed empty.

**Files:**
- `app/Models/CompanyDocumentForm.php`
- `app/Services/CompanyDocumentOffenseMemoService.php`
- `tests/Unit/CompanyDocumentOffenseMemoServiceTest.php`

**Summary:**
- Offense tags appear when template has `icct_offense_id` **or** a `nature_of_offense` field on the form (ICCT offense block).

## 14:21 — Offense memo merge tags (disciplinary action + frequency)

**Why:** Memo templates with Nature of Offense need TAGS for penalty text and computed offense frequency (e.g. 2nd send → Second Offense).

**Files:**
- `app/Services/CompanyDocumentOffenseMemoService.php`
- `app/Support/CompanyDocumentMergeTagCatalog.php`
- `app/Support/CompanyDocumentElementCatalog.php`
- `app/Services/CompanyDocumentMergeTagService.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Unit/CompanyDocumentOffenseMemoServiceTest.php`
- `tests/Unit/CompanyDocumentMergeTagCatalogTest.php`
- `tests/Unit/CompanyDocumentMergeTagServiceTest.php`

**Summary:**
- TAGS panel shows **Disciplinary action** and **Offense frequency** only when template has Nature of Offense set.
- Frequency = prior sends of same memo to same employee + 1; penalty from ICCT matrix (category × frequency).

## 14:14 — Dynamic offense categories and frequencies

**Why:** Users need to add penalty tier columns and offense frequency rows beyond fixed A–D / 1st–6th.

**Files:**
- `database/migrations/2026_09_19_160000_create_lu_icct_offense_frequencies_table.php`
- `app/Models/LuIcctOffenseFrequency.php`
- `database/seeders/IcctOffenseFrequencySeeder.php`
- `config/hr_lookups.php`
- `app/Support/HrLookup.php`
- `app/Policies/HrLookupPolicy.php`
- `app/Http/Controllers/HrLookupController.php`
- `app/Models/LuIcctOffenseCategory.php`
- `app/Models/LuIcctOffensePenalty.php`
- `resources/views/hr-lookups/index.blade.php`
- `resources/views/hr-lookups/offense-categories/_manage.blade.php`
- `resources/views/hr-lookups/offense-categories/_form.blade.php`
- `resources/views/hr-lookups/offense-categories/_frequency-form.blade.php`
- `resources/views/partials/sidebar.blade.php`
- `database/seeders/CompanyDocumentIcctOffensesSeeder.php`
- `tests/Feature/HrOffenseCategoryLookupTest.php`

**Summary:**
- New `lu_icct_offense_frequencies` table; matrix rows/columns read from DB.
- **Add Category** / **Add Frequency** buttons on Offense Categories page.
- Category code validation relaxed (max 16 chars); memo dropdowns sync after changes.

## 14:10 — Offense Categories: matrix-only UI with Edit on columns

**Why:** Penalties showed only on Edit modal, not Add; duplicate A–D list table below the matrix was confusing.

**Files:**
- `resources/views/hr-lookups/index.blade.php`
- `resources/views/hr-lookups/offense-categories/_manage.blade.php`
- `resources/views/hr-lookups/offense-categories/_form.blade.php`
- `app/Http/Controllers/HrLookupController.php`
- `config/hr_lookups.php`
- `tests/Feature/HrOffenseCategoryLookupTest.php`

**Summary:**
- Removed bottom CRUD table and Add button — fixed tiers A–D edited from matrix column headers.
- Edit modal keeps Table of Penalties fields; create path also syncs penalties if used later.

## 14:08 — ICCT penalty matrix as official seed data

**Why:** Replace generic "Category A" defaults with the ICCT Table of Penalties (image reference) for categories and frequency rows.

**Files:**
- `database/seeders/IcctOffenseCategorySeeder.php`
- `database/seeders/IcctOffensePenaltySeeder.php`
- `database/migrations/2026_09_19_140000_create_lu_icct_offense_categories_table.php`
- `tests/Feature/HrOffenseCategoryLookupTest.php`

**Summary:**
- Categories A–D: label = code, sort order 1 / 2 / 5 / 7.
- Penalty seeder loads full 13-cell matrix (1st–6th offense × A–D); prunes stray rows on re-run.

## 14:06 — Penalty fields on Offense Categories edit modal

**Why:** Users expected to set "A → First Offense → Verbal Reprimand" on the Offense Categories screen, not a separate menu.

**Files:**
- `app/Models/LuIcctOffenseCategory.php`
- `app/Http/Controllers/HrLookupController.php`
- `resources/views/hr-lookups/offense-categories/_form.blade.php`
- `resources/views/hr-lookups/offense-categories/_matrix.blade.php`
- `resources/views/hr-lookups/index.blade.php`
- `resources/views/hr-lookups/_results.blade.php`
- `config/hr_lookups.php`
- `tests/Feature/HrOffenseCategoryLookupTest.php`

**Summary:**
- Offense Categories index shows full A–D penalty matrix preview.
- Edit Category modal includes First–Sixth Offense penalty text fields.
- Saving a category syncs `lu_icct_offense_penalties` rows for that tier.

## 14:00 — Align offense categories with ICCT penalty matrix (A–D)

**Why:** Categories are penalty tiers A–D with a frequency matrix (1st–6th offense), not combo codes like B - C on the matrix itself.

**Files:**
- `database/migrations/2026_09_19_150000_align_offense_categories_with_penalty_matrix.php`
- `app/Models/LuIcctOffenseCategory.php`
- `app/Models/LuIcctOffense.php`
- `app/Models/LuIcctOffensePenalty.php`
- `config/hr_lookups.php`
- `database/seeders/IcctOffenseCategorySeeder.php`
- `database/seeders/IcctOffensePenaltySeeder.php`
- `resources/views/partials/sidebar-icon.blade.php`
- `tests/Feature/HrOffenseCategoryLookupTest.php`
- `tests/Feature/HrNatureOfOffenseLookupTest.php`

**Summary:**
- **Offense Categories** = A, B, C, D only (penalty tiers from the matrix image).
- New **Category Penalties** HR lookup to maintain matrix rows (Frequency × Category → Penalty text).
- Nature of Offense still allows combo severity codes (B - C, B - D, C - D) on individual offenses; those are not matrix categories.
- Penalty rows linked to category FK; 13 seeded matrix cells unchanged in meaning.

## 13:54 — Offense Categories HR lookup maintenance

**Why:** Category values were hardcoded; admins need CRUD maintenance separate from Nature of Offense rows.

**Files:**
- `database/migrations/2026_09_19_140000_create_lu_icct_offense_categories_table.php`
- `app/Models/LuIcctOffenseCategory.php`
- `app/Models/LuIcctOffense.php`
- `config/hr_lookups.php`
- `app/Support/HrLookup.php`
- `app/Http/Controllers/HrLookupController.php`
- `database/seeders/IcctOffenseCategorySeeder.php`
- `database/seeders/IcctOffenseSeeder.php`
- `resources/views/partials/sidebar-icon.blade.php`
- `tests/Feature/HrOffenseCategoryLookupTest.php`
- `tests/Feature/HrNatureOfOffenseLookupTest.php`

**Summary:**
- New **HR → Offense Categories** lookup (`lu_icct_offense_categories`) with Add/Edit/Activate/Delete.
- Nature of Offense form picks Category from maintained list; `category` text kept in sync for memos/reports.
- Categories assigned to offenses or penalties cannot be deleted.

## 13:53 — Memo send confirmation modal

**Why:** Send actions should require explicit confirmation before emailing memos.

**Files:**
- `resources/views/timekeeping/memo/_confirm-send-modal.blade.php`
- `resources/views/timekeeping/memo/index.blade.php`
- `resources/views/timekeeping/memo/_results.blade.php`
- `resources/views/timekeeping/memo/_details-content.blade.php`
- `resources/js/timekeeping-memo.js`

**Summary:**
- Added Confirm Send modal for row Send, Send Selected batch, and View details Send Memo.
- Cancel closes the modal; Send proceeds with the original POST.

## 13:38 — Fix cleared Field label reappearing after save/refresh

**Why:** Clearing Field label in the designer showed "Saved." but the label came back on refresh.

**Files:**
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- `normalizeElementLabel()` no longer replaces an intentionally empty string with the element type default.
- Default label is applied only when `label` is omitted/null (new elements without a label).
- Added regression test for empty long-text field labels.

## 12:41 — Remove CHOICE tab from Company Documents designer palette

**Why:** CHOICE elements (Dropdown, Radio, Checkbox, Yes/No) are not needed in the form builder palette; seeded templates still supply dropdowns where required.

**Files:**
- `app/Support/CompanyDocumentElementCatalog.php`

**Summary:**
- Removed `CHOICE` category from the Form Elements palette (BASIC / TEXT / DATE / TAGS / ADVANCED only).
- Kept legacy `defaultLabel()` mapping so existing forms with choice-type fields still save and display correctly.
