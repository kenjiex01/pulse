## 10:05 — Memo send timeout fix (PDF + email)

**Why:** Memo send hit PHP 30s limit in Guzzle (SES) — PDF HTML contained huge base64 images and email ran inside a DB transaction.

**Files:**
- `app/Support/CompanyDocumentMemoDompdfPreparer.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `app/Services/TimekeepingMemoSendService.php`
- `app/Http/Controllers/TimekeepingMemoController.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`

**Summary:**
- Materialize memo images to temp files before PDF HTML (no multi‑100KB data URLs in Dompdf)
- Optionally downscale wide images with GD; PDF render ~0.1s vs prior multi‑second path
- Send email after DB commit; `set_time_limit(120)` on memo send / `300` on batch send

## 11:15 — Memo preview shows exact email PDF (WYSIWYG)

**Why:** HTML canvas preview and Dompdf output could never match pixel-perfect; users need to see the real attachment before send.

**Files:**
- `app/Services/TimekeepingMemoPreviewService.php`
- `app/Http/Controllers/TimekeepingMemoController.php`
- `app/Support/TimekeepingMemo.php`
- `routes/web.php`
- `resources/views/timekeeping/memo/_preview-content.blade.php`
- `tests/Feature/TimekeepingMemoTest.php`

**Summary:**
- Preview modal embeds `preview-pdf` iframe — same `renderPdf()` bytes as email attachment
- Added route `timekeeping.memo.preview-pdf`; PHPUnit asserts preview PDF size/pages match send pipeline
