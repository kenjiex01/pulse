## 16:30 — Master file upload: match by employee number, partial update

**Why:** Existing employees should update from master file uploads when the employee number matches; only filled Excel columns should change (blanks must not wipe data).

**Files:**
- `app/Services/EmployeeUploadRowMapper.php`
- `tests/Unit/EmployeeUploadRowMapperTest.php`
- `resources/views/employees/_upload-form.blade.php`

**Summary:**
- Match existing rows by **employee number** (not employee number + email).
- Allow email change on update when the new email is not used by another employee; blank email on update leaves the current email unchanged.
- Auto-apply partial-row validation for updates (same as disable-required for filled columns only).
- Updated upload form help text and unit tests.

## 16:28 — Show master file upload changes in employee history

**Why:** Upload updates must appear in Employee History details and the HR Historical Data report.

**Files:**
- `app/Services/EmployeeHistoryService.php`
- `app/Services/EmployeeUploadService.php`
- `app/Services/Reports/HistoricalDataReportService.php`
- `resources/views/employees/partials/_history-results.blade.php`
- `tests/Unit/EmployeeHistoryServiceTest.php`

**Summary:**
- Include legacy `edit` upload logs in employee history queries; new uploads log as `update`.
- Historical Data report treats **Updated** filter as update + legacy upload entries.
- History tab shows upload remarks such as `Updated employee via upload: …` with field-level old/new values.

## 09:45 — Desktop: keep navigation in one window (Windows)

**Why:** Electron/NativePHP opens a second app window for `target="_blank"`, Ctrl/Cmd+click, and some report/upload links.

**Files:**
- `app/Providers/NativeAppServiceProvider.php`
- `resources/js/app.js`

**Summary:**
- Main window calls `suppressNewWindows()` so Electron does not spawn extra BrowserWindows.
- Desktop JS reroutes same-origin `_blank`/modifier-click links to the current window; external URLs open in the default browser; downloads use a hidden iframe.

## 16:30 — Desktop build v1.0.4

**Why:** Release master file upload fixes, upload history visibility, memo/NTE, and ICCT offense updates.

**Version:** `NATIVEPHP_APP_VERSION=1.0.4`

**Outputs:**
- `dist/People360-1.0.4-arm64.dmg`
- `dist/People360-1.0.4-setup.exe`
