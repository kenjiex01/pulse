## 16:45 — Payslip email: AWS SES SMTP (ap-southeast-2) env template

**Why:** ICCT SES SMTP uses `email-smtp.ap-southeast-2.amazonaws.com` and `noreply@skolaris.icct.edu.ph` as sender; document correct `.env` shape.

**Files:**
- `.env.example`

**Summary:**
- Default mail example: `MAIL_MAILER=smtp`, host `email-smtp.ap-southeast-2.amazonaws.com:587`, from `noreply@skolaris.icct.edu.ph`
- `AWS_DEFAULT_REGION=ap-southeast-2` aligned with SMTP endpoint
- Local `.env` updated (not committed); SMTP smoke test still returns 535 until IT provides valid SES SMTP password

## 16:50 — Payslip email: switch to SES API (not SMTP)

**Why:** User requested SES API mailer instead of SMTP/IAM SMTP login; ICCT SES is in ap-southeast-2.

**Files:**
- `.env.example`

**Summary:**
- Default `MAIL_MAILER=ses`; SMTP host/username/password cleared in example
- `AWS_DEFAULT_REGION=ap-southeast-2`, from `noreply@skolaris.icct.edu.ph`
- Local `.env` switched to SES API; smoke test **OK** via Laravel Mail
