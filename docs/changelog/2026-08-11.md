# 2026-08-11

## 10:41 — Master File upload: up to 5 campus assignments

**Why:** Allow optional additional campus/biometric slots on employee master-file upload (only primary required).

**Files:**
- `config/employee_upload.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `resources/views/employees/_upload-form.blade.php`
- `resources/templates/employee_upload_template.xlsx`

**Summary:**
- Added Campus Code/Biometric/College/Department/Program columns for slots **3–5** (slot 1 required; 2–5 optional)
- Upload mapper validates optional slots only when filled; still enforces unique campus per employee
- Regenerated master-file Excel template; help text notes one campus required, up to 5 total

## 10:53 — Remove JSON columns from master-file upload

**Why:** Drop unused JSON collection columns from employee master data upload template and import.

**Files:**
- `config/employee_upload.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `resources/templates/employee_upload_template.xlsx`

**Summary:**
- Removed Family Members / Employment History / Exams / Seminars / Awards / References JSON columns
- Removed `json_collection_map` and related validate/import logic
- Regenerated master-file Excel template

## 11:03 — Employee Credentials tab (after Access)

**Why:** Let HR attach credential files (description + attachment) per employee from the employee form.

**Files:**
- `database/migrations/2026_08_11_030147_create_tbl_employee_credentials_table.php`
- `app/Models/EmployeeCredential.php`
- `app/Models/Employee.php`
- `app/Http/Controllers/EmployeeCredentialController.php`
- `routes/web.php`
- `resources/views/employees/_tabs-nav.blade.php`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/partials/_credentials-tab.blade.php`
- `resources/views/employees/partials/_credentials-modals.blade.php`
- `resources/views/employees/partials/_credential-add-form.blade.php`

**Summary:**
- Added **Credentials** tab after Access with file list table, Add modal (Description + file), download, and soft delete
- On create: tab visible with “save employee first” message; uploads work after employee is saved
- Soft-deletes cascade when employee is soft-deleted; `sys_logs` on upload/download/delete

## 11:04 — Fix credential SysLog named parameters

**Why:** `SysLogService::record` uses `newValues` / `oldValues`, not `newData` / `oldData`.

**Files:**
- `app/Http/Controllers/EmployeeCredentialController.php`

**Summary:**
- Fixed create/delete audit calls so credential upload no longer 500s

## 11:17 — Credential file preview modal

**Why:** View uploaded credentials in-app via an eye icon without leaving the employee form.

**Files:**
- `composer.json` / `composer.lock` (`phpoffice/phpword`)
- `app/Services/EmployeeCredentialPreviewService.php`
- `app/Http/Controllers/EmployeeCredentialController.php`
- `routes/web.php`
- `resources/views/employees/partials/_credentials-tab.blade.php`
- `resources/views/employees/partials/_credentials-modals.blade.php`
- `resources/views/employees/partials/_credential-preview-body.blade.php`
- `resources/js/app.js`

**Summary:**
- Actions column: Preview (eye) opens stacked modal with iframe preview
- Images/PDF stream inline; Excel/CSV via PhpSpreadsheet HTML; Word via PhpWord HTML; text in `<pre>`; audio/video players; other types show download fallback
- `sys_logs` read entry on preview

## 11:26 — Desktop-safe credential preview

**Why:** Electron/NativePHP blanks on some binary iframe navigations; preview must work in the desktop shell.

**Files:**
- `app/Services/EmployeeCredentialPreviewService.php`
- `resources/views/employees/partials/_credentials-tab.blade.php`
- `resources/js/app.js`

**Summary:**
- Preview endpoint always returns HTML wrappers for images/PDFs (content URL nested)
- On `data-pulse-desktop=1`, image/PDF load via fetch + blob `srcdoc` so the iframe never hits raw binary navigation
- Office/text HTML previews unchanged (same path as browser)

## 11:44 — Fix legacy .doc Word preview

**Why:** PhpWord defaulted to the `.docx` (zip) reader for `.doc` files, so preview fell back to download.

**Files:**
- `app/Services/EmployeeCredentialPreviewService.php`

**Summary:**
- Pick Word reader by extension + file magic (MsDoc for OLE `.doc`, Word2007 for `.docx`, etc.)
- Try readers in order; OLE text scrape as last resort
- Verified `file-sample_100kB.doc` now returns HTML preview

## 11:58 — LibreOffice PDF preview for Word/Excel

**Why:** Pure PHP (PhpWord) cannot render charts/OLE objects inside legacy `.doc`; LibreOffice → PDF gives faithful preview when installed.

**Files:**
- `app/Services/LibreOfficeDocumentConverter.php`
- `app/Services/EmployeeCredentialPreviewService.php`
- `config/document_preview.php`
- `.env.example` (`LIBREOFFICE_PATH`, `LIBREOFFICE_TIMEOUT`)

**Summary:**
- Prefer LibreOffice conversion to cached PDF for Word/Excel credential previews (charts/images visible)
- Fall back to PhpWord/PhpSpreadsheet HTML when LibreOffice is not installed
- Desktop treats convertible Office files as `pdf` kind so blob embed works
- Improved PhpWord layout + optional embedded JPEG/PNG scrape as secondary fallback

## 12:01 — Desktop LibreOffice preview + optional runtime download

**Why:** Chart-accurate Office preview must work in NativePHP; clients without LibreOffice can download a portable runtime once into app storage.

**Files:**
- `app/Services/LibreOfficeRuntimeInstaller.php`
- `app/Services/LibreOfficeDocumentConverter.php`
- `app/Http/Controllers/DocumentPreviewEngineController.php`
- `config/document_preview.php`
- `routes/web.php`
- `resources/views/partials/document-preview-engine-modal.blade.php`
- `resources/views/layouts/app.blade.php`
- `resources/js/app.js`
- `.env.example`

**Summary:**
- Desktop/browser: use system LibreOffice if present (your Mac brew install works with `native:serve`)
- If missing: prompt to download LibreOffice into `storage/app/libreoffice-runtime` (~290–360 MB once)
- Preview uses converted PDF embed; PhpWord remains fallback when engine unavailable

## 12:35 — Desktop build 0.1.50

**Why:** Ship Credentials tab, file preview, and LibreOffice-based Office preview to desktop clients.

**Files:**
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.50`)
- `pulse/dist/Pulse-0.1.50-arm64.dmg`
- `pulse/dist/Pulse-0.1.50-setup.exe`

**Summary:**
- Built paired macOS + Windows installers for **0.1.50**
- Paths:
  - `/Users/kentordillos/Documents/ISKOLARIS/pulse/dist/Pulse-0.1.50-arm64.dmg` (~354 MB)
  - `/Users/kentordillos/Documents/ISKOLARIS/pulse/dist/Pulse-0.1.50-setup.exe` (~229 MB)
- Uploaded to `s3://skolaris-payroll-backups-prod/payroll_installer/` + `latest.json` → **0.1.50**

## 12:56 — Harden desktop installer S3 upload

**Why:** Large multipart uploads were timing out mid-transfer.

**Files:**
- `config/filesystems.php`
- `app/Services/DesktopInstallerUpdateService.php`

**Summary:**
- Extended `backup-s3` HTTP timeout to 900s
- Retried each installer upload up to 3 times on failure

## 13:32 — Load screen on desktop installer download

**Why:** "Download update" used `target="_blank"`, so the full-screen loader never appeared when starting a large installer download.

**Files:**
- `app/Http/Controllers/DesktopInstallerUpdateController.php`
- `resources/views/partials/desktop-installer-update-modal.blade.php`
- `resources/js/app.js`

**Summary:**
- Clicking **Download update** shows the Pulse full-screen loader (“Preparing…” → “Starting download…”)
- Fetches a JSON pre-signed URL, starts the download via hidden iframe (avoids navigating the desktop app away)
- Status text under the button confirms when the download has started

## 15:15 — Refresh Pulse Progress & Targets report

**Why:** Capture Aug 10–11 work (Credentials, master upload, desktop 0.1.50) and week targets through Aug 16.

**Files:**
- `docs/Pulse-Progress-and-Target-Report-2026-08-03-to-08-16.md`
- `docs/Pulse-Progress-and-Target-Report-2026-08-03-to-08-16.docx`

**Summary:**
- Progress period extended through **Aug 11**; added Aug 10–11 completions
- Targets (Aug 11–16): reports testing/feedback + Credentials UAT + desktop 0.1.50 adoption
- Regenerated companion Word document

## 16:48 — Add Shift Code (per day) on payroll batch detail

**Why:** Allow a per-employee calendar-date shift override from batch employee detail so late/UT/hours use that shift after the next Process/Reprocess.

**Files:**
- `database/migrations/2026_08_11_084109_create_tbl_employee_shift_overrides_table.php`
- `app/Models/EmployeeShiftOverride.php`
- `app/Services/EmployeeShiftResolver.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollAttendanceHoursWorkedService.php`
- `app/Services/PayrollOvertimeService.php`
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Support/PayrollTransactionModule.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-shift-code-modal.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-shift-code-form.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- New `tbl_employee_shift_overrides` (soft deletes); scope = employee + work date
- `EmployeeShiftResolver` prioritizes day override over timekeeping setup shift
- Attendance / time-logs / OT paths resolve shift per session date
- UI: **Add Shift Code** beside **Add Income**; modal save (upsert) + period list + soft delete; no auto-reprocess
- Flash reminds user to Process/Reprocess; `sys_logs` on create/update/delete

## 17:02 — Upload Adjustments: Shift Code tab

**Why:** Let users bulk-upload per-day shift overrides (same data as Add Shift Code) from Upload Adjustments.

**Files:**
- `config/payroll_transaction.php`
- `database/migrations/2026_08_11_085200_create_raw_payroll_shift_codes_table.php`
- `app/Models/RawPayrollShiftCode.php`
- `app/Models/RawPayrollTransaction.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`

**Summary:**
- New **Shift Code** upload tab after Hours Worked (`?upload=shift-codes`)
- CSV: `emp_num`, `full_name`, `work_date`, `shift_code` (date must be within selected pay period)
- Commit writes upload audit rows + upserts `tbl_employee_shift_overrides`
- Success flash: Process/Reprocess the batch to apply to pay (no auto-reprocess)

## 17:03 — Sys logs for every Shift Code write

**Why:** Audit trail for modal CRUD and upload commit (batch + each row).

**Files:**
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`

**Summary:**
- Modal Add/Update/Delete shift override already logged with `newValues`/`oldValues`
- Upload commit: batch log on `raw_payroll_transactions` plus per-row logs on `raw_payroll_shift_codes` and `tbl_employee_shift_overrides` (create/update)
- List/view/purge of Shift Code upload batches already covered by existing upload `sys_logs`

## 17:59 — Manual Add Overtime on payroll batch detail

**Why:** Let users file OT by date + start/end, validated against excess hours and time logs; pay from approved ∩ excess on Process/Reprocess.

**Files:**
- `database/migrations/2026_08_11_090922_create_tbl_employee_overtime_approvals_table.php`
- `app/Models/EmployeeOvertimeApproval.php`
- `app/Services/EmployeeOvertimeApprovalService.php`
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-overtime-modal.blade.php`
- `resources/views/payroll/transaction/_batch-employee-add-overtime-form.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- **Add Overtime** beside Shift Code / Income on batch employee detail
- Validates: logs exist; OT inside punch window; employee has excess hours; OT overlaps excess; policy min/OT treatment
- Soft-delete list + `sys_logs` on create/delete
- Punch-based payroll OT now bills from approved OT ∩ excess (not auto total excess)
- Success flash: Process/Reprocess to apply

## 20:53 — Delete old desktop installers from S3 on upload

**Why:** Each build should leave only the newly published version in S3 (no leftover old EXEs/DMGs).

**Files:**
- `app/Services/DesktopInstallerUpdateService.php`
- `app/Console/Commands/UploadDesktopInstallersCommand.php`
- `config/desktop_updater.php`
- `scripts/build-desktop.sh`

**Summary:**
- After `desktop:upload-installers`, prune **all** other objects under `payroll_installer/` (not only Pulse-*.exe/dmg)
- Keep only the just-uploaded EXE/DMG set + `latest.json`
- Fail the upload/build if any delete fails (IAM must allow `s3:DeleteObject`)

## 21:01 — Desktop build 0.1.51 (macOS + Windows)

**Why:** Ship Shift Code / Overtime payroll features and S3 installer prune in a paired release.

**Files:**
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.51`)
- `dist/Pulse-0.1.51-arm64.dmg`
- `dist/Pulse-0.1.51-setup.exe`

**Summary:**
- Built macOS DMG + Windows EXE for **0.1.51**
- Uploaded to `s3://skolaris-payroll-backups-prod/payroll_installer/`
- Deleted older S3 installers (0.1.46–0.1.50 + legacy unversioned names); prefix keeps only 0.1.51
