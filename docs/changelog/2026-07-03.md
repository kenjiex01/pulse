## 00:00 — Time Logs sample files for Staff employee

**Why:** User needs a ready-to-upload Time Logs file and matching Format Type for a Staff employee.

**Files:**
- `database/seeders/TimeCaptureFormatSeeder.php`
- `docs/samples/time-logs-staff-sample.txt`
- `docs/samples/time-logs-biometric-staff-sample.txt`

**Summary:**
- Added **STAFF** time capture format (employee number + date + time in/out per row) alongside existing **BIOMETRIC**.
- Sample upload for Staff employee **Maria Santos** (`2026-00001`) — Jun 2–6, 2026, 8:00 AM–5:00 PM (Jun 3 late 5 min, Jun 4 undertime 4:30 PM out).
- Seeder restores soft-deleted formats by `device_name` when re-run.

## 00:05 — Fix Time Logs commit for STAFF format (actual_date)

**Why:** Upload commit failed with `Undefined array key "workdate"` when using STAFF format (`actual_date` column).

**Files:**
- `app/Services/TimeLogsUploadService.php`

**Summary:**
- `resolveWorkDateTimes()` and biometric persist now resolve date from `actual_date`, `workdate`, or `date_out` — not hardcoded `workdate` only.

## 00:12 — Time Logs commit uses format field mappings only

**Why:** Upload commit must use the date/time columns configured on the selected Format Type — not hardcoded field name guesses.

**Files:**
- `app/Support/TimeCaptureFormat.php`
- `app/Services/TimeLogsUploadService.php`

**Summary:**
- Added `fieldRoles()` / `requireFieldRole()` — reads mapped columns from `tbl_timecapture_fields` per format.
- Parse, date range, and commit now use whatever the format defines (`actual_date`, `workdate`, `time_in`, `worktime`, etc.).

## 00:18 — Skip duplicate time logs on upload commit

**Why:** Re-uploading the same time logs must not create duplicate punch rows in the database.

**Files:**
- `app/Services/TimeLogsUploadService.php`
- `app/Http/Controllers/TimeLogsController.php`

**Summary:**
- Duplicate key: same `employee_id` + `dt_datetime` + `is_in` (within file or already in DB).
- Duplicates are skipped on commit; success message reports inserted vs skipped counts.
- If every row is a duplicate, commit aborts with an error and rolls back the empty batch.

## 00:22 — Attendance View: separate Date and Time columns

**Why:** Employee Profile Attendance View should not combine date and time in one column.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`

**Summary:**
- Split **Date / Time** into **Date** (`M j, Y`) and **Time** (`g:i A`) columns.
