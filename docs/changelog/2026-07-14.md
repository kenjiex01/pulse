## 09:56 — Employee bulk upload button

**Why:** Allow importing employee master data from CSV beside the existing Add Employee action.

**Files:**
- `config/employee_upload.php`
- `app/Services/EmployeeUploadService.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `app/Http/Controllers/EmployeeController.php`
- `routes/web.php`
- `resources/views/partials/page-header.blade.php`
- `resources/views/employees/index.blade.php`
- `resources/views/employees/_upload-form.blade.php`
- `resources/views/employees/_upload-preview.blade.php`
- `docs/samples/employee-upload-sample.csv`

**Summary:**
- Added **Upload** secondary button next to **Add Employee** on the employee list page.
- CSV template download, parse preview, and commit flow create employees with employment and campus assignment (no salary).
- Page header partial now supports an optional secondary modal action button.

## 10:05 — Employee upload full-field parity

**Why:** CSV upload must include all manual Add Employee fields, not just basic master data.

**Files:**
- `config/employee_upload.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Services/EmployeeUploadService.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `resources/views/employees/_upload-form.blade.php`
- `resources/views/employees/_upload-preview.blade.php`
- `docs/samples/employee-upload-sample.csv`

**Summary:**
- Expanded template to ~110 columns: personal, campus (primary + secondary), employment (hybrid support), salary + incomes/deductions, contact, address, access, extended profile, and JSON collections.
- Upload commit now syncs employment, campus assignments, and salary via the same services as manual creation.
- Required fields aligned with wizard: salary setup and system role included.

## 12:06 — Excel date auto-format fix

**Why:** Excel was rewriting birth dates (e.g. `2000-01-01` → `1/1/2000`) when users moved between cells.

**Files:**
- `config/employee_upload.php`
- `app/Services/EmployeeUploadService.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `resources/views/employees/_upload-form.blade.php`

**Summary:**
- Template download is now **.xlsx** with all columns set to **Text** format so Excel does not auto-format dates or IDs.
- Upload accepts `.xlsx` and parses Excel date serials plus `M/D/YYYY` and `YYYY-MM-DD` formats.
- Data entry starts at row 4 (rows 1–2 are headers; row 3 is sample).

## 12:39 — Desktop build v0.1.7 (macOS + Windows)

**Why:** Ship employee bulk upload, Excel template fix, and all recent changes in new installers.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.7`)
- `dist/Pulse-0.1.7-arm64.dmg` (generated)
- `dist/Pulse-0.1.7-arm64.zip` (generated)
- `dist/Pulse-0.1.7-setup.exe` (generated)

**Summary:**
- Bumped to **0.1.7** and rebuilt macOS (arm64 DMG/ZIP) + Windows (x64 EXE).
- Includes employee upload (full fields), Excel `.xlsx` template, and desktop bootstrap fixes from prior releases.
- Existing client databases are preserved on upgrade; bootstrap runs once per version.

## 13:10 — Sumulong Campus DTR report upload parser

**Why:** Sumulong DTR exports use punch-log layout (Employee + Date/Time/In-Out), not the flat 4-column template.

**Files:**
- `app/Services/TimeLogsDtr/SumulongDtrReportParser.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `app/Support/TimeLogsDtr.php`
- `config/time_logs_dtr.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `docs/samples/sumulong-dtr-sample.xlsx`

**Summary:**
- Sumulong (`SU`) now parses the real **DTR Report** `.xlsx` (employee blocks + punch rows).
- Resolves biometric ID from `Employee: NAME (ID)` and pairs first In / last Out per day.
- Accepts `DD/MM/YYYY` dates and Excel time serials; matches sample file `JUNE 11-26,2026 SUMULONG SAMPLE DTR.xlsx`.

## 13:30 — Sumulong DTR employee lookup by ID number and name

**Why:** Sumulong DTR uses `Employee: NAME (237)` — lookup should use the number in parentheses and strip parens, not require biometric ID only.

**Files:**
- `app/Services/TimeLogsDtr/SumulongDtrEmployeeResolver.php`
- `app/Services/TimeLogsDtr/SumulongDtrReportParser.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`

**Summary:**
- Parser extracts name + ID from parentheses separately.
- Resolver tries employee number match, biometric ID, then name on Sumulong campus.
- Error messages now say "Employee not found" instead of "Invalid Biometric ID".

## 13:45 — Sumulong DTR: store every In/Out punch individually

**Why:** June 18, 2026 had In 06:53 → Out 16:01 → In 16:02; collapse logic kept only first In + last Out and dropped the 16:02 In.

**Files:**
- `app/Services/TimeLogsDtr/SumulongDtrReportParser.php`
- `app/Services/TimeLogsDtrUploadService.php`

**Summary:**
- Parser now emits one row per punch (`punch_time`, `is_in`) instead of collapsing to daily pairs.
- Sumulong commit path persists each punch to `raw_timekeeping_inandout` individually.
- June 18 sample for employee 237 now captures all 3 punches (06:53 In, 16:01 Out, 16:02 In).
