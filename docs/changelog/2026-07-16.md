## 22:20 — Fix Employee Profile setup save (405 PUT method)

**Why:** Saving timekeeping setup returned 405 — nested attendance edit forms inside the setup form leaked `_method=PUT` into the POST submit.

**Files:**
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Lazy-loaded Attendance/Employee Load panels moved outside the setup `<form>` so nested edit forms cannot override the method.
- Setup submit handler strips stray `_method` fields before POST.

## 22:15 — Flexi-time shift code (actual hours rendered)

**Why:** User requested flexi shift where employees may arrive anytime, are paid actual rendered hours only, have no late/undertime, and earn OT beyond expected hours per day (e.g. 8).

**Files:**
- `database/migrations/2026_07_16_220000_add_flexi_time_fields_to_shift_codes.php`
- `app/Models/ShiftCode.php`
- `app/Support/ShiftCode.php`
- `app/Services/FlexiShiftPayrollService.php`
- `app/Services/PayrollAttendanceHoursWorkedService.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/timekeeping/shift-codes/_form.blade.php`
- `resources/views/timekeeping/shift-codes/_results.blade.php`
- `resources/js/app.js`
- `database/seeders/FlexiShiftCodeSeeder.php`
- `database/seeders/DesktopReferenceDataSeeder.php`
- `tests/Unit/FlexiShiftPayrollServiceTest.php`
- `docs/user-guide/index.html`

**Summary:**
- Shift Code form: **Flexi-time Shift** checkbox + **Expected Hours Per Day** (default 8).
- Payroll: actual IN→OUT hours minus break punches; basic = min(actual, expected); OT = excess; no LTDE/UTDE.
- Seeded `FLXI` shift code for desktop/reference installs.

## 21:53 — Remove Overdue and Withheld from Compliance Status

**Why:** User requested only Pending and Compliant in the employee Compliance Status dropdown.

**Files:**
- `app/Models/Employee.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `resources/views/employees/partials/_employment-information.blade.php`
- `resources/views/employees/_filters.blade.php`
- `config/employee_upload.php`
- `docs/user-guide/index.html`

**Summary:**
- Added `Employee::selectableComplianceStatuses()` — Pending and Compliant only.
- Form, list filter, validation, and employee upload now use the reduced set.
- Legacy overdue/withheld values still display in badges if already stored.

## 21:50 — Fix new employee wizard not saving campus assignments

**Why:** Assignment & Organization data (biometric ID, college, department, program) was lost on wizard save — campus assignments were validated but never merged into the wizard session.

**Files:**
- `app/Http/Requests/Employee/WizardDetailsRequest.php`
- `resources/js/app.js`
- `tests/Feature/EmployeeWizardCampusAssignmentTest.php`

**Summary:**
- `WizardDetailsRequest::validated()` now includes `campus_assignments` alongside employment and salary rows so step 2 data persists to final `store()`.
- On form submit, re-enable disabled campus-assignment selects so college/department/program values POST correctly.
- Feature tests confirm session merge and `tbl_employee_campus_assignments` persistence (e.g. biometric ID 8376 on Cainta).


**Why:** User provided Cainta staff `.xls` sample (Timesheet Report with per-employee In/Out columns); old flat 4-column layout did not match.

**Files:**
- `app/Services/TimeLogsDtr/CaintaTimesheetReportParser.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `app/Support/TimeLogsDtr.php`
- `config/time_logs_dtr.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `docs/samples/cainta-staff-dtr-june-2026-sample.xls`
- `docs/user-guide/index.html`
- `tests/Unit/CaintaTimesheetReportParserTest.php`

**Summary:**
- Parses `Employee: NAME (biometric ID)` blocks with Date + In 1/Out 1 … In 5 columns into individual punch rows.
- Resolves employees via campus biometric ID; persists like San Mateo/Sumulong per-punch upload.

## 21:15 — Paired desktop builds rule + Windows v0.1.11

**Why:** User requires macOS and Windows installers together on every release; v0.1.11 was missing Windows.

**Files:**
- `.cursor/rules/desktop-build.mdc`
- `scripts/build-desktop.sh`
- `dist/Pulse-0.1.11-setup.exe`

**Summary:**
- Rule updated: default `./scripts/build-desktop.sh` builds **both** mac + win; single-platform only on explicit request.
- Built `Pulse-0.1.11-setup.exe` (10 PM backup test schedule, matches Mac build).

## 21:12 — Desktop build v0.1.11 (10 PM backup test schedule)

**Why:** Test build for daily S3 cloud backup with schedule temporarily set to 10 PM; dev `.env` restored to 10 AM after build.

**Files:**
- `.env` (local — schedule 22 during build, reverted to 10)
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.11`)

**Summary:**
- Built `Pulse-0.1.11-arm64.dmg` with `DB_BACKUP_CLOUD_SCHEDULE_HOUR=22` baked in for tonight's backup test.
- Local dev schedule restored to 10 AM.

## 21:06 — Daily desktop database backup to S3 (gzip)

**Why:** User requested automatic full-database SQL export, gzip, and upload to S3 at 10 AM with catch-up upload on desktop app open after 10 AM if missed.

**Files:**
- `app/Services/DesktopCloudBackupService.php`
- `app/Services/DatabaseBackupService.php`
- `app/Console/Commands/UploadDatabaseBackupCommand.php`
- `app/Http/Middleware/EnsureDesktopCloudBackup.php`
- `app/Providers/AppServiceProvider.php`
- `bootstrap/app.php`
- `config/backup.php`
- `config/filesystems.php`
- `.env.example`
- `tests/Unit/DesktopCloudBackupServiceTest.php`
- `tests/Unit/DatabaseBackupServiceGzipTest.php`

**Summary:**
- Dumps DB to `pulse-db-yyyy-mm-dd-hh-mm-ss.sql`, gzips, uploads to `payroll-backups/` on S3 via `DB_BACKUP_S3_*` env keys (survives NativePHP packaging).
- Runs after 10 AM Asia/Manila once per day; retries on desktop open and while app is in use via middleware; `php artisan backup:upload-cloud` for manual runs.

## 20:52 — Fix Attendance View stuck on "Loading…" after save

**Why:** After saving an attendance log edit, redirect reopened Employee Profile on Attendance tab but the lazy-loaded table never fetched — `loadEmployeeProfileLazyPanel` was called before it was defined in `app.js`.

**Files:**
- `resources/js/app.js`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`

**Summary:**
- Moved lazy-panel loader before tab init; extracted `initEmployeeProfileFormTabs()` and re-run on modal auto-open / live-table refresh.
- Fixed `data-lazy-pending` to trigger only when the active tab needs AJAX load.

## 20:48 — Fix attendance edit modal closing Employee Profile

**Why:** Pencil click closed the parent Employee Profile modal instead of opening the edit modal on top.

**Files:**
- `resources/js/app.js`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/css/app.css` (if modal-overlay-nested was added there)

**Summary:**
- Modal stack fix: edit/original attendance modals open above Employee Profile without closing parent (`data-modal-stack`, `modal-overlay-nested`).

## 20:40 — Employee Profile: edit attendance logs with original-value tracking

**Why:** User requested per-row edit on Attendance View, edited marker, and ability to view original uploaded values.

**Files:**
- `database/migrations/2026_07_16_120000_add_edit_tracking_to_raw_timekeeping_inandout.php`
- `app/Models/RawTimekeepingInandout.php`
- `app/Services/EmployeeAttendanceLogService.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/views/timekeeping/employee-profile/_attendance-log-*.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `tests/Unit/EmployeeAttendanceLogServiceTest.php`

**Summary:**
- Edit modal per attendance row (date, time, Time In/Out); `employee-profile.update` permission required.
- First edit stores `original_dt_datetime` / `original_is_in`; row shows **Edited** badge + View Original modal.
- Changes write to `sys_logs`; re-process payroll to apply corrected punches.

## 19:15 — Fix break late rounding zeroing sub-hour penalties

**Why:** With 60-min scheduled break and 69-min actual break, 9-min break late became 0 LTDE because hour-level Round Off turned 9 min into 0 hours.

**Files:**
- `app/Support/TimekeepingPolicy.php`
- `app/Services/PayrollBreakService.php`
- `tests/Unit/PayrollBreakServiceTest.php`

**Summary:**
- `applyBreakTardinessRoundingMinutes()` — break late rounds in minutes (9 stays 9), not whole hours.
- OT still uses hour-based `applyRoundingMinutes()`.
- Apr 17 example: 69 − 60 = 9 min billable → LTDE 0.15 hr.

## 19:05 — Payroll: first IN / last OUT session; middle punches = break logs

**Why:** User clarified payroll should use first time-in and last time-out for work window; punches in between are break logs.

**Files:**
- `app/Services/PayrollBreakService.php`
- `app/Services/TimeLogsPayrollService.php`
- `tests/Unit/PayrollBreakServiceTest.php`
- `docs/user-guide/index.html`

**Summary:**
- `payrollSessionFromPunches()` — central first IN / last OUT per day (late, undertime, OT, ND).
- `breakSegmentsFromPunches()` — middle OUT→IN pairs between session boundaries = break duration.
- User guide §7.6 documents San Mateo Att.log import + payroll interpretation.

## 18:52 — Att.log: same IN/OUT tag when punches are within 5 minutes

**Why:** Rapid duplicate scans (e.g. 08:02 then 08:04) should stay the same direction, not alternate to OUT.

**Files:**
- `app/Services/TimeLogsDtr/SanMateoCardReportParser.php`
- `tests/Unit/SanMateoCardReportParserTest.php`

**Summary:**
- `assignInOutTags()` — first punch IN; flip only when gap to previous punch is more than 5 minutes.
- Example: 08:02 IN, 08:04 IN, 12:00 OUT, 12:02 OUT.

## 18:45 — Att.log report: import every punch (not just first in/last out)

**Why:** User requested all individual logs from Att.log report cells (e.g. 07:24, 11:51, 13:00, 16:33), not a single time-in/time-out pair per day.

**Files:**
- `app/Services/TimeLogsDtr/SanMateoCardReportParser.php`
- `app/Services/TimeLogsDtrUploadService.php`
- `tests/Unit/SanMateoCardReportParserTest.php`

**Summary:**
- Att.log report now emits one row per punch (`punch_time` + alternating `is_in` / `is_out`).
- Card Report rows still use `time_in`/`time_out`; commit expands them to individual punches.
- Dedup skips punches already captured from Card Report (e.g. 07:24 IN and 16:33 OUT on Apr 17); adds break punches 11:51 OUT and 13:00 IN.

## 18:38 — San Mateo Att.log report sheet + dedupe on DTR upload

**Why:** User requested parsing the `Att.log report` worksheet from StandardReport `.xls` while skipping rows already captured from Card Report tabs.

**Files:**
- `app/Services/TimeLogsDtr/SanMateoCardReportParser.php`
- `config/time_logs_dtr.php`
- `tests/Unit/SanMateoCardReportParserTest.php`

**Summary:**
- San Mateo DTR parser now reads `Att.log report` after all Card Report sheets (skips `Att.log report_orig`).
- Attendance grid rows map biometric ID + day columns to time-in/time-out pairs.
- Duplicate rows (same biometric, date, time in/out) from Card Report are not imported again from Att.log report.


**Why:** User could only see the first 20 valid rows in upload preview with no way to browse the rest.

**Files:**
- `resources/views/partials/client-pagination-controls.blade.php`
- `resources/views/timekeeping/time-logs/_upload-preview.blade.php`
- `resources/views/timekeeping/employee-load/_upload-preview.blade.php`
- `resources/views/payroll/transaction/_upload-preview.blade.php`
- `resources/views/employees/_upload-preview.blade.php`
- `resources/js/app.js`

**Summary:**
- All upload preview modals now render every valid row and use client-side pagination (10/20/50/100 per page, Previous/Next, numbered pages).
- Added **Show all** / **Show pages** toggle to view the full list in one scrollable table.
- Reused and extended existing `initClientPagination()` helper.

## 22:27 — Employee Profile search bar fix

**Why:** Search input typed values but the table never filtered — live-table JS never initialized.

**Files:**
- `resources/js/app.js`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `tests/Feature/TimekeepingEmployeeProfileSearchTest.php`

**Summary:**
- Moved `initEmployeeProfileSetupRoots()` above its first call — earlier `ReferenceError` stopped `initLiveTable()` from binding search listeners.
- Aligned Employee Profile pagination default to `LiveTable::perPage()` (10).
- Added AJAX search feature test (`bisco` → single BISCOCHO row).

## 23:11 — Fix desktop cloud backup S3 secret stripped on build

**Why:** 10 PM backup test did not run on packaged Windows desktop — `DB_BACKUP_S3_SECRET` was removed by NativePHP `*_SECRET` env cleanup.

**Files:**
- `config/nativephp.php`
- `app/Services/DesktopCloudBackupService.php`
- `app/Http/Controllers/DatabaseController.php`
- `resources/views/database/index.blade.php`
- `tests/Unit/NativePhpEnvCleanupTest.php`
- `.env.example`

**Summary:**
- Removed `*_SECRET` from `cleanup_env_keys` so `DB_BACKUP_S3_SECRET` survives desktop packaging.
- Database page now shows cloud backup schedule, status, and last S3 upload.
- Added unit test ensuring cleanup patterns do not strip backup credentials.

## 23:17 — Desktop build v0.1.12 (cloud backup secret fix)

**Why:** Rebuild desktop installers with `DB_BACKUP_S3_SECRET` preserved and 10 PM backup schedule for catch-up test.

**Files:**
- `.env` (build-time schedule 22, restored to 10 after build)
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.12`)

**Summary:**
- Built `Pulse-0.1.12-arm64.dmg` and `Pulse-0.1.12-setup.exe` with cloud backup fix + Database status page.
- Installers: `pulse/dist/Pulse-0.1.12-arm64.dmg`, `pulse/dist/Pulse-0.1.12-setup.exe`.

## 23:25 — Desktop build v0.1.13 (11:40 PM backup test)

**Why:** User requested 11:40 PM schedule for tonight's cloud backup test.

**Files:**
- `.env` (build-time schedule 23:40, restored to 10:00 after build)
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.13`)

**Summary:**
- Built `Pulse-0.1.13-setup.exe` with `DB_BACKUP_CLOUD_SCHEDULE_HOUR=23` and `SCHEDULE_MINUTE=40`.
- Installer: `pulse/dist/Pulse-0.1.13-setup.exe`.

## 23:35 — Clear today's cloud backup marker (admin)

**Why:** User needed to reset a stale "uploaded today" marker when S3 had no matching backup so tonight's test can retry.

**Files:**
- `app/Services/DesktopCloudBackupService.php`
- `app/Http/Controllers/DatabaseController.php`
- `resources/views/database/index.blade.php`
- `routes/web.php`
- `tests/Unit/DesktopCloudBackupServiceTest.php`

**Summary:**
- Added `clearTodayMarker()` and admin **Clear today's backup marker** button on Database page.
- Cleared local dev marker files under `storage/app/`.

## 23:38 — Desktop build v0.1.14 (clear marker + 11:40 PM backup)

**Why:** Fresh Windows build for tonight's backup test with admin marker reset and S3 secret fix.

**Files:**
- `.env` (build-time schedule 23:40, restored to 10:00 after build)
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.14`)

**Summary:**
- Built `Pulse-0.1.14-setup.exe` — cloud backup fix, **Clear today's backup marker** button, schedule 11:40 PM.
- Installer: `pulse/dist/Pulse-0.1.14-setup.exe`.

## 23:49 — Desktop build v0.1.15 (11:55 PM backup test)

**Why:** User requested another Windows test build with backup at 11:55 PM; dev schedule restored to 10 AM after build.

**Files:**
- `.env` (build-time schedule 23:55, restored to 10:00 after build)
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.15`)

**Summary:**
- Built `Pulse-0.1.15-setup.exe` with `DB_BACKUP_CLOUD_SCHEDULE_HOUR=23`, `SCHEDULE_MINUTE=55`.
- Local dev `.env` schedule back to **10:00 AM**.
- Installer: `pulse/dist/Pulse-0.1.15-setup.exe`.
