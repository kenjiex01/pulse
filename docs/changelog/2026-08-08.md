# 2026-08-08

## 13:43 — Payroll Alphalist report (PATHS schedules)

**Why:** Port PATHS Alphalist to Pulse for BIR year-end alphabetical listing under posted payroll reports.

**Files:**
- `app/Services/Reports/AlphalistReportService.php`
- `app/Support/AlphalistColumnMapper.php`
- `app/Services/Reports/BirFormEmployeeSelection.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `config/payroll_reports.php`
- `database/seeders/ReportSeeder.php`
- `resources/views/payroll/reports/options/alphalist.blade.php`

**Summary:**
- Added **Alphalist** report (Excel) with sheets Schedule 7.1 / 7.3 / 7.4 / 7.5 matching PATHS layout intent
- Options: applicable payroll year + optional day factor (7.5); employer TIN from BIR Forms Setup
- Includes all employees with posted payroll in the year; amounts via BIR 2316 income-type mapping
- Schedule split: MWE → 7.5, terminated → 7.1, mid-year hire → 7.4 (present employer only), else → 7.3

## 13:45 — Remove PATHS from Alphalist UI copy

**Why:** End users should not see the legacy system name in report options.

**Files:**
- `resources/views/payroll/reports/options/alphalist.blade.php`
- `resources/views/payroll/reports/options/bir-1601c.blade.php`
- `database/seeders/ReportSeeder.php`
- `app/Services/Reports/AlphalistReportService.php`

**Summary:**
- Replaced “PATHS schedules” with “BIR schedules” in Alphalist help text and DB description
- Removed “PATHS-style stamp” from BIR 1601-C options help

## 13:46 — Fix Alphalist empty Output dropdown

**Why:** Output select used `$fileType->name` but the column is `label`, so options rendered blank and generate failed validation.

**Files:**
- `resources/views/payroll/reports/options/alphalist.blade.php`

**Summary:**
- Use `$fileType->label` (Excel) like other payroll report option views

## 13:49 — Fix Alphalist Excel generate (PhpSpreadsheet 5)

**Why:** `setCellValueByColumnAndRow()` was removed in PhpSpreadsheet 5.x, so generate crashed.

**Files:**
- `app/Services/Reports/AlphalistReportService.php`
- `app/Services/Reports/Bir1601cReportService.php`

**Summary:**
- Replaced removed API with `setCellValue([$col, $row], $value)`

## 13:52 — Alphalist schedule multi-select

**Why:** Users need to choose which BIR schedules to include in the Excel (not always all sheets).

**Files:**
- `resources/views/payroll/reports/options/alphalist.blade.php`
- `config/payroll_reports.php`
- `app/Services/Reports/AlphalistReportService.php`

**Summary:**
- Added Schedule(s) multi-select: 7.1 / 7.3 / 7.4 / 7.5 (default all)
- Excel output includes only selected schedule sheets (no 7.2 — not in PATHS/BIR 2316 annex set we ported)

## 13:54 — Fix Alphalist Schedule select height (override form-input h-10)

**Why:** `.form-input` sets fixed `h-10`, so `min-h-*` alone did not enlarge the multi-select.

**Files:**
- `resources/views/payroll/reports/options/alphalist.blade.php`

**Summary:**
- Use `!h-auto min-h-56` + `size="4"` so all four schedules are visible

## 13:56 — Fix Schedule multi-select shrunk by form-input h-10

**Why:** `.form-input` forces `h-10`; Tailwind utilities that weren’t in the built CSS didn’t override it, so the box shrank further with `size="4"`.

**Files:**
- `resources/css/app.css`
- `resources/views/payroll/reports/options/alphalist.blade.php`
- `public/build/*` (npm run build)

**Summary:**
- Added `select.form-input[multiple] { h-auto min-h-48 }`
- Inline `min-height: 11rem` on Alphalist Schedule select for immediate effect
- Rebuilt frontend assets

## 13:58 — Tone down Alphalist Schedule select height

**Why:** Previous min-height (11rem) was too tall.

**Files:**
- `resources/views/payroll/reports/options/alphalist.blade.php`
- `resources/css/app.css`
- `public/build/*`

**Summary:**
- Reduced Schedule select to ~4 visible rows (`min-height: 6.75rem`)
