## 09:50 — Fix employee upload 500 on campus assignment sync

**Why:** Master file import failed with duplicate key `uq_employee_campus_assignments_employee_campus` when an employee already had multiple campuses.

**Files:**
- `app/Services/EmployeeCampusAssignmentSync.php`
- `app/Services/EmployeeUploadService.php`
- `tests/Unit/EmployeeCampusAssignmentSyncTest.php`

**Summary:**
- Match campus rows by `campus_id` instead of row index (avoids updating the wrong assignment into a duplicate campus).
- Upload updates merge campus rows only (`removeUnlisted: false`) so unlisted campuses are kept.
- When upload marks a primary campus, other assignments are demoted from primary.

## 09:55 — Desktop build v1.0.5

**Why:** Ship campus upload fix and single-window navigation for Windows desktop.

**Version:** `NATIVEPHP_APP_VERSION=1.0.5`

**Outputs:**
- `dist/People360-1.0.5-arm64.dmg`
- `dist/People360-1.0.5-setup.exe`
