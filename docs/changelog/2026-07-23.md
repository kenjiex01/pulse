
## 09:30 — Teaching Loads tab on Time Logs

**Why:** Allow pulling faculty teaching loads from Skolaris with per-employee last-sync tracking, date-range selection, and progress feedback.

**Files:**
- `config/time_logs.php`
- `config/skolaris.php`
- `.env.example`
- `database/migrations/2026_07_23_100000_create_teaching_load_tables.php`
- `app/Models/TeachingLoadSyncStatus.php`
- `app/Models/TeachingLoadSession.php`
- `app/Models/Employee.php`
- `app/Support/TimeLogs.php`
- `app/Services/SkolarisApiService.php`
- `app/Services/TeachingLoadPullService.php`
- `app/Http/Controllers/TimeLogsController.php`
- `routes/web.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_teaching-loads-results.blade.php`
- `resources/views/timekeeping/time-logs/_pull-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Added **Teaching Loads** tab with table showing last Skolaris pull, session count, and date range per faculty employee.
- Pull modal: date range + multi-select employees (faculty only, employee number must exist in Skolaris Pulse).
- Chunked pull with progress bar; stores sessions for the selected date range in `teaching_load_sessions`.
- Skolaris API: Pulse API key first (`SKOLARIS_PULSE_API_KEY`), JWT fallback for daily-loads and employee number list.

## 10:45 — Skolaris Pulse API key and desktop base URL

**Why:** Teaching Loads pull failed because the frontend bridge URL returned HTML and the API key path used endpoints/headers the Desktop App key cannot access.

**Files:**
- `pulse/.env`
- `pulse/.env.example`
- `pulse/config/skolaris.php`
- `pulse/app/Services/SkolarisApiService.php`
- `pulse/scripts/build-desktop.sh`

**Summary:**
- Set `SKOLARIS_PULSE_API_BASE_URL` to direct backend `https://api-skolaris.icct.edu.ph/api/v1/pulse-api/v1` (desktop cannot use `skolaris.icct.edu.ph/pulse/api` HTML bridge).
- Kept Desktop App API key in local `.env`; build script now verifies Pulse API env vars before packaging.
- Pulse API client: no `asJson()` on GET; employee list uses `timekeeping/employees` (key has timekeeping permissions, not employee sync).
- Increased Skolaris HTTP timeout to 120s; employee list cached for 6 hours.

## 12:40 — Teaching Loads empty list: employee number mismatch + date-based load

**Why:** Modal showed no employees even when Skolaris had loads (e.g. ORDANZA on 2026-06-16) because Pulse used local numbers like `2026-00103` while Skolaris uses `24-CAS160189`.

**Files:**
- `app/Support/TimeLogs.php`
- `app/Http/Controllers/TimeLogsController.php`
- `routes/web.php`
- `resources/views/timekeeping/time-logs/_pull-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Root cause: Pulse faculty employee numbers did not match Skolaris; intersection was empty.
- Synced local ORDANZA to `24-CAS160189` for testing.
- Pull modal now has **Load from Skolaris** — fetches faculty with loads for the selected date range, then keeps only Pulse faculty with matching employee numbers.

## 12:55 — Teaching Loads: Pulse employee list (no Load from Skolaris)

**Why:** Employee picker should list existing Pulse faculty with matching Skolaris employee numbers for multi-select; date range is only for the pull payload.

**Files:**
- `resources/views/timekeeping/time-logs/_pull-form.blade.php`
- `resources/js/app.js`
- `app/Http/Controllers/TimeLogsController.php`
- `app/Support/TimeLogs.php`
- `routes/web.php`

**Summary:**
- Removed **Load from Skolaris** for the employee list.
- Modal shows Pulse-eligible faculty immediately (multi-select + client search).
- Date From/To still used only when starting the pull.

## 13:05 — Teaching Loads pull: keep dates visible until modal close

**Why:** Date From/To looked cleared on Start Pull because inputs were `disabled`.

**Files:**
- `resources/js/app.js`

**Summary:**
- Dates are no longer disabled during pull.
- Form (dates, search, checkboxes, progress) resets only when the pull modal closes.

## 13:10 — Database: always show SQL upload/restore

**Why:** Restore SQL should be visible on the Database page without the "." key easter egg.

**Files:**
- `resources/views/database/index.blade.php`
- `resources/js/app.js`

**Summary:**
- **Restore SQL Backup** panel is always visible (upload .sql + confirm replace).
- Removed the 5× period / hold-`.` easter egg reveal.

## 13:15 — Daily cloud backup filename includes desktop name

**Why:** Distinguish S3 backups from different machines by including the computer hostname in the object key.

**Files:**
- `app/Services/DesktopCloudBackupService.php`
- `tests/Unit/DesktopCloudBackupServiceTest.php`
- `resources/views/database/index.blade.php`

**Summary:**
- S3 upload filename format: `pulse-db-{desktop-name}-{Y-m-d-H-i-s}.sql.gz`
- Desktop name from `gethostname()` / `php_uname('n')`, sanitized for S3 keys.

## 13:30 — Clarify Daily Cloud Backup status on browser

**Why:** Status showed “Pending after 10:00” even when schedule had passed, because upload only runs in NativePHP desktop — not `artisan serve`.

**Files:**
- `app/Services/DesktopCloudBackupService.php`
- `resources/views/database/index.blade.php`

**Summary:**
- Status now shows **Waiting for desktop app** when opened in the browser.

## 13:35 — Daily cloud backup runs in browser too

**Why:** Cloud backup was gated to NativePHP only; user needs the same once-per-day S3 upload from browser/`artisan serve`.

**Files:**
- `app/Services/DesktopCloudBackupService.php`
- `resources/views/database/index.blade.php`
- `config/backup.php`

**Summary:**
- Removed `isNativeDesktop()` gate from `backupIfNeeded()`.
- Status copy updated for browser + desktop.

## 16:49 — Restructure teaching load pull history

**Why:** Change the Teaching Loads tab from a per-employee last-sync list into pull batch history with drill-down views.

**Files:**
- `app/Models/TeachingLoadPullBatch.php`
- `app/Models/TeachingLoadSession.php`
- `app/Models/Employee.php`
- `app/Services/TeachingLoadPullService.php`
- `app/Support/TimeLogs.php`
- `app/Http/Controllers/TimeLogsController.php`
- `config/time_logs.php`
- `database/migrations/2026_07_23_170000_create_teaching_load_pull_batches_table.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_teaching-loads-results.blade.php`
- `resources/views/timekeeping/time-logs/_teaching-load-pull-batch-content.blade.php`
- `resources/views/timekeeping/time-logs/_teaching-load-employee-content.blade.php`

**Summary:**
- Added `teaching_load_pull_batches` and linked teaching load session rows to a specific pull batch.
- Backfilled existing teaching load session rows into pull batch history so old pulls still appear in the new UI.
- Updated the Teaching Loads tab to list pull batches first, then show employees per batch, then show the exact pulled load rows per employee.
- Changed new teaching load pulls to append historical batch data instead of replacing prior teaching load session rows.

## 16:52 — Add back button on teaching load employee modal

**Why:** Allow users to return from employee load rows to the parent pull batch employee list.

**Files:**
- `resources/views/timekeeping/time-logs/index.blade.php`

**Summary:**
- Added the shared modal `backUrl` on the Teaching Load Rows modal so it navigates back to the selected pull batch view.

## 16:55 — Teaching load pull duplicate check

**Why:** Avoid re-saving identical Skolaris teaching load rows and only overwrite Pulse data when the source content changed.

**Files:**
- `app/Services/TeachingLoadPullService.php`
- `resources/js/app.js`
- `public/build/*`

**Summary:**
- Compared incoming Skolaris load rows with existing Pulse sessions for the same employee and date range before saving.
- Skipped unchanged employees during pull and showed `unchanged — skipped` in the progress UI.
- Overwrote only when Skolaris data differed by replacing existing sessions in that employee date range.

## 17:03 — Fix Teaching Loads page timeout

**Why:** Opening Teaching Loads was calling Skolaris employee-number listing on every page load and exceeding PHP max execution time.

**Files:**
- `app/Support/TimeLogs.php`
- `app/Http/Controllers/TimeLogsController.php`
- `resources/views/timekeeping/time-logs/_pull-form.blade.php`
- `resources/views/timekeeping/time-logs/index.blade.php`

**Summary:**
- Removed Skolaris employee-number API calls from Teaching Loads page render and pull modal employee listing.
- Pull modal now lists active Pulse faculty locally; Skolaris data is verified only during the per-employee pull step.

## 17:21 — Reflect Skolaris teaching loads in Employee Load profile

**Why:** Pulled Teaching Loads from Skolaris should appear in Employee Profile → Employee Load, not only in the Time Logs pull history.

**Files:**
- `app/Services/TeachingLoadPullService.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`

**Summary:**
- Synced Skolaris teaching load pulls into `raw_employee_load_entries` (same source used by Employee Profile and payroll).
- On pull overwrite, only Skolaris-sourced employee load rows for that employee/date range are replaced.
- Backfilled existing pulled teaching load sessions into Employee Load entries (Angelica now shows 2 sessions).

## 17:27 — Employee Load tab pagination with Show all

**Why:** Add per-page controls and Show all on Employee Profile → Employee Load.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`
- `resources/views/partials/client-pagination-controls.blade.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/js/app.js`
- `public/build/*`

**Summary:**
- Switched Employee Load tab rows to client pagination with Show selector (10/25/50/100) and Show all.
- Re-initialized client pagination after Employee Profile lazy tab loads.

## 17:34 — Employee Load Time In/Out from attendance logs

**Why:** Time In and Time Out on Employee Load must come from Time Logs punches, not from Skolaris class schedule times.

**Files:**
- `app/Services/EmployeeLoadAttendanceMatcher.php`
- `app/Services/TeachingLoadPullService.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`

**Summary:**
- Skolaris pull now stores schedule/subject only in Employee Load rows; Time In/Out are matched from `raw_timekeeping_inandout`.
- Employee Load tab refreshes Skolaris-sourced punch times from logs on each view.
- Cleared previously copied schedule times from existing Skolaris-synced Employee Load rows.

## 18:10 — Faculty payroll uses teaching load Hours with log cross-check

**Why:** Faculty payroll batch computation must use pulled teaching-load Hours, only when the day has attendance logs, with late from the first Time In and shift-code rules still applied.

**Files:**
- `app/Services/FacultyLoadPayrollService.php`
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/PayrollAttendanceHoursWorkedService.php`
- `app/Services/PayrollBatchService.php`
- `app/Services/TeachingLoadPullService.php`
- `app/Models/RawEmployeeLoadEntry.php`
- `app/Models/Employee.php`
- `database/migrations/2026_07_23_181000_add_total_hours_to_raw_employee_load_entries.php`
- `tests/Unit/FacultyLoadPayrollServiceTest.php`

**Summary:**
- Added `total_hours` on employee load entries and backfilled from teaching load sessions.
- Faculty TIME_IN_OUT payroll now prefers load Hours instead of the daily punch/shift path when teaching loads exist.
- Load Hours are included only for days that have attendance logs; days without logs are skipped.
- Late uses the first Time In of the day vs shift code time in (fallback: earliest class schedule), including flexi/grace via existing shift/policy rules.

## 18:47 — Faculty Basic is 0 when no teaching loads

**Why:** Faculty have no auto-log policy; Basic must come from teaching loads only — never Leaves/fixed BASC when loads (or load-day logs) are missing.

**Files:**
- `app/Services/FacultyLoadPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `tests/Unit/FacultyLoadPayrollServiceTest.php`

**Summary:**
- Faculty payroll path now applies for all faculty regardless of Basic Computation (Leaves vs Time-In/Time-Out).
- When there are no teaching loads / no computable load hours, Basic taxable is forced to 0 instead of paying the salary BASC amount.
- `resolveAttendancePayroll` always returns the faculty result (including zeros) so Leaves does not fall through to full basic.

## 18:58 — Faculty Basic uses salary hourly rate (use basic as hourly)

**Why:** With “Use Basic Income as Hourly Rate” checked, Basic must be Hours × basic amount (e.g. 3 × 500 = 1,500), not daily rate × days (62.50).

**Files:**
- `app/Services/PayrollAttendanceHoursWorkedService.php`
- `app/Services/FacultyLoadPayrollService.php`

**Summary:**
- Faculty hours-worked path no longer blocked when salary Basic Computation is Leaves.
- Faculty fallback basic taxable now uses `hourlyRate()` × computed load hours (respects `use_basic_income_as_hourly_rate` for Faculty and Staff).
- Reprocessed Angelica batch detail: BASC Hours 3.00, Taxable 1,500.00.

## 19:11 — Employee Load hours from Time In/Out + Waive Late

**Why:** Uploaded teaching-load Hours must use Time In → Time Out, with an Edit action to waive late so late is not deducted from that class’s hours.

**Files:**
- `database/migrations/2026_07_23_190500_add_late_waived_to_raw_employee_load_entries.php`
- `app/Models/RawEmployeeLoadEntry.php`
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/FacultyLoadPayrollService.php`
- `app/Http/Controllers/TimekeepingEmployeeLoadController.php`
- `app/Support/TimekeepingEmployeeLoad.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-load/_show-content.blade.php`
- `resources/views/timekeeping/employee-load/_entry-edit-form.blade.php`
- `resources/views/timekeeping/employee-load/index.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`
- `tests/Unit/FacultyLoadPayrollServiceTest.php`

**Summary:**
- Hours = Time Out − Time In; when **Waive Late** is checked, the late gap is credited back (e.g. 1:15–2:00 on a 1:00–2:00 class → 1.00 hr) and late minutes are not billed.
- Batch view has Edit (stacked modal) for Time In/Out, Waive Late, Remarks, Comments; Hours + Late columns added.
- Faculty payroll counts uploaded rows that already have Time In/Out without requiring separate biometric day logs.

## 19:25 — Fix Payroll Register print preview blank page

**Why:** Safari print preview showed a mostly white page (sidebar/chrome only) because overflow-x table wrappers and app layout margins hide report rows when printing.

**Files:**
- `resources/css/app.css`
- `resources/views/payroll/reports/preview.blade.php`
- `resources/views/payroll/reports/_preview.blade.php`

**Summary:**
- Added `@media print` rules: hide sidebar/header/controls, reset main offset margins, force `overflow: visible` on scroll wrappers, black text on white.
- Report preview toolbar marked `data-print-hide`; sheet uses `report-print-sheet`.
- Ran `npm run build` for desktop/production assets.

## 19:28 — Payroll Register print via dedicated iframe document

**Why:** Safari print preview was still fully white when printing the app layout; print now uses a clean standalone HTML document with the report table.

**Files:**
- `resources/views/payroll/reports/_print-document.blade.php`
- `resources/views/payroll/reports/preview.blade.php`
- `resources/js/app.js`
- `resources/css/app.css`

**Summary:**
- Print button opens a hidden, self-contained report document in an iframe and prints that (no sidebar/loader/layout).
- Inline black-on-white table styles so rows appear even with “Print backgrounds” off.
- Ran `npm run build`.

## 20:16 — Desktop build 0.1.18 (macOS + Windows)

**Why:** Ship current Pulse changes (Teaching Loads, Skolaris Pulse API, payroll print, and related fixes) as paired Mac/Windows installers with the Pulse API key bundled.

**Files:**
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.18`)
- `dist/Pulse-0.1.18-arm64.dmg`
- `dist/Pulse-0.1.18-setup.exe`

**Summary:**
- Bumped version to **0.1.18**.
- Build preflight confirmed `SKOLARIS_PULSE_API_KEY` + `SKOLARIS_PULSE_API_BASE_URL` (not stripped by `cleanup_env_keys`).
- Ran `./scripts/build-desktop.sh all` (icons, DesktopBootstrapSeeder, Vite production build, native mac arm64 + win x64).
- Installers: `pulse/dist/Pulse-0.1.18-arm64.dmg` (177M), `pulse/dist/Pulse-0.1.18-setup.exe` (150M).
- Older `Pulse-0.1.15-*` pruned; kept latest 3 versions.

## 20:19 — PhilHealth contribution table 2026 (5%)

**Why:** Replace outdated PhilHealth brackets with the 2026 5% estimated contribution table (floor ₱10k → ₱250 employee share).

**Files:**
- `database/seeders/GovernmentTablesSeeder.php`
- `.env` / `.env.example` (`NATIVEPHP_APP_VERSION=0.1.19`)

**Summary:**
- Floor: salary ≤ ₱10,000 → EE ₱250 / ER ₱250 (total ₱500).
- ₱1,000 MSC steps through ₱99,000 at 5% split 50/50; ceiling ₱100,000+ → ₱2,500 / ₱2,500.
- Matches sample points: 10k/20k/30k/40k/50k/70k/100k.
- Force-replaced old brackets; reseeding ran locally. Desktop apps pick this up on bootstrap after **0.1.19** build.

## 20:22 — SSS contribution table Jan 2025 (Circular 2024-006)

**Why:** Replace outdated SSS brackets with the official January 2025 schedule (15% of MSC).

**Files:**
- `database/seeders/GovernmentTablesSeeder.php`

**Summary:**
- MSC floor ₱5,000 / ceiling ₱35,000; ₱500 compensation steps.
- ER 10% + EE 5%; MPF applies on MSC above ₱20,000 (stored in EE/ER SSS totals).
- EC ₱10 when MSC < ₱15,000; ₱30 when MSC ≥ ₱15,000.
- Verified samples: 5k/10k/15k/20k/20.5k/25k/30k/35k match the circular.
- Reseeded locally; desktop picks up via bootstrap on **0.1.19** build.
