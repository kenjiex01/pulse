## 09:25 — Employee upload template 500 fix + desktop startup speed

**Why:** Windows desktop v0.1.7 showed 500 Server Error on employee template download; app felt slow on every launch.

**Files:**
- `resources/templates/employee_upload_template.xlsx`
- `config/employee_upload.php`
- `config/uploads.php`
- `app/Services/EmployeeUploadService.php`
- `app/Http/Controllers/EmployeeUploadController.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Providers/AppServiceProvider.php`
- `resources/views/employees/_upload-form.blade.php`

**Summary:**
- Template download now serves a **pre-built .xlsx** from `resources/templates/` via `response()->download()` — no runtime PhpSpreadsheet/zip dependency on click.
- Sample row moved to config (no DB query on template download); row mapper lookups are lazy-loaded only during upload parse.
- Desktop PHP ini: `memory_limit=256M`, `max_execution_time=120`; migrate runs only when pending migrations exist (not every app open).
- Excel text-format range optimized (single block style vs per-column loop).

## 09:32 — Desktop performance: sidebar + permission cache

**Why:** Desktop app felt slow — sidebar alone ran ~40 SQLite queries per page via N+1 `hasSubModuleAccess()` calls.

**Files:**
- `app/Services/SidebarNavigationService.php`
- `app/Models/User.php`
- `app/Providers/AppServiceProvider.php`
- `app/Http/Middleware/PrepareAuthenticatedUser.php`
- `app/Http/Middleware/EnsureModuleAccess.php`
- `app/Http/Controllers/RoleController.php`
- `app/Http/Controllers/UserController.php`
- `bootstrap/app.php`

**Summary:**
- New `SidebarNavigationService` builds menu in ~3–6 queries (was ~40); per-request memory cache avoids repeat work.
- `User` access checks batch-load role/module/sub-module permissions once per request (18 checks = 3 queries, not 36).
- `PrepareAuthenticatedUser` middleware eager-loads roles on every auth request.
- `EnsureModuleAccess` caches sub-module lookup by route name within the request.
- Sidebar cache invalidated when roles or user role assignments change.

## 09:36 — Desktop pay type dropdown auto-repair on reinstall

**Why:** Pay type dropdowns (employee salary, payroll batch, payroll calendar) were empty after desktop reinstall — `ReferenceDataBootstrapService` was removed since v0.1.1.

**Files:**
- `app/Services/ReferenceDataBootstrapService.php`
- `app/Providers/AppServiceProvider.php`

**Summary:**
- Restored auto-repair on every desktop app open: seeds `lu_pay_types` (Daily/Weekly/Semi-Monthly/Monthly) when any of IDs 1–4 are missing.
- Also repairs empty basic computation lookups and geographic address tables (country/region/province/city).
- Fresh SQLite on reinstall now clears the desktop bootstrap version marker so navigation + reference sync runs again.

## 09:48 — Desktop build v0.1.8 (macOS + Windows)

**Why:** Ship template download fix, performance optimizations, Sumulong DTR punch fix, and pay type auto-repair in new installers.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.8`)
- `dist/Pulse-0.1.8-arm64.dmg` (generated)
- `dist/Pulse-0.1.8-arm64.zip` (generated)
- `dist/Pulse-0.1.8-setup.exe` (generated)

**Summary:**
- Bumped to **0.1.8** and rebuilt macOS (arm64 DMG/ZIP) + Windows (x64 EXE).
- Includes: employee upload template fix, sidebar/permission performance, startup migrate optimization, individual Sumulong DTR punches, pay type auto-repair on reinstall.

## 16:19 — Fix Time logs 500 on desktop (SQLite)

**Why:** Desktop Time logs page showed 500 because `dtrCampuses()` used MySQL-only `FIELD()`.

**Files:**
- `app/Support/TimeLogs.php`

**Summary:**
- Replaced `orderByRaw("FIELD(...)")` with PHP `sortBy` on campus code order (`SA`, `SU`, `CA`).
- Works on both MySQL (dev) and SQLite (desktop) without SQL dialect differences.

## 16:35 — Wire policy Leave Type into payroll batch process

**Why:** Late/undertime should be tagged using `tardiness_leave_type_id` and `undertime_leave_type_id` from Timekeeping Policy during batch process/reprocess.

**Files:**
- `database/migrations/2026_07_15_163500_create_trn_payroll_leaves_table.php`
- `app/Models/PayrollLeave.php`
- `app/Models/PayrollBatchDetail.php`
- `app/Services/PayrollAttendanceLeaveService.php`
- `app/Services/PayrollBatchService.php`

**Summary:**
- Added `trn_payroll_leaves` table + `PayrollLeave` model for batch leave tagging.
- New `PayrollAttendanceLeaveService` builds per-session leave records from policy: tardiness → `tardiness_leave_type_id`, undertime → `undertime_leave_type_id`, mark-as-absent → `awol_leave_type_id`.
- Batch process/reprocess now persists auto leave records alongside existing LTDE/UTDE deductions; uploaded `raw_payroll_leaves` are also merged.
- Reprocess clears auto leave lines (keeps manual lines when preserved).

## 16:40 — Wire overtime policy into payroll batch process

**Why:** Overtime settings (excess hours, before/after schedule, min minutes, rounding, special OT) must drive OVRT income on batch process/reprocess.

**Files:**
- `app/Support/TimekeepingPolicy.php`
- `app/Services/PayrollOvertimeService.php`
- `app/Services/PayrollBatchService.php`

**Summary:**
- Added `resolveOvertimeEquivalent()`, rounding helper, and `EXCESS_HOUR_CONSIDER_OT` constant.
- New `PayrollOvertimeService` computes billable OT from policy: before/after schedule, min minutes, special OT split, equivalents, rounding.
- Batch process now auto-adds/updates **OVRT** income when policy treats excess hours as OT.
- Works for both time logs and employee load attendance sources.

## 16:48 — Regular-day OT minimum minutes per segment

**Why:** Payroll batch must follow policy "Consider as OT" setup — excess below minimum (e.g. 29 min when min is 30) must not count as OT, matching legacy PATHS behavior.

**Files:**
- `app/Services/PayrollOvertimeService.php`

**Summary:**
- Minimum minutes now applied **per segment** (before schedule and after schedule separately), not on combined total.
- After-schedule regular OT and special OT each get their own minimum threshold (`min_minutes`, `special_ot_min_minutes`).
- Example: min 30 + 29 min excess after shift → 0 OVRT; 30 min excess → billable OT.

## 16:52 — Wire breaks policy into payroll batch process

**Why:** Breaks tab settings (computation basis, fix schedule, break tardiness grace/rounding/leave type) must drive payroll when employees have break punches in time logs.

**Files:**
- `app/Support/TimekeepingPolicy.php`
- `app/Services/PayrollBreakService.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollAttendanceLeaveService.php`
- `app/Services/PayrollBatchService.php`

**Summary:**
- Added `resolveBreakTardinessEquivalent()` and break computation constants.
- New `PayrollBreakService` reads shift break minutes, extracts break OUT/IN pairs from time-log punches, and applies policy (`break_computation`, `is_fix_break`, grace, equivalents, rounding).
- When **Deduct Tardiness in Breaks** is enabled: batch auto-tags `break_tardiness_leave_type_id` and adds LTDE deduction for break late minutes.
- Uses actual break punches when policy = "Use actual break time"; falls back to scheduled break minutes when **Fix Break Schedule** is checked and no break punches exist.

## 17:12 — Fix breaks policy payroll effect (actual vs scheduled)

**Why:** Breaks setup must affect payroll batch; scheduled-break mode was blocking break-late detection, and late/break-late should combine into one LTDE line.

**Files:**
- `app/Services/PayrollBreakService.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollAttendanceLeaveService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/timekeeping/policy/_settings-breaks.blade.php`
- `resources/js/app.js`

**Summary:**
- Break late now compares **actual break punches** vs **allowed minutes** (shift break + grace) — works with both scheduled and actual break policy modes.
- Grace period and "Actual Late less Grace Period" increase allowed break minutes before tardiness applies.
- Late + break-late minutes merge into a single **LTDE** deduction line on batch process.
- Break tardiness leave tag still uses `break_tardiness_leave_type_id`.
- Fixed break settings toggle boolean + JS sync for Rounding/Leave Type fields.

## 17:15 — Fix Breaks tab disabled Rounding/Leave Type fields

**Why:** Rounding Rule and Leave Type stayed disabled even when "Deduct Tardiness in Breaks" was checked.

**Files:**
- `resources/views/timekeeping/policy/_settings-breaks.blade.php`
- `resources/js/app.js`

**Summary:**
- Removed server-side `@disabled` on break tardiness fields so JS can enable them when the checkbox is ticked.
- Fixed `$deductBreakTardiness` boolean coercion from policy/old input.
- `syncBreakTardinessFields` now uses `setAttribute`/`removeAttribute` for reliable enable/disable on selects.
- Rebuilt frontend assets (`npm run build`).

## 17:19 — Remove Fix Break Schedule from Breaks policy

**Why:** Setting was unused in Pulse payroll and not needed in the Breaks tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-breaks.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `app/Services/PayrollBreakService.php`
- `scripts/check_timekeeping_policy.php`

**Summary:**
- Removed Fix Break Schedule checkbox from Breaks policy UI.
- Stopped validating/saving `is_fix_break` on breaks settings update.
- Removed `is_fix_break` fallback in `PayrollBreakService::consumedBreakMinutes()`.
- DB column retained for compatibility; no migration needed.

## 17:21 — Remove Hide Negative Leaves and Enable Notification

**Why:** User requested removal of unused Leaves and Absences policy toggles.

**Files:**
- `resources/views/timekeeping/policy/_settings-leaves-absences.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `resources/js/app.js`
- `scripts/check_timekeeping_policy.php`

**Summary:**
- Removed Hide Negative Leaves and Enable Notification checkboxes from Leaves and Absences tab.
- Removed Attendance Notification When Processing textarea (tied to Enable Notification).
- Stopped validating/saving `hide_negative_leaves`, `enable_notification`, and `notif_for_process`.
- Removed `syncNotificationFields` JS helper.

## 17:22 — Remove Validity of Late File from Leaves and Absences

**Why:** User requested removal of unused late-file validity setting.

**Files:**
- `resources/views/timekeeping/policy/_settings-leaves-absences.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `scripts/check_timekeeping_policy.php`

**Summary:**
- Removed Validity of Late File (days) field from Leaves and Absences tab.
- Stopped validating/saving `validity_of_late_file` on leaves-absences settings update.
- DB column and default (30) retained for compatibility.

## 17:23 — Remove Leave Equivalents section from Leaves and Absences

**Why:** User requested removal of unused Leave Equivalents UI on the leaves-absences tab.

**Files:**
- `resources/views/timekeeping/policy/_settings-leaves-absences.blade.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `config/timekeeping_policy.php`

**Summary:**
- Removed Leave Equivalents section (leave type picker, table, Add Equivalent).
- Dropped `leave` from `timekeeping_policy.equivalents` config (no CRUD routes for leave equivalents).
- Removed controller data loading for leave equivalents on leaves-absences tab.
- `tbl_timekeeping_policy_leave` table/model retained for existing data compatibility.

## 17:24 — Remove Leaves and Absences policy tab

**Why:** User requested removal of the entire Leaves and Absences settings tab.

**Files:**
- `config/timekeeping_policy.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `scripts/check_timekeeping_policy.php`

**Deleted:**
- `resources/views/timekeeping/policy/_settings-leaves-absences.blade.php`

**Summary:**
- Removed Leaves and Absences from policy settings tab navigation.
- Removed leaves-absences validation/save handlers and blade partial.
- Old `/leaves-absences` URLs now return 404.
- Policy columns (`awol_leave_type_id`, `leave_processing_mode`, etc.) retained in DB for payroll compatibility.

## 17:25 — Remove Team Settings policy tab

**Why:** User requested removal of the Team Settings tab from policy settings.

**Files:**
- `config/timekeeping_policy.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `scripts/check_timekeeping_policy.php`

**Deleted:**
- `resources/views/timekeeping/policy/_settings-team-settings.blade.php`

**Summary:**
- Removed Team Settings from policy settings tab navigation.
- Removed team-settings validation/save handlers and Limit on Views UI.
- Old `/team-settings` URLs now return 404.
- `timekeeping_policy_team_setting_id` column retained in DB for compatibility.

## 17:26 — Remove Logs Tagging policy tab

**Why:** User requested removal of the Logs Tagging tab from policy settings.

**Files:**
- `config/timekeeping_policy.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `resources/js/app.js`
- `scripts/check_timekeeping_policy.php`

**Deleted:**
- `resources/views/timekeeping/policy/_settings-logs-tagging.blade.php`

**Summary:**
- Removed Logs Tagging from policy settings tab navigation.
- Removed logs-tagging validation/save handlers and tag/description UI.
- Removed `syncLogsTaggingFields` JS helper; rebuilt frontend assets.
- Old `/logs-tagging` URLs now return 404.
- Logs tagging columns retained in DB for compatibility.

## 17:30 — Desktop build v0.1.9 (macOS + Windows)

**Why:** User requested fresh desktop installers after policy UI cleanup, breaks payroll wiring, and related fixes.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.9`)
- `dist/Pulse-0.1.9-arm64.dmg` (generated)
- `dist/Pulse-0.1.9-arm64.zip` (generated)
- `dist/Pulse-0.1.9-setup.exe` (generated)

**Summary:**
- Bumped to **0.1.9** and ran `scripts/build-desktop.sh all`.
- macOS Apple Silicon DMG/ZIP + Windows x64 NSIS installer produced in `pulse/dist/`.
- Code signing/notarization skipped (no Apple Developer ID / Windows cert on build machine) — OK for internal/testing installs.

## 19:10 — Employee upload sample files

**Why:** User requested a ready-to-upload sample for the Employee Management upload modal.

**Files:**
- `docs/samples/employee-upload-sample.csv`
- `docs/samples/employee-upload-sample.xlsx`
- `scripts/generate-employee-upload-sample.php`

**Summary:**
- Added 3 test employees (`2026-00101`–`2026-00103`) with unique emails and biometric IDs (`1001`–`1003`), campus `CA`, Daily + Time-In/Time-Out salary setup, role `staff`.
- Uses official template header rows; omits built-in `2026-00099` sample row to avoid duplicate-import errors.
- Script regenerates CSV + XLSX; validated parse returns 3 valid rows, 0 errors.

## 19:15 — Fix employee upload sample campus code

**Why:** Upload preview failed with `Unknown campus code (CA)` on user's database.

**Files:**
- `docs/samples/employee-upload-sample.csv`
- `docs/samples/employee-upload-sample.xlsx`
- `scripts/generate-employee-upload-sample.php`
- `config/employee_upload.php`
- `app/Services/ReferenceDataBootstrapService.php`

**Summary:**
- Sample generator now resolves campus code from the connected database (currently `AG`) instead of hardcoded `CA`.
- Default template sample row campus changed to `AG`.
- Desktop bootstrap re-runs `CampusSeeder` when expected ICCT campus codes are missing.

## 19:20 — Fix Overtime tab Rounding Rule disabled state

**Why:** Rounding Rule stayed greyed out on desktop when Treatment of Excess Hours was set to "Consider excess hours as OT".

**Files:**
- `resources/views/timekeeping/policy/_settings-overtime.blade.php`
- `resources/js/app.js`

**Summary:**
- Removed server-side `@disabled` on OT fields; JS now toggles via `data-ot-input` markers.
- `syncExcessHourFields` uses `removeAttribute('disabled')` when enabling (same fix as Breaks tab).
- Rebuilt frontend assets (`npm run build`).

## 19:25 — Remove General tab policy fields

**Why:** User requested removal of unused General tab settings.

**Files:**
- `resources/views/timekeeping/policy/_settings-general.blade.php`
- `app/Support/TimekeepingPolicy.php`
- `scripts/check_timekeeping_policy.php`

**Summary:**
- Removed **Non Regular Hours Computation Basis** dropdown from General tab.
- Removed **Minimum no. of hours rendered per week** field from rest-day validation section.
- Dropped related validation/save handling; DB columns retained for compatibility.

## 19:30 — Hide Payroll Calendar Deduction & Loan Priority tab

**Why:** User requested temporary removal of the Deduction & Loan Priority tab from Payroll Calendar.

**Files:**
- `app/Support/PayrollCalendarModule.php`
- `app/Http/Controllers/PayrollCalendarController.php`
- `resources/views/payroll/calendar/_module-tabs-nav.blade.php`
- `resources/views/payroll/calendar/index.blade.php`

**Summary:**
- Removed **Deduction & Loan Priority** from module tabs; top tab bar hidden when only Pay Periods remains.
- Priority/enable/move routes now redirect to Pay Periods (old URLs still safe).
- Priority views and DB tables kept for future restore.

## 19:35 — Restore Hours Worked upload tab

**Why:** User requested **Hours Worked** sub-tab beside Deductions under Upload Adjustments.

**Files:**
- `config/payroll_transaction.php`

**Summary:**
- Re-added `hours-worked` upload type (Day Type, Time Type, No. of Hours columns).
- Tab appears after Deductions; existing upload service/views already support this type.

## 19:40 — Hours Worked upload sample for employee 2026-00003

**Why:** User requested a ready-to-upload sample for employee `2026-00003`.

**Files:**
- `docs/samples/hours-worked-upload-2026-00003-sample.csv`

**Summary:**
- CSV for **Upload Adjustments → Hours Worked** with 3 rows: REGU/BP 40h, REGU/OT 4h, REGU/NDIF 2h.
- Uses official template header rows; validated parse returns 3 valid rows, 0 errors.

## 19:42 — Wire Hours Worked upload into payroll batch computation

**Why:** Uploaded hours were saved to `raw_payroll_hours_worked` but batch process still used flat salary BASC (e.g. 16,500) instead of hours × rate.

**Files:**
- `app/Services/PayrollHoursWorkedPayrollService.php`
- `app/Services/PayrollBatchService.php`

**Summary:**
- New service loads raw hours per employee/pay calendar and computes income lines via rate group (and ND rate group for night diff).
- `prepareDetailTransactions()` overrides BASC and adds OT/ND income types from uploaded hours when rows exist.
- Upload commit and `applyUploadedHoursWorkedToDetail()` refresh incomes on open/processed batches.
- Re-process batch after upload (or re-upload) so existing processed batches pick up hours-based amounts.

## 19:48 — Add attendance log hours to uploaded hours worked

**Why:** User expects payroll to sum time-log hours and uploaded hours per day/time type (e.g. 23h logs + 20h upload = 43h BP), not replace one with the other.

**Files:**
- `app/Services/PayrollAttendanceHoursWorkedService.php`
- `app/Services/PayrollHoursWorkedPayrollService.php`
- `app/Services/PayrollOvertimeService.php`

**Summary:**
- New service derives BP/OT/SOT/NDIF hours from time logs or employee load within the pay period date range.
- `PayrollHoursWorkedPayrollService` merges attendance rows with `raw_payroll_hours_worked` uploads by `day_type_id` + `time_type_id` before rate computation.
- Overtime breakdown helper added for separate OT vs SOT hours from logs.

## 19:45 — Hours column on payroll batch income tab

**Why:** User requested hours displayed beside each income line in Employee Payroll Detail (same visibility as deduction hours).

**Files:**
- `database/migrations/2026_07_15_194500_add_hours_to_payroll_incomes.php`
- `app/Models/PayrollIncome.php`
- `app/Services/PayrollHoursWorkedPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`

**Summary:**
- Added nullable `hours` on `trn_payroll_incomes`; persisted from merged attendance + uploaded hours per income type.
- Income tab table shows Hours column and footer total when any line has hours.
- Overtime from time logs also stores rendered hours on OVRT lines.

## 19:43 — Hours on Net Pay tab summary

**Why:** User requested hours shown on Net Pay tab when income/deduction lines have hours (matching Incomes tab).

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`

**Summary:**
- Income and deduction summary lines show `X.XX hrs` before the amount when hours exist.
- Gross Income and Total Deductions footers include hours totals when applicable.

## 19:44 — Remove total hours from Net Pay summary footers

**Why:** User does not want summed hours beside Gross Income (or deduction total) on Net Pay tab.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`

**Summary:**
- Per-line hours on income/deduction rows kept; removed hours totals from Gross Income and Total Deductions footers.

## 19:47 — Desktop build v0.1.10 (macOS + Windows)

**Why:** User requested new desktop build with all current functionalities included.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.10`)
- `.cursor/rules/desktop-build.mdc` (new — full-build checklist for future releases)
- `dist/Pulse-0.1.10-arm64.dmg`, `dist/Pulse-0.1.10-arm64.zip`, `dist/Pulse-0.1.10-setup.exe`

**Summary:**
- Bumped app version to **0.1.10**.
- Ran full `build-desktop.sh` pipeline: icons, `DesktopBootstrapSeeder` smoke test, employee upload template regen, `npm run build`, NativePHP installers.
- Includes today's payroll hours-worked wiring, income/net-pay hours columns, overtime tab fix, Hours Worked upload tab, and bootstrap/reference data sync.

## 20:44 — Hours Worked sample image in user guide

**Why:** User requested a visual sample of the Hours Worked upload file instead of a file path link in the guide.

**Files:**
- `docs/user-guide/images/54-hours-worked-upload-sample.png`
- `docs/user-guide/index.html`
- `docs/user-guide/README.md`
- `scripts/capture-hours-worked-sample-image.mjs`

**Summary:**
- Generated spreadsheet-style PNG from `hours-worked-upload-2026-00003-sample.csv` content (employee 2026-00003).
- Replaced `docs/samples/...csv` link in semi-monthly flow with Figure 55 image embed.


**Why:** Current ICCT payroll practice uploads Hours Worked only — Time Logs not used yet.

**Files:**
- `docs/user-guide/index.html`
- `docs/user-guide/README.md`

**Summary:**
- Revised "Run semi-monthly payroll" steps: Employee Profile → Hours Worked upload → create batch → Process → Post.
- Added callout that Time Logs is future/optional; Hours Worked is the primary hours source for now.
- Updated Hours Worked field description and README batch workflow step 3.


**Why:** User requested a screenshot/visual for every guide section and subsection.

**Files:**
- `docs/user-guide/index.html`
- `docs/user-guide/images/18-*.png` through `53-*.png` (35 new screenshots)
- `scripts/capture-user-guide-screenshots.mjs`

**Summary:**
- Expanded Playwright capture script to 51 automated pages (skips user-provided `17-tardiness-undertime-policy.png`).
- Captures HR wizard/salary/upload/filters/lookups, all Timekeeping policy tabs and uploads, Payroll sub-modules, and Admin create modals.
- Wired `<figure>` blocks into every `field-section` and major subsection in `index.html` (54 figures total).


**Why:** User requested the actual Tardiness and Undertime policy screen added to the HTML guide with field explanations.

**Files:**
- `docs/user-guide/images/17-tardiness-undertime-policy.png`
- `docs/user-guide/index.html`

**Summary:**
- Added Figure 18 screenshot (Default Policy → Tardiness and Undertime tab) with navigation callout.
- Refined field table rows to match visible UI (flexi, grace, rounding, leave types, Save Settings).
- Added `#tk-tardiness` anchor in Timekeeping sub-nav.

## 20:25 — Full field-by-field user guide (all modules)

**Why:** User requested every form field explained with purpose (gamit) across the entire HTML guide.

**Files:**
- `docs/user-guide/index.html`
- `scripts/expand-guide-field-tables.mjs`

**Summary:**
- Standardized all field tables to 4 columns: Field, Ano ito, Para saan (Gamit), Notes.
- Expanded HR (employee wizard, salary, upload, lookups), Payroll (rate, maintenance, govt, calendar, transaction), Administration, and Navigation sections.
- Timekeeping tables upgraded with dedicated Gamit column via expand script.

## 20:20 — Timekeeping field explanations in user guide HTML

**Why:** User requested per-field explanations for the Timekeeping section of the HTML guide.

**Files:**
- `docs/user-guide/index.html`

**Summary:**
- Expanded section 7 with field-by-field tables for Policy, Shift Code, Time Capturing, Holiday Settings, policy settings tabs, Time Logs, Employee Profile, and Employee Load.
- Added in-page sub-navigation links and field-table styling.

## 20:08 — Pulse user guide HTML export

**Why:** User requested a standalone HTML file for the illustrated user guide.

**Files:**
- `docs/user-guide/index.html`

**Summary:**
- Added self-contained HTML guide with Pulse branding, sticky table of contents, embedded screenshots, and print-friendly layout.
- Images load from sibling `images/` folder — open `index.html` in any browser.

## 20:02 — Pulse user guide with system screenshots

**Why:** User requested an illustrated user guide using real screenshots from the running Pulse app.

**Files:**
- `docs/user-guide/README.md`
- `docs/user-guide/images/*.png` (16 screenshots)
- `scripts/capture-user-guide-screenshots.mjs`

**Summary:**
- Added end-user guide covering login, navigation, administration, HR, timekeeping, payroll, uploads, and common workflows.
- Screenshots captured via Playwright using system Chrome (no separate Chromium install).
- Included screenshot refresh script for future UI updates.

## 19:49 — Prune dist to 3 latest desktop versions

**Why:** User requested old installers removed — keep only the 3 most recent versions in `dist/`.

**Files:**
- `scripts/build-desktop.sh`
- `.cursor/rules/desktop-build.mdc`
- `dist/` (removed Pulse-0.1.6-* and Pulse-0.1.7-*)

**Summary:**
- Deleted v0.1.6 and v0.1.7 artifacts; retained v0.1.8, v0.1.9, v0.1.10.
- `build-desktop.sh` now auto-prunes after each build (keeps 3 latest semver versions).
