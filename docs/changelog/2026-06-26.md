## 13:20 — Payroll batch Process button

**Why:** Generate per-employee income and deduction lines only when the batch is processed, not when employees are added.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Models/PayrollBatch.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_show-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- Added **Process** button on the payroll batch modal (visible when batch is Pending and has employees).
- New `POST payroll/transaction/batches/{batch}/process` route builds income/deduction rows from employee salary for each batch detail, then sets status to Processed.
- Stopped auto-generating payroll lines when adding employees or creating a batch with all employees.
- Employee detail modal shows an empty-state notice until the batch has been processed for that employee.

## 13:45 — Fix re-adding removed payroll batch employees

**Why:** Adding an employee who was previously removed from the same batch caused a duplicate-key SQL error.

**Files:**
- `app/Services/PayrollBatchService.php`

**Summary:**
- `assignEmployeeToBatch()` restores soft-deleted batch detail rows instead of inserting duplicates.
- Clears old income/deduction lines on restore so Process can regenerate fresh data.

## 14:00 — Hard delete payroll batch employee removal

**Why:** Removing an employee from a batch should permanently delete the assignment and related payroll lines.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Models/PayrollBatchDetail.php`

**Summary:**
- `removeEmployees()` now uses `forceDelete()` on batch detail rows.
- Parent `forceDelete` cascades hard delete to income and deduction child rows.
- Re-adding an employee after removal creates a fresh detail row (no soft-delete restore).

## 22:58 — Hard delete payroll batch details (no soft delete)

**Why:** Soft-deleted batch detail rows blocked re-adding employees via unique constraint (`Duplicate entry '2-3'`).

**Files:**
- `app/Models/PayrollBatchDetail.php`
- `app/Services/PayrollBatchService.php`
- `database/migrations/2026_06_30_230000_hard_delete_payroll_batch_details.php`

**Summary:**
- Removed `SoftDeletes` from `PayrollBatchDetail`; delete always hard-deletes the row and force-deletes income/deduction lines.
- Migration purges existing soft-deleted detail rows and drops `deleted_at` from `trn_payroll_batch_details`.
