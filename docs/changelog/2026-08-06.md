# 2026-08-06

## 12:18 — Payslip: Hourly Rate → Daily Rate

**Why:** Faculty/hybrid payslip should show daily rate (hourly × hours per day from salary setup), not hourly rate.

**Files:**
- `app/Services/Reports/PayslipReportService.php`
- `resources/views/payroll/reports/_payslip-document.blade.php`

**Summary:**
- Renamed faculty/hybrid payslip footer label from Hourly Rate to Daily Rate
- Value is `hourly_rate × hours_per_day` from the employee salary setup (e.g. 75 × 8 = 600)
- Applied to on-screen/PDF payslip and Excel export

## 12:22 — Reports: include posted payroll batches

**Why:** Payroll Register (and SSS/PhilHealth/Pag-IBIG) should still list batches after posting, with a Posted label.

**Files:**
- `app/Services/Reports/ReportBatchOptionsService.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `app/Services/Reports/PayrollContributionBatchSupport.php`
- `app/Services/Reports/SssContributionReportService.php`
- `resources/views/payroll/reports/options/payreg.blade.php`
- `resources/views/payroll/reports/options/sss.blade.php`
- `resources/views/payroll/reports/options/phil.blade.php`
- `resources/views/payroll/reports/options/pagibig.blade.php`

**Summary:**
- Batch dropdowns now include PROCESSED and POSTED batches
- Posted batches labeled with `— Posted` in the option text
- Report generation accepts posted batches (not PROCESSED-only)

## 12:28 — Payroll Register: Daily Rate column

**Why:** Show Daily Rate beside Rate per hour on the ICCT payroll register.

**Files:**
- `config/payroll_register_layout_columns.json`
- `config/payroll_register_layout.php`
- `app/Services/Reports/PayrollRegisterRowBuilder.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Added `Daily Rate` column immediately after `Rate per hour`
- Value = hourly rate × salary `hours_per_day` (same formula as payslip)
- Shifted remaining Excel letters through `CF`

## 12:35 — Gov contribution reports: digits-only ID numbers

**Why:** PhilHealth / Pag-IBIG / SSS (and Pag-IBIG TIN) should show without hyphens on-screen and in PDF/Excel exports.

**Files:**
- `app/Services/Reports/PhilhealthContributionReportService.php`
- `app/Services/Reports/PagibigContributionReportService.php`
- `app/Services/Reports/SssContributionReportService.php`

**Summary:**
- Report output uses `GovernmentIdNumbers::normalize()` (digits only) instead of hyphenated `format()`
- Applies to preview, PDF, and Excel for SSS, PhilHealth, and Pag-IBIG contribution reports

## 12:40 — Payroll Register PDF: fit wide columns

**Why:** Exporting Payroll Register to PDF on A4 landscape clipped/overlapped ~84 columns.

**Files:**
- `app/Support/ReportPdfDownload.php`
- `resources/views/payroll/reports/pdf-document.blade.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `resources/views/payroll/reports/_icct-payroll-register-table.blade.php`
- `tests/Unit/ReportPdfDownloadTest.php`

**Summary:**
- Payroll Register PDF now uses a custom wide page (scaled to column count) instead of squeezed A4 landscape
- Smaller PDF font, nowrap cells, and auto table width to avoid overlap/cutoff
- Added unit coverage for wide register PDF generation

## 13:19 — Payroll Register: remove red-marked columns

**Why:** Annotated Payroll Register Excel marked unused/legacy columns in red; remove them from on-screen, PDF, and Excel generation.

**Files:**
- `config/payroll_register_layout_columns.json`
- `config/payroll_register_layout.php`
- `tests/Unit/PayrollRegisterRowBuilderTest.php`

**Summary:**
- Removed 41 red-highlighted columns (Payroll Period, Pay Date, adj/teaching filler columns, duplicate GROSS/Philhealth Gross, Total Deductions, etc.)
- Kept Daily Rate and remaining active ICCT columns (now 43 columns through `AQ`)
- Applies to preview, PDF, and Excel via shared layout config

## 13:45 — Add Greenhills / N. Domingo / Washington / 108 / 225 campuses

**Why:** New campuses must appear in Campuses maintenance and Timelogs DTR upload dropdown.

**Files:**
- `database/seeders/data/skolaris_campuses_programs.json`
- `config/time_logs_dtr.php`
- `app/Services/ReferenceDataBootstrapService.php`

**Summary:**
- Seeded campuses: `GH` Greenhills, `ND` N. Domingo, `WR` Washington Residences, `O8` 108, `F6` 225 6th Floor
- Added to Timelogs DTR using Cainta Timesheet Report (`.xls`) format
- Desktop/reference bootstrap expects the new campus codes

## 14:54 — Salary: Is Above minimum wage earner

**Why:** Track per-employee-salary flag for minimum wage status in master file and uploads (including hybrid = one checkbox per salary).

**Files:**
- `database/migrations/2026_08_06_145200_add_is_above_minimum_wage_earner_to_employee_salaries.php`
- `app/Models/EmployeeSalary.php`
- `app/Models/Employee.php`
- `app/Services/EmployeeSalarySync.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `config/employee_upload.php`
- `config/employee_salary_upload.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/views/employees/partials/_employee-salary.blade.php`
- `resources/views/employees/partials/_employee-salary-previous-detail.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `resources/templates/employee_upload_template.xlsx`
- `resources/templates/employee_salary_upload_template.xlsx`

**Summary:**
- Added `is_above_minimum_wage_earner` boolean on `tbl_employee_salaries` (default false)
- Checkbox on each salary panel (hybrid gets one per Faculty/Staff salary)
- Included in employee upload (`salary_*` / `salary2_*`) and salary upload templates
- Shown on employee view and previous salary detail; sync/history compares the flag
- Regenerated master-file and salary upload `.xlsx` templates

## 15:48 — BIR Employees' Tax Withheld report

**Why:** Add payroll report matching ICCT `tax` worksheet: MWE vs above-minimum-wage placement, taxable No/With W/T, tax withheld, and deminimis benefits.

**Files:**
- `app/Support/BirTaxWithheldClassifier.php`
- `app/Services/Reports/BirTaxWithheldReportService.php`
- `config/payroll_reports.php`
- `database/seeders/ReportSeeder.php`
- `database/seeders/PayrollMaintenanceSeeder.php`
- `resources/views/payroll/reports/options/bir-tax.blade.php`
- `resources/views/payroll/reports/_bir-tax-withheld-table.blade.php`
- `resources/views/payroll/reports/_preview.blade.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `app/Support/ReportPdfDownload.php`
- `tests/Unit/BirTaxWithheldClassifierTest.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- New report **BIR Employees' Tax Withheld** (preview / Excel / PDF)
- Unticked `is_above_minimum_wage_earner` → MWE Income (+ Non Taxable Overtime); if over TRAIN monthly exempt (₱20,833) → Taxable Inc. with W/T
- Ticked → Taxable Inc. No W/T until exempt threshold, then with W/T + Tax Withheld (WHTX)
- De minimis Benefit from income types named Deminimis (seeded `DEMN` Allowance type); also falls back to salary setup amounts
- Same pay month/year multi-batch select as other contribution reports

## 16:04 — BIR Form 1601-C and 2316 reports

**Why:** Add official-like BIR 1601-C (monthly remittance) and 2316 (per-employee certificate) payroll reports with employee multi-select before generation.

**Files:**
- `config/bir_forms.php`
- `config/payroll_reports.php`
- `.env.example`
- `app/Services/Reports/BirFormEmployeeSelection.php`
- `app/Services/Reports/Bir1601cReportService.php`
- `app/Services/Reports/Bir2316ReportService.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `app/Support/ReportPdfDownload.php`
- `database/seeders/ReportSeeder.php`
- `resources/views/payroll/reports/options/_batch-employee-picker.blade.php`
- `resources/views/payroll/reports/options/bir-1601c.blade.php`
- `resources/views/payroll/reports/options/bir-2316.blade.php`
- `resources/views/payroll/reports/_bir-1601c-document.blade.php`
- `resources/views/payroll/reports/_bir-2316-document.blade.php`
- `resources/views/payroll/reports/_preview.blade.php`
- `resources/views/payroll/reports/_report-document-body.blade.php`
- `resources/views/payroll/reports/index.blade.php`
- `resources/js/app.js`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Two new **Posted Batch Reports**: BIR Form 1601-C and BIR Form 2316
- Before generate: pick posted payroll batch + multi-select employees (same UX as Payslip)
- 1601-C: employer header + schedule of payees (TIN, ATC WI010, tax base, tax withheld)
- 2316: one certificate per selected employee (employer/employee/compensation sections)
- Preview, Excel, and PDF supported; amounts from posted batch incomes + WHTX / statutory deductions
- Company TIN / RDO / signatory via `BIR_*` env keys (`config/bir_forms.php`)

## 16:36 — BIR 2316 official form layout

**Why:** Match Pulse BIR Form 2316 preview/PDF to the official September 2021 (ENCS) certificate layout from the provided sample.

**Files:**
- `app/Support/Bir2316FormMapper.php`
- `app/Services/Reports/BirFormEmployeeSelection.php`
- `app/Services/Reports/Bir2316ReportService.php`
- `config/bir_forms.php`
- `.env.example`
- `resources/views/payroll/reports/_bir-2316-document.blade.php`
- `resources/views/payroll/reports/pdf-document.blade.php`
- `tests/Unit/Bir2316FormMapperTest.php`

**Summary:**
- Rebuilt 2316 as two-column official form (Part I–III + IVA left; Part IV-B right; declaration + substituted filing footer)
- Numbered items 1–54 with TIN/ZIP/DOB boxes, MWE checkbox, Main Employer mark
- Amounts mapped from posted-batch incomes (BASC/OVRT/13TH/DEMN) + statutory deductions + WHTX
- SMW day/month rates via `BIR_SMW_*` env; PDF uses tight A4 portrait margins

## 16:50 — Copy PATHS blank BIR PDF templates into Pulse

**Why:** Prepare PATHS-style BIR form generation (official blank PDF + field stamp) by bringing templates local.

**Files:**
- `resources/bir-forms/BIR2316.pdf`
- `resources/bir-forms/BIR1601c.pdf`

**Summary:**
- Copied from `paths-mvc/common/reports/` (read-only reference) into Pulse
- Source files unchanged in paths-mvc

## 17:08 — BIR Forms Setup module under Payroll

**Why:** Give HR a Payroll Settings screen to configure employer/signatory/SMW details used by BIR 1601-C and 2316, instead of env-only defaults.

**Files:**
- `database/migrations/2026_08_06_170200_create_tbl_bir_form_settings_table.php`
- `app/Models/BirFormSetting.php`
- `app/Support/BirFormSettings.php`
- `app/Http/Controllers/BirFormSettingsController.php`
- `app/Policies/BirFormSettingsPolicy.php`
- `app/Providers/AppServiceProvider.php`
- `resources/views/payroll/bir-forms/index.blade.php`
- `resources/views/partials/sidebar-icon.blade.php`
- `routes/web.php`
- `database/seeders/SubModuleSeeder.php`
- `app/Services/Reports/Bir1601cReportService.php`
- `app/Services/Reports/Bir2316ReportService.php`

**Summary:**
- New submodule **BIR Forms Setup** (`payroll.bir-forms.*`) before **Reports** (sort 6 / Reports → 7)
- Singleton table stores company name/address/TIN/RDO/ZIP, signatory, ATC, SMW day/month
- Reports resolve settings via `BirFormSettings` (DB overrides `config/bir_forms.php` / env)
- Soft deletes on settings model; save writes `sys_logs`

## 17:18 — BIR 2316 options use Payroll Year (annual sum)

**Why:** Form 2316 is annual — HR selects a payroll year and employees; amounts come from all posted batches in that year, not a single batch.

**Files:**
- `resources/views/payroll/reports/options/bir-2316.blade.php`
- `config/payroll_reports.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `routes/web.php`
- `app/Services/Reports/BirFormEmployeeSelection.php`
- `app/Services/Reports/ReportBatchOptionsService.php`
- `app/Services/Reports/Bir2316ReportService.php`
- `resources/js/app.js`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- Options UI: **Payroll Year** + employee multi-select (employees with posted payroll in that year)
- New `payroll.reports.year-employees` JSON endpoint
- `resolveForYear()` sums incomes/deductions across all posted batches for the year (Jan 1–Dec 31 period)
- 1601-C / Payslip still use posted batch picker

## 17:36 — Fix BIR 1601-C signatory (#27) stamp position

**Why:** Signatory name overlapped the printed role labels under the signature line in box 27.

**Files:**
- `app/Services/Reports/Bir1601cOfficialPdfWriter.php`

**Summary:**
- Raised name stamp (`Y=257`) with PATHS-like cell size (`74×3`, font 8) so it sits clear of the role labels
- Title/Position moved to PATHS field coords (`Y=278`, `X=16`) instead of mid-box overlap

## 17:39 — Lower BIR 1601-C #27 signatory name

**Why:** After the previous raise, the printed name sat too high and clipped into item 26.

**Files:**
- `app/Services/Reports/Bir1601cOfficialPdfWriter.php`

**Summary:**
- Restored PATHS signatory name position (`Y=265`) on the signature line

## 17:52 — Desktop installer update check from S3

**Why:** Desktop clients should detect a newer installer under `payroll_installer/` on `skolaris-payroll-backups-prod` and prompt download when `NATIVEPHP_APP_VERSION` is behind.

**Files:**
- `config/desktop_updater.php`
- `app/Services/DesktopInstallerUpdateService.php`
- `app/Http/Middleware/EnsureDesktopInstallerUpdate.php`
- `app/Http/Controllers/DesktopInstallerUpdateController.php`
- `app/Console/Commands/UploadDesktopInstallersCommand.php`
- `bootstrap/app.php`
- `routes/web.php`
- `resources/views/partials/desktop-installer-update-modal.blade.php`
- `resources/views/layouts/app.blade.php`
- `resources/views/layouts/guest.blade.php`
- `scripts/build-desktop.sh`
- `.env.example`
- `tests/Unit/DesktopInstallerUpdateConfigTest.php`

**Summary:**
- Reuses `DB_BACKUP_S3_*` credentials; prefix `payroll_installer/`
- After a successful desktop build, uploads matching EXE/DMG + `latest.json`
- Desktop (login + app) shows update modal when S3 version &gt; installed; Download (pre-signed URL) or Remind me later
- Platform-aware: Windows → `Pulse-*-setup.exe`, macOS arm64 → `Pulse-*-arm64.dmg`

## 18:12 — Update modal notes DB is preserved on upgrade

**Why:** Clarify for HR that installing a newer desktop build keeps local SQLite and only applies pending migrate/bootstrap.

**Files:**
- `resources/views/partials/desktop-installer-update-modal.blade.php`

**Summary:**
- Modal copy states payroll data is kept; new version auto-runs pending migrations/module sync (no wipe)

## 18:15 — S3 installer prefix keeps only current version

**Why:** Avoid accumulating old installers under `payroll_installer/`; each upload replaces the prior set.

**Files:**
- `app/Services/DesktopInstallerUpdateService.php`
- `app/Console/Commands/UploadDesktopInstallersCommand.php`
- `config/desktop_updater.php`

**Summary:**
- After uploading the new EXE + DMG (+ `latest.json`), delete other `Pulse-*` objects in the prefix
- One version remains (both platforms for that version)

## 18:20 — Desktop build 0.1.46 (mac + Windows + S3)

**Why:** Ship all pending Pulse changes (BIR Forms Setup, 2316 payroll year, 1601-C signatory fix, desktop installer update check) as paired installers and publish to S3.

**Version:** `0.1.46`

**Outputs:**
- `dist/Pulse-0.1.46-arm64.dmg`
- `dist/Pulse-0.1.46-setup.exe`
- S3 `payroll_installer/Pulse-0.1.46-setup.exe`
- S3 `payroll_installer/Pulse-0.1.46-arm64.dmg`
- S3 `payroll_installer/latest.json`

**Summary:**
- Bumped `NATIVEPHP_APP_VERSION` to 0.1.46
- Full `build-desktop.sh all` succeeded; S3 upload completed (single-version prefix)

## 18:38 — Force desktop installer update (no Remind me later)

**Why:** Users must download the newer S3 installer; postponing is not allowed.

**Files:**
- `resources/views/partials/desktop-installer-update-modal.blade.php`
- `app/Services/DesktopInstallerUpdateService.php`
- `app/Http/Controllers/DesktopInstallerUpdateController.php`
- `routes/web.php`
- `config/desktop_updater.php`
- `.env.example`

**Summary:**
- Modal is required-update only (Download button); dismiss route/UI removed
- Overlay blocks UI until they download/install the new version

## 18:50 — Desktop build 0.1.47 + forced update + stable S3 names

**Why:** Ship force-update modal; publish one EXE/DMG set via overwrite (IAM has no DeleteObject).

**Version:** `0.1.47`

**Outputs:**
- `dist/Pulse-0.1.47-arm64.dmg`
- `dist/Pulse-0.1.47-setup.exe`
- S3 `payroll_installer/Pulse-setup.exe` (stable overwrite)
- S3 `payroll_installer/Pulse-arm64.dmg` (stable overwrite)
- S3 `payroll_installer/latest.json` → version 0.1.47

**Summary:**
- No “Remind me later”; update modal is required
- S3 uses fixed object names so each release overwrites the previous installer
- Legacy versioned keys may remain if DeleteObject is denied; clients read `latest.json`

## 19:12 — Desktop installer check on every request

**Why:** After a new S3 upload, clients should detect the newer installer immediately (not after a multi-hour cache).

**Files:**
- `config/desktop_updater.php`
- `app/Services/DesktopInstallerUpdateService.php`
- `.env.example`

**Summary:**
- `DESKTOP_INSTALLER_CHECK_INTERVAL_MINUTES=0` → read `latest.json` on every desktop page load
- Previous default was 360 minutes with a 5-minute minimum floor

## 19:19 — Desktop build 0.1.48 (every-request S3 check)

**Why:** Ship immediate installer detection after S3 upload.

**Version:** `0.1.48`

**Outputs:**
- `dist/Pulse-0.1.48-arm64.dmg`
- `dist/Pulse-0.1.48-setup.exe`
- S3 `payroll_installer/Pulse-setup.exe` / `Pulse-arm64.dmg` / `latest.json` → 0.1.48

**Summary:**
- Clients on 0.1.48+ check `latest.json` on every page load
- Forced update modal; stable S3 overwrite names

## 19:28 — Re-upload 0.1.48 as versioned S3 object names

**Why:** Console listing should show `Pulse-0.1.48-*`; users thought the build was not uploaded because only stable generic names were visible.

**Files:**
- `config/desktop_updater.php`
- `app/Services/DesktopInstallerUpdateService.php`
- `app/Console/Commands/UploadDesktopInstallersCommand.php`

**Summary:**
- Uploads use versioned keys again (`Pulse-0.1.48-setup.exe`, `Pulse-0.1.48-arm64.dmg`)
- `latest.json` → 0.1.48 pointing at those keys
- Older objects remain without `s3:DeleteObject` (manual cleanup in AWS console)

## 19:34 — Desktop build 0.1.49 (mac + Windows + S3)

**Why:** New paired desktop release with versioned S3 object names.

**Version:** `0.1.49`

**Outputs:**
- `dist/Pulse-0.1.49-arm64.dmg`
- `dist/Pulse-0.1.49-setup.exe`
- S3 `payroll_installer/Pulse-0.1.49-setup.exe`
- S3 `payroll_installer/Pulse-0.1.49-arm64.dmg`
- S3 `payroll_installer/latest.json` → 0.1.49














