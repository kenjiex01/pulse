# Changelog — 2026-07-08

## 19:40 — Attendance View table pagination

**Why:** The Employee Profile → Attendance View table showed all logs at once; user requested pagination.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/js/app.js`

**Summary:**
- Wrapped the attendance table in a `data-client-paginate` container (10 rows/page) with Previous/Next controls and a "Showing X–Y of N" indicator.
- Added a reusable `initClientPagination()` client-side paginator in `app.js`; initializes on load (inline view modal) and after lazy panel loads (setup form attendance tab).
- Controls hide automatically when there is only one page; no server round trip (rows already loaded, capped at 50).
- Rebuilt frontend assets (`npm run build`).

## 19:41 — Attendance View sorted by date ascending

**Why:** Logs were showing newest-first (Feb 27 before Feb 2); user wants chronological order (earliest date first).

**Files:**
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`

**Summary:**
- Changed both attendance queries (inline view modal + lazy controller) from `orderByDesc` to `orderBy` on `dt_datetime` / `timekeeping_inandout_id`.
- Note: with the 50-row cap, the table now shows the earliest 50 logs in ascending order.

## 19:42 — Attendance View defaults to last page

**Why:** With ascending date order, opening the tab should land on the most recent logs (last page), not the oldest.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `data-paginate-start="last"` on the attendance paginate container.
- `initClientPagination()` now honors `data-paginate-start`; starts on the last page when set, otherwise page 1.
- Rebuilt frontend assets (`npm run build`).

## 19:44 — Numbered page buttons with sliding window

**Why:** User wants numbered pagination (1 2 3 4 5 …) that slides as pages change, showing max 5 numbers.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `data-paginate-pages` container between Previous/Next.
- `initClientPagination()` renders up to 5 numbered page buttons in a sliding window centered on the current page, with leading/trailing `…` when there are more pages; active page highlighted.
- Clicking a number jumps to that page; window recenters on navigation.
- Rebuilt frontend assets (`npm run build`).

## 19:45 — Employee Load tab uses same client-side pagination

**Why:** User wants the Employee Load tab to use the same numbered pagination as Attendance View.

**Files:**
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`

**Summary:**
- Switched Employee Load from server-side `paginate()` to `->limit(500)->get()` so rows render for client-side pagination.
- Wrapped the table in `data-client-paginate` (10/page, `data-paginate-start="last"`) with `data-paginate-row` rows and the shared numbered controls (Previous / 1 2 3 4 5 … / Next).
- Reused the existing `initClientPagination()` (already invoked after lazy panel load) — no JS changes needed.
- Removed the old `partials.data-table-pagination` include for this tab; `->total()` checks changed to `->isNotEmpty()`.

## 19:57 — Hours column on Late / Undertime deductions

**Why:** User needs to see how many hours of late/undertime produced each deduction amount in Employee Payroll Detail.

**Files:**
- `database/migrations/2026_07_08_195700_add_hours_to_payroll_deductions.php`
- `app/Models/PayrollDeduction.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`

**Summary:**
- Added nullable `hours` (decimal 10,4) on `trn_payroll_deductions`.
- On process/reprocess, Late (`LTDE`) and Undertime (`UTDE`) lines now store `minutes / 60`.
- Deductions tab shows an Hours column next to Description; only Late/Undertime display a value — other deductions show `—`.
- Existing processed batches need **Re-process** to backfill hours.

## 20:00 — Server-side pagination for Attendance View and Employee Load

**Why:** Client-side pagination loaded all rows at once; with many logs this slows the UI. Load only the current page from the server.

**Files:**
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-employee-load-view.blade.php`
- `resources/views/partials/numbered-pagination.blade.php`

**Summary:**
- Attendance View and Employee Load now use Laravel `paginate(10)` (server-side); only the active page is fetched.
- Default page is the **last** page (newest dates at end of ascending sort).
- Shared `numbered-pagination` partial: Previous / sliding 5 page numbers / Next; page clicks AJAX-reload via existing lazy panel + `data-live-table-page`.
- View modal Attendance tab is lazy-loaded (same as Employee Load) instead of inline `limit(50)` dump.

## 20:03 — Fix full-page loader stuck on Attendance pagination

**Why:** Clicking numbered pagination showed the SKOLARIS full-screen "Loading..." forever because the global `<a>` click handler treated AJAX page links as full navigation.

**Files:**
- `resources/js/app.js`

**Summary:**
- Skipped `PulseLoader` for links with `data-live-table-page` or inside `[data-employee-profile-lazy-content]`.
- Rebuilt frontend assets (`npm run build`).

## 20:04 — Keep modal open when paginating Attendance / Employee Load

**Why:** Pagination clicks inside the Employee Profile modal were handled by the parent live-table and reloaded the employee list (full page), closing the modal.

**Files:**
- `resources/js/app.js`

**Summary:**
- Live-table click handler now ignores `data-live-table-page` links inside `[data-employee-profile-lazy-content]`.
- Employee Profile pagination handler uses capture phase + `stopPropagation` so it AJAX-loads the tab only and keeps the modal open.
- Rebuilt frontend assets (`npm run build`).

## 20:07 — Fix Pag-IBIG zero when gross ≥ ₱5,000

**Why:** Batch 0005 (Feb 16–28) had no PIBG line even though Pag-IBIG was scheduled; gross taxable (~₱9,871) fell outside the old bracket logic and returned ₱0.

**Files:**
- `app/Services/GovernmentDeductionPayrollService.php`

**Summary:**
- `computePagibig()` now uses MSC up to the highest `salary_cap` (₱5,000) and applies that row’s percent when gross ≥ cap, instead of returning zero.
- Below ₱1,500: 2%/1%; ₱1,500–₱5,000+: 2%/2% on min(gross, ₱5,000) → max ₱100 EE / ₱100 ER.
- Re-seeded `GovernmentTablesSeeder` so contribution rates are percent values (2.00), not 200.00.
- Re-process the batch to create the missing Pag-IBIG line.

## 20:09 — Pag-IBIG contributions are peso amounts (not %)

**Why:** User had already set Pag-IBIG table values as peso amounts; an earlier `GovernmentTablesSeeder` re-run overwrote them back to `2.00` / `1.00` (legacy % values). Computation was also treating the column as percent of gross.

**Files:**
- `app/Services/GovernmentDeductionPayrollService.php`
- `database/seeders/GovernmentTablesSeeder.php`
- `config/government_tables.php`

**Summary:**
- `computePagibig()` now uses table `employee_contribution` / `employer_contribution` as **fixed peso amounts** for the matching salary_cap bracket (no ÷100).
- Seeder uses `firstOrCreate` only — **will not overwrite** existing Pag-IBIG rows on re-seed; default seed amounts are ₱100 / ₱100.
- Restored DB rows to ₱100 EE / ₱100 ER.
- UI labels: Employee/Employer Amount.
- Re-process payroll batch to apply Pag-IBIG amount.

## 20:16 — SSS / PhilHealth / WHT use whole-month gross

**Why:** User wants PhilHealth, SSS, and Withholding Tax based on the employee's total gross across all payroll batches that share the same calendar month + pay year (e.g. both Feb 1–15 and Feb 16–28 in 2026), not just the current period's gross.

**Files:**
- `app/Services/GovernmentDeductionPayrollService.php`

**Summary:**
- Added `monthlyGrossTaxable()` — sums taxable incomes for the employee on batches whose calendars match `pay_year` + `calendar_month`.
- PhilHealth, SSS, and WHT now compute from that monthly total; Pag-IBIG still uses the current period gross (fixed peso bracket).
- WHT looks up the **monthly** withholding table when using monthly gross.
- Example for 2026-00003 Feb 2026: period ~₱9,871.67 → monthly ~₱19,743.34; SSS/PHIL brackets rise accordingly.
- Re-process the batch to refresh deduction amounts.

## 20:24 — Unpost button per posted batch row

**Why:** Unpost Batches tab listed posted batches but had no action to unpost them.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/_unpost-batches-results.blade.php`

**Summary:**
- Added `canUnpostBatch()` / `unpostBatch()` — sets status back to PROCESSED and clears `posted_by_id` / `dt_posted`.
- New `payroll.transaction.unpost` route + controller method with SysLog.
- Per-row **Unpost** button with confirm dialog on the Unpost Batches table.

## 20:25 — Hide Posted batches from Payroll Batches list

**Why:** Posted batches belong on the Unpost Batches tab only; they should not appear under Payroll Batches.

**Files:**
- `app/Http/Controllers/PayrollTransactionController.php`

**Summary:**
- Payroll Batches tab query now excludes `payroll_batch_status_id = POSTED`.
- Unpost Batches tab remains the only list of posted batches.

## 20:27 — Fix stuck loader when cancelling Unpost confirm

**Why:** Clicking Cancel on the Unpost confirm still showed the SKOLARIS full-page loader forever because the global submit handler ran anyway.

**Files:**
- `resources/js/app.js`
- `resources/views/payroll/transaction/_unpost-batches-results.blade.php`

**Summary:**
- Confirm dialog for Unpost uses `data-confirm-submit` with a capture-phase handler that cancels submit cleanly.
- Global PulseLoader is skipped when `event.defaultPrevented` (Cancel).
- Rebuilt frontend assets (`npm run build`).

## 20:30 — After Post, go to Unpost Batches (not open Posted modal)

**Why:** Re-posting an unposted batch redirected back to Payroll Batches with the view modal open, so the Posted batch still appeared with the locked banner.

**Files:**
- `app/Http/Controllers/PayrollTransactionController.php`

**Summary:**
- Successful `postBatch` now redirects to **Unpost Batches** (where Posted batches live).
- Opening a Posted batch via `view_payroll_batch` on the Payroll Batches tab redirects to Unpost Batches instead of showing the locked modal there.

## 21:00 — Apply uploaded deductions to payroll batch details

**Why:** Deduction uploads were saved in `raw_payroll_deductions` but never copied into `trn_payroll_deductions`, so Employee Payroll Detail did not show them (e.g. LTDE ₱700).

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`

**Summary:**
- `applyUploadedDeductionsToDetail()` merges raw calendar uploads into the batch detail during process/reprocess.
- `applyRawUploadToOpenBatches()` applies a newly committed upload immediately to PROCESSED (or editable) batches for the same pay period.
- If `emp_amount`/`empr_amount` are blank, `amount` is used as the employee share (matches user's upload with only Amount filled).
- Existing upload batch #2 for 2026-00003 was applied to batch 0005 (extra LTDE ₱700 line).

## 21:05 — Hours column on Deduction upload template (Late / Undertime)

**Why:** User wants Late/Undertime deduction uploads to include hours so they appear in Employee Payroll Detail Hours column.

**Files:**
- `database/migrations/2026_07_08_210300_add_hours_to_raw_payroll_deductions.php`
- `config/payroll_transaction.php`
- `app/Models/RawPayrollDeduction.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`

**Summary:**
- Added `hours` field to Deductions upload template (after Deduction Type Code), with hint that it is required for LTDE/UTDE.
- Validation requires hours > 0 when deduction type is Late or Undertime; blank OK for other types.
- Stored on `raw_payroll_deductions.hours` and copied to `trn_payroll_deductions.hours` when applied to the batch.
- Upload Batch detail view shows an Hours column.

## 21:06 — Sum same deduction types into one row (Late / etc.)

**Why:** Multiple Late (LTDE) lines from time logs + upload showed as separate rows; user wants one summed Late row in Employee Payroll Detail.

**Files:**
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-net-pay.blade.php`

**Summary:**
- Deductions tab groups by `deduction_type_id` and sums hours, employee amount, and employer share into a single display row.
- Net Pay breakdown uses the same grouped rows so Late appears once with the combined employee amount.
- DB rows remain separate (audit/reprocess safe); display-only consolidation.

## 21:13 — Hide Hours Worked / Leaves / Loans upload tabs

**Why:** User asked to remove Hours Worked, Leaves, and Loans from Upload Adjustments sub-tabs.

**Files:**
- `config/payroll_transaction.php`

**Summary:**
- Removed `hours-worked`, `leaves`, and `loans` from `upload_types` so the Upload Adjustments nav only shows Incomes and Deductions.
- Existing raw upload tables/service branches for those types are left in place but unreachable from the UI.

## 21:19 — Seed Pag-IBIG table from current Government Tables data

**Why:** Persist the on-screen Pag-IBIG rows (₱200 / ₱200) as the default seeder values.

**Files:**
- `database/seeders/GovernmentTablesSeeder.php`

**Summary:**
- Updated Pag-IBIG seed to salary caps 1,500 and 5,000 with employee/employer amounts of 200.00 each.
- Switched from `firstOrCreate` to `updateOrCreate` so re-seed refreshes these official amounts.
- Ran `GovernmentTablesSeeder` successfully.


