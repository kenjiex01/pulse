## 13:50 — Pulse features tracker spreadsheet

**Why:** Provide a shareable Excel inventory of implemented Pulse modules and features.

**Files:**
- `docs/pulse-features-tracker.xlsx`
- `scripts/generate-pulse-features-tracker.php`

**Summary:**
- 53 feature rows across Administration, Human Resource, Payroll, and Timekeeping.
- Columns: Item, Module, Feature / Task, Description, Type, Priority, Status, Access, Assigned To.
- Regenerate with `php scripts/generate-pulse-features-tracker.php`.

## 15:55 — Payroll Reports submodule with Payroll Register

**Why:** Add database-driven report definitions and port PATHS payroll register generation into Pulse.

**Files:**
- `database/migrations/2026_07_21_150000_create_report_tables.php`
- `app/Models/ReportClassification.php`, `ReportGroup.php`, `Report.php`, `ReportFileType.php`
- `database/seeders/ReportSeeder.php`
- `config/payroll_reports.php`
- `app/Support/PayrollReportsModule.php`
- `app/Policies/PayrollReportsPolicy.php`
- `app/Http/Controllers/PayrollReportsController.php`
- `app/Services/Reports/PayrollRegisterReportService.php`
- `app/Services/Reports/ReportBatchOptionsService.php`
- `resources/views/payroll/reports/**`
- `resources/js/app.js`
- `routes/web.php`
- `database/seeders/SubModuleSeeder.php`
- `tests/Feature/PayrollReportsTest.php`

**Summary:**
- New Payroll → Reports submodule with report classification dropdown (Payroll active first).
- Report types stored in `lu_report_classifications`, `tbl_report_groups`, and `tbl_reports`.
- Report type onChange loads dynamic option fields via AJAX (Payroll Register: batches, add details, group/sort, output).
- Generates on-screen preview or Excel export from processed payroll batch data.

## 16:10 — Report on-screen preview opens in new tab

**Why:** Keep the reports form open while viewing generated output.

**Files:**
- `app/Http/Controllers/PayrollReportsController.php`
- `resources/views/payroll/reports/preview.blade.php`
- `resources/views/payroll/reports/index.blade.php`
- `resources/js/app.js`

**Summary:**
- HTML preview renders on a dedicated preview page instead of inline on the index.
- Form submits with `target="_blank"` when output is on-screen preview; Excel download stays in the same tab.

## 16:15 — Round withholding tax table values to whole numbers

**Why:** Tax table amounts should display and save without centavos across all pay frequencies.

**Files:**
- `app/Support/GovernmentTables.php`
- `app/Http/Controllers/GovernmentTablesController.php`
- `database/seeders/GovernmentTablesSeeder.php`
- `resources/views/payroll/government-tables/_wtax2023-annual-results.blade.php`
- `resources/views/payroll/government-tables/_form-wtax-annual.blade.php`
- `tests/Unit/GovernmentTablesWtaxRoundingTest.php`

**Summary:**
- Added `roundWtaxValue`, `formatWtaxGridValue`, `normalizeWtaxGridColumn`, and `normalizeAnnualWtaxAttributes` helpers.
- Grid load/save and annual create/update now round Tax Amount, Plus (%), and over thresholds to whole numbers.
- Seeder re-applies rounded values; annual top bracket `income_to` capped at 99,999,999 to fit column precision.
- Annual table display and form inputs use whole-number formatting (`step="1"`).

## 16:20 — Shift code Break In/Out optional labels

**Why:** Break window fields on the shift code form should be optional and use clearer labels.

**Files:**
- `app/Support/ShiftCode.php`
- `resources/views/timekeeping/shift-codes/_form.blade.php`
- `resources/views/timekeeping/shift-codes/_results.blade.php`
- `resources/js/app.js`
- `tests/Unit/ShiftCodeValidationTest.php`

**Summary:**
- Renamed form labels from Time In/Time Out to Break In/Break Out.
- Removed required validation, HTML `required`, and flexi-time disable/clear behavior for those fields.
- Empty values save as `00:00`; list shows em dash when unset.

## 16:30 — Employee salary history with effectivity dates

**Why:** Support current/previous salary records and apply mid-period salary changes in payroll computation.

**Files:**
- `database/migrations/2026_07_21_162500_add_salary_effectivity_history.php`
- `app/Models/EmployeeSalary.php`
- `app/Models/EmployeeEmploymentInformation.php`
- `app/Services/EmployeeSalaryResolverService.php`
- `app/Services/EmployeeSalarySync.php`
- `app/Services/EmployeeLoadPayrollService.php`
- `app/Services/TimeLogsPayrollService.php`
- `app/Services/PayrollBatchService.php`
- `resources/views/employees/partials/_employee-salary*.blade.php`
- `resources/js/app.js`
- `tests/Unit/EmployeeSalaryEffectivityPayrollTest.php`
- `tests/Unit/EmployeeSalaryHistorySyncTest.php`

**Summary:**
- Renamed `date_effective` to `date_effective_from`; added nullable `date_effective_to` (null = current/open).
- Employee Salary tab now has **Current Salary** and **Previous Salary** sub-tabs per employment panel.
- Saving a salary change with a new effectivity date closes the previous record (`date_effective_to = new from - 1 day`) and inserts a new current row.
- Payroll resolves all overlapping salaries for the batch period; TIME-IN/OUT basic pay uses per-day rate (e.g. Jun 1–10 old rate, Jun 11–15 new rate); fixed incomes/deductions are prorated by overlap days.

## 16:32 — Employee bulk upload salary effectivity columns

**Why:** Bulk upload template should match salary effectivity from/to fields used in the employee form.

**Files:**
- `config/employee_upload.php`
- `app/Services/EmployeeUploadRowMapper.php`
- `app/Services/EmployeeSalarySync.php`
- `scripts/generate-employee-upload-sample.php`
- `docs/samples/employee-upload-sample.csv`
- `docs/samples/employee-upload-sample.xlsx`

**Summary:**
- Renamed upload columns to `salary_date_effective_from` / `salary_date_effective_to` (and salary 2 variants).
- Mapper validates from/to dates; legacy `salary_date_effective` column still accepted as effectivity from.
- Upload saves `date_effective_to` when provided; sample CSV/XLSX regenerated.

## 16:35 — Fix Previous Salary tab click

**Why:** Previous Salary scope tab was not responding to clicks on the Employee Salary form.

**Files:**
- `resources/js/app.js`
- `resources/css/app.css`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`

**Summary:**
- Switched Current/Previous salary tab handling to document-level click delegation so it works reliably on the edit form.
- Gave scope tabs their own CSS classes and higher z-index so they are not covered by the Income/Deduction sub-tab row.
- Rebuilt frontend assets (`npm run build`).

## 16:37 — Salary scope tabs match Income/Deduction tab style

**Why:** Current/Previous Salary looked like plain links instead of proper underline tabs.

**Files:**
- `resources/css/app.css`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/js/app.js`

**Summary:**
- Unified scope tabs (Current/Previous) with Income/Deduction tab styling: dark blue label, light underline when inactive, solid blue underline when active.
- Shared `.employee-salary-tab-bar` wrapper; removed separate scope-tab CSS classes.
- Hid redundant inner "Salary" heading on single-employment panels so only the tab row shows.

## 16:49 — Previous Salary full setup snapshot UI

**Why:** Previous Salary tab should show the complete saved salary setup (income, deduction, general fields), matching PATHS behavior.

**Files:**
- `resources/views/employees/partials/_employee-salary-previous.blade.php`
- `resources/views/employees/partials/_employee-salary-previous-detail.blade.php`
- `resources/js/app.js`
- `resources/css/app.css`
- `app/Http/Controllers/EmployeeController.php`
- `tests/Unit/EmployeeSalaryHistorySyncTest.php`

**Summary:**
- Previous Salary tab now lists historical records; clicking a row shows read-only Income, Deduction, and General setup (effectivity, pay type, rate groups, hourly rate, etc.).
- Eager-loads full previous salary relations on employee edit/show.
- Test confirms closed history rows retain income amounts and general salary fields.
