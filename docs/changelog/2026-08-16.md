# 2026-08-16

## 17:48 — Persist payroll attendance day metrics on Process

**Why:** Attendance View and related reports should read late/OT/undertime/etc. from data saved when a payroll batch is processed, not recompute from punches every time.

**Files:**
- `database/migrations/2026_08_16_174500_create_trn_payroll_attendance_days_table.php`
- `app/Models/PayrollAttendanceDay.php`
- `app/Models/PayrollBatchDetail.php`
- `app/Services/PayrollAttendanceDayPersistenceService.php`
- `app/Services/PayrollBatchService.php`
- `app/Services/EmployeeAttendanceViewService.php`
- `app/Services/PayrollAttendanceDayBreakdownService.php`

**Summary:**
- New `trn_payroll_attendance_days` table snapshots daily basic/OT/late/undertime/ND/etc. per batch detail
- Process / Reprocess writes (and clears) these rows with incomes/deductions
- Attendance View UI + report read saved rows when present; batch LTDE/UTDE/OVRT modals prefer saved days
- Legacy batches without snapshots still fall back to live compute until Process/Reprocess backfills

## 17:58 — Desktop build Pulse 0.1.58

**Why:** Ship attendance-day persistence and current payroll/UI fixes in paired macOS + Windows installers.

**Files:**
- `pulse/.env.example` (`NATIVEPHP_APP_VERSION=0.1.58`)
- `pulse/dist/Pulse-0.1.58-arm64.dmg`
- `pulse/dist/Pulse-0.1.58-setup.exe`

**Summary:**
- Version bumped 0.1.57 → 0.1.58
- Installers built and uploaded to S3 `payroll_installer/`
- Includes `trn_payroll_attendance_days` Process snapshot feature
