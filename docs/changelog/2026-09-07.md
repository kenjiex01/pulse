## 10:05 — Weekly progress report Sep 1–7 / targets Sep 8–14

**Why:** Stakeholder report: last week = payslip email done; this week = pull approved web forms into payroll batch.

**Files:**
- `docs/People360-and-Collector-Progress-and-Target-Report-2026-09-01-to-09-14.md`
- `docs/People360-and-Collector-Progress-and-Target-Report-2026-09-01-to-09-14.docx`
- `docs/generate-progress-report-docx-2026-09-01-to-09-14.py`
- `docs/hris-blueprint-gap-analysis.md`
- `docs/People360-HRIS-Blueprint-Progress.html`
- `docs/generate-hris-blueprint-progress-docx.py`
- `docs/People360-HRIS-Blueprint-Progress.docx`
- `docs/People360-HRIS-Blueprint-Progress-2026-09-04.docx`

**Summary:**
- **Progress (1–7 Sep):** Payslip email via SES (Payroll Transaction → Payslip tab)
- **Targets (8–14 Sep):** Sync approved ESS forms from web; apply to payroll batch
- Removed blueprint / M02 bank-account items from stakeholder weekly report (payslip + forms only)

## 10:15 — Dashboard: biometric collector S3 status by campus

**Why:** Show which campuses uploaded collector logs to S3 today and when logs were last pulled into People360.

**Files:**
- `app/Services/BiometricCollectorDashboardService.php`
- `app/Services/BiometricLogsS3PullService.php`
- `app/Http/Controllers/DashboardController.php`
- `resources/views/dashboard.blade.php`
- `resources/views/dashboard/_biometric-collector-status.blade.php`
- `tests/Feature/DashboardBiometricCollectorStatusTest.php`

**Summary:**
- Dashboard panel reads `biometric_logs/{Y}/{M}/{collector}/` on backup S3 (e.g. skolaris-payroll-backups-prod, ap-southeast-2)
- **Collected today** vs **No upload today** per active campus; **Last pulled** from `raw_timekeeping_transactions` S3 imports
- Unmapped collector folders listed when S3 folder name does not match a campus
- 5-minute cache; link to Time Logs S3 pull when user has access

## 10:45 — Dashboard: show last S3 collector upload per campus

**Why:** Sumulong (and similar campuses) had S3 uploads on Sept 4 but dashboard showed only "Last pulled" from People360 imports — which was empty when logs were not yet imported.

**Files:**
- `app/Services/BiometricCollectorDashboardService.php`
- `resources/views/dashboard/_biometric-collector-status.blade.php`
- `tests/Feature/DashboardBiometricCollectorStatusTest.php`

**Summary:**
- Scan current month S3 prefix for latest collector file timestamp per campus folder
- New column **Last S3 upload (month)** separate from **Last pulled into People360**
- `lastPulledAtByCampus()` also matches transactions via S3 path folder when `campus_id` is null

## 10:55 — Company documents: merge tags in designer palette

**Why:** HR memo templates need dynamic employee and attendance placeholders (name, lates, absences, current date/time).

**Files:**
- `app/Support/CompanyDocumentMergeTagCatalog.php`
- `app/Support/CompanyDocumentElementCatalog.php`
- `app/Services/CompanyDocumentMergeTagService.php`
- `app/Models/CompanyDocumentElement.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `resources/js/company-document-designer.js`
- `resources/views/company-documents/designer.blade.php`
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/company-documents/submissions/_field.blade.php`
- `tests/Unit/CompanyDocumentMergeTagCatalogTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- New **TAGS** tab in Form Elements palette with 11 merge tags
- Tags render as styled chips in designer/preview; sample values in preview until employee context is wired
- `current_date`, `current_time`, `current_datetime` resolve live on submission/preview

## 10:58 — Company document tags show label in design/preview

**Why:** Designer and preview should display tag names (e.g. Employee Full name), not sample values (Juan Dela Cruz).

**Files:**
- `resources/js/company-document-designer.js`

**Summary:**
- Merge tag chips now show the tag label in Build canvas and Preview modal
- Live resolved values remain for submission/generation flows

## 11:05 — Company documents: inline tags inside text fields

**Why:** Template authors need to insert merge tags inside paragraph/text area content alongside typed text.

**Files:**
- `app/Services/CompanyDocumentMergeTagService.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `resources/js/company-document-designer.js`
- `resources/views/company-documents/designer.blade.php`
- `resources/views/company-documents/_preview-content.blade.php`
- `resources/views/company-documents/submissions/_field.blade.php`
- `tests/Unit/CompanyDocumentMergeTagServiceTest.php`

**Summary:**
- Inline syntax: `{{employee_full_name}}` mixed with plain text
- Paragraph, heading, long/short text (default text) support tag insert via properties panel or TAGS palette click
- Design/preview render tag labels as chips inside text; submission keeps editable default text with tokens

## 11:10 — Fix inline tag chips wrapping to new line

**Why:** Tags inside paragraph text dropped below preceding text in canvas/preview due to inline-flex + max-w-full and aggressive word-break.

**Files:**
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `app/Services/CompanyDocumentMergeTagService.php`

**Summary:**
- Inline merge tags use `.cd-inline-merge-tag` (inline-block, nowrap) so tags stay on the same line as adjacent text

## 11:15 — Persist designer signature drawings on save/reload

**Why:** Drawn signatures in the document designer disappeared after page reload because preview state was memory-only.

**Files:**
- `resources/js/company-document-designer.js`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Signature pad state saved in element `settings.signature_preview` when clicking Save
- Restored on designer/preview load from saved template settings

## 11:18 — Preview: clean signature display without pad controls

**Why:** Document Preview modal should show only the saved signature image, not Draw/Upload/Clear buttons or file input text.

**Files:**
- `resources/js/signature-pad.js`
- `resources/js/company-document-designer.js`

**Summary:**
- Readonly preview renders signature as static image (or “No signature” placeholder)
- Interactive signature pad kept only in designer Build canvas

## 11:25 — Paragraph font family, size, and color in designer

**Why:** Template authors need to style paragraph text (font, size, color) for memos and document body copy.

**Files:**
- `app/Support/CompanyDocumentTextStyle.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `resources/js/company-document-designer.js`
- `resources/views/company-documents/submissions/_field.blade.php`
- `tests/Unit/CompanyDocumentTextStyleTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Paragraph Properties panel adds Font dropdown, Size (px), and Color picker
- Styles apply on designer canvas, Document Preview, and submission form
- Font settings persist in element `settings` on Save

## 11:30 — Paragraph bold, italic, underline with save persistence

**Why:** Template authors need to emphasize selected words in paragraph text and keep formatting after Save/reload.

**Files:**
- `app/Support/CompanyDocumentInlineFormatting.php`
- `app/Services/CompanyDocumentMergeTagService.php`
- `app/Http/Controllers/CompanyDocumentDesignerController.php`
- `resources/js/company-document-designer.js`
- `tests/Unit/CompanyDocumentInlineFormattingTest.php`
- `tests/Unit/CompanyDocumentMergeTagServiceTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- Paragraph Properties adds B / I / U toolbar — select text, click to wrap with `<b>`, `<i>`, or `<u>`
- Formatting stored in paragraph `label`, sanitized on save, restored on reload
- Canvas, preview, and submission form render allowed inline formatting safely

## 11:45 — Timekeeping Memo and Memo Setup modules

**Why:** HR needs to find employees with late/undertime/absent violations and send configured Company Document memos, with per-day send tracking.

**Files:**
- `database/migrations/2026_09_07_120000_create_timekeeping_memo_tables.php`
- `app/Models/TimekeepingMemoSetup.php`, `TimekeepingMemoSendLog.php`
- `app/Services/TimekeepingMemoAttendanceService.php`, `TimekeepingMemoSendService.php`
- `app/Http/Controllers/TimekeepingMemoController.php`, `TimekeepingMemoSetupController.php`
- `app/Support/TimekeepingMemo.php`, `TimekeepingMemoSetup.php`
- `app/Policies/TimekeepingMemoPolicy.php`, `TimekeepingMemoSetupPolicy.php`
- `resources/views/timekeeping/memo/**`, `resources/views/timekeeping/memo-setup/**`
- `resources/js/timekeeping-memo.js`
- `database/seeders/SubModuleSeeder.php`, `routes/web.php`, `AppServiceProvider.php`
- `tests/Feature/TimekeepingMemoTest.php`

**Summary:**
- **Memo Setup** — pick one Company Document template each for late, undertime, absent
- **Memo** — date range + violation type + minimum count filters; employee list with campus, count, View/Send, batch send
- View modal lists violation dates, memo-sent status, and date checkboxes (defaults to all if none selected)

## 11:50 — Memo Setup dropdown shows template name only

**Why:** Dropdown labels showed `Name (code)` which looked different from Company Documents cards that display name only.

**Files:**
- `resources/views/timekeeping/memo-setup/index.blade.php`

**Summary:**
- Memo template options now display the same title as Company Documents (e.g. "Absent memo", "test")

## 12:05 — Memo preview before send

**Why:** Users need to review the resolved memo template for an employee before sending.

**Files:**
- `app/Services/TimekeepingMemoPreviewService.php`
- `app/Services/TimekeepingMemoSendService.php`
- `app/Http/Controllers/TimekeepingMemoController.php`
- `app/Support/TimekeepingMemo.php`
- `resources/views/timekeeping/memo/_preview-content.blade.php`, `_preview-modal.blade.php`, `_results.blade.php`, `_details-content.blade.php`, `index.blade.php`
- `resources/js/timekeeping-memo.js`, `resources/js/company-document-designer.js`, `resources/js/app.js`
- `routes/web.php`
- `tests/Feature/TimekeepingMemoTest.php`

**Summary:**
- **Preview** button on Memo list Actions and Violation Details modal
- Opens modal with Company Document canvas preview; merge tags resolved for employee and selected violation dates
- Detail modal respects checked dates; empty selection includes all listed dates (same as send)

## 12:15 — Merge tags for violation dates

**Why:** Memo templates need to list the actual late, undertime, or absent dates—not just counts.

**Files:**
- `app/Support/CompanyDocumentMergeTagCatalog.php`
- `app/Services/CompanyDocumentMergeTagService.php`
- `app/Services/TimekeepingMemoSendService.php`
- `app/Services/TimekeepingMemoPreviewService.php`
- `tests/Unit/CompanyDocumentMergeTagCatalogTest.php`
- `tests/Unit/CompanyDocumentMergeTagServiceTest.php`
- `tests/Feature/CompanyDocumentFormTest.php`

**Summary:**
- New TAGS palette items: **Late dates**, **Undertime dates**, **Absent dates**
- Resolves to formatted date list (e.g. `Aug 11, 2026; Aug 12, 2026`) on memo send/preview when violation type matches
- Designer preview shows sample dates when no employee context

## 12:25 — Memo Setup email subject, body, and CC

**Why:** Each violation type needs configurable email content sent to the employee when a memo is dispatched.

**Files:**
- `database/migrations/2026_09_07_130000_add_email_fields_to_timekeeping_memo_setups.php`
- `app/Models/TimekeepingMemoSetup.php`
- `app/Http/Controllers/TimekeepingMemoSetupController.php`
- `app/Services/TimekeepingMemoEmailService.php`, `TimekeepingMemoSendService.php`
- `app/Mail/TimekeepingMemoMail.php`
- `resources/views/timekeeping/memo-setup/index.blade.php`
- `resources/views/emails/timekeeping-memo.blade.php`
- `tests/Feature/TimekeepingMemoTest.php`
- `tests/Unit/TimekeepingMemoEmailServiceTest.php`

**Summary:**
- Memo Setup — per late/undertime/absent: **Email subject** and **Email body** (required), **Email CC** (optional, comma-separated)
- Merge tags supported in subject/body; resolved on send
- Memo send now emails the employee using the configured subject, body, and CC

## 12:35 — Remove designer dot grid from memo preview

**Why:** Memo preview showed the form-designer alignment dots; preview and email should look clean.

**Files:**
- `resources/css/app.css`

**Summary:**
- Dot grid background limited to Company Document **designer** canvas only
- Preview/read-only canvas uses plain white background

## 14:05 — S3 pull GET redirect to modal

**Why:** Browser back/refresh on `/timekeeping/time-logs/s3-pull` caused 405 because that URL is POST-only.

**Files:**
- `routes/web.php`

**Summary:**
- GET on `timekeeping/time-logs/s3-pull` now redirects to Time Logs tab with S3 pull modal open

## 14:25 — Payslip email body cleanup and PDF password

**Why:** Payslip emails should not expose net pay in the body; attached PDF must be protected with the employee birth date.

**Files:**
- `resources/views/emails/payslip.blade.php`
- `app/Services/PayslipEmailService.php`
- `app/Support/PayslipPdfProtector.php`
- `composer.json`, `composer.lock`
- `tests/Unit/PayslipPdfProtectorTest.php`
- `tests/Feature/PayrollTransactionPayslipTest.php`

**Summary:**
- Removed net pay line from payslip email body; added note that PDF opens with birth date **YYYYMMDD**
- PDF attachment encrypted with employee `birth_date` as password (required before send)

## 14:30 — Memo preview shows template author signature

**Why:** Memo preview showed "No signature" even when the template designer had a saved author signature.

**Files:**
- `app/Services/TimekeepingMemoPreviewService.php`
- `app/Services/TimekeepingMemoSendService.php`
- `app/Services/CompanyDocumentFileService.php`
- `resources/js/company-document-designer.js`
- `tests/Feature/TimekeepingMemoTest.php`
- `tests/Unit/CompanyDocumentFileServiceTest.php`

**Summary:**
- Preview no longer overwrites signature fields with the field label text
- JS keeps `settings.signature_preview` when merging server preview values
- Memo send stores the template author signature image from `signature_preview`

## 14:45 — Include rendered memo document in memo email

**Why:** Memo send email only showed Memo Setup intro text ("test") and omitted the actual memo template content.

**Files:**
- `app/Services/CompanyDocumentMemoRenderService.php`, `CompanyDocumentMemoValueResolver.php`
- `app/Services/TimekeepingMemoSendService.php`, `TimekeepingMemoEmailService.php`, `TimekeepingMemoPreviewService.php`
- `app/Mail/TimekeepingMemoMail.php`
- `resources/views/emails/timekeeping-memo.blade.php`
- `resources/views/emails/partials/company-document-memo.blade.php`, `company-document-memo-field.blade.php`
- `resources/views/timekeeping/memo-setup/index.blade.php`
- `tests/Unit/CompanyDocumentMemoRenderServiceTest.php`, `TimekeepingMemoEmailServiceTest.php`

**Summary:**
- Email now includes rendered memo template (fields, merge tags, signature) below the intro body
- Memo Setup copy clarifies intro body vs full memo document in email

## 15:40 — Memo email sends PDF attachment instead of inline HTML

**Why:** Memo HTML in markdown email showed as raw tags; employee should receive a PDF file like payslip.

**Files:**
- `app/Services/CompanyDocumentMemoRenderService.php`
- `app/Services/TimekeepingMemoEmailService.php`, `TimekeepingMemoSendService.php`
- `app/Mail/TimekeepingMemoMail.php`
- `app/Support/MemoPdfFilename.php`
- `resources/views/emails/timekeeping-memo.blade.php`
- `resources/views/emails/company-document-memo-pdf.blade.php`
- `resources/views/timekeeping/memo-setup/index.blade.php`
- `tests/Unit/CompanyDocumentMemoRenderServiceTest.php`, `TimekeepingMemoEmailServiceTest.php`, `MemoPdfFilenameTest.php`

**Summary:**
- Memo template rendered to PDF via Dompdf and attached to email
- Email body is intro text only; no inline HTML memo block

## 15:40 — Remove payslip PDF password note from email body

**Why:** HR will inform employees about the PDF password separately.

**Files:**
- `resources/views/emails/payslip.blade.php`

**Summary:**
- Removed password-protected / YYYYMMDD instruction from payslip email body
- PDF password protection on attachment unchanged

## 15:42 — Fix memo email Blade parse error

**Why:** Inline `@if/@endif` inside another `@if` caused ParseError on memo send.

**Files:**
- `resources/views/emails/timekeeping-memo.blade.php`

**Summary:**
- Split nested conditionals into separate Blade blocks so memo send email renders correctly

## 15:45 — Fix blank memo PDF attachment

**Why:** Memo PDF opened as empty pages in Gmail; Dompdf did not paint the email-table HTML layout.

**Files:**
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/pdf/company-document-memo.blade.php`
- `resources/views/pdf/company-document-memo-field.blade.php`
- `tests/Unit/CompanyDocumentMemoRenderServiceTest.php`

**Summary:**
- PDF now uses Dompdf-friendly positioned layout (designer `pos_x`/`pos_y`) with DejaVu Sans
- Removed email-table PDF wrapper that rendered as blank pages
- Images/signatures constrained so PDF stays on one page when possible

## 16:10 — Memo PDF stacked layout for proper alignment

**Why:** Absolute designer coordinates overlapped the SAMPLE image, fields, and footer text in the emailed PDF.

**Files:**
- `resources/views/pdf/company-document-memo.blade.php`
- `resources/views/pdf/company-document-memo-field.blade.php`

**Summary:**
- PDF fields now render top-to-bottom by designer `pos_y` / `sort_order` instead of overlapping absolute positions
- Paragraph/footer block and images use consistent bordered/centered styling

## 16:15 — Memo PDF matches preview canvas layout

**Why:** Email PDF used a stacked layout; preview uses designer positions so SAMPLE stamp, fields, and footer must align the same way.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/pdf/company-document-memo-field.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`
- `tests/Unit/CompanyDocumentMemoRenderServiceTest.php`

**Summary:**
- PDF uses same absolute designer coordinates, box sizes, and field styling as memo preview
- Date field defaults to today (`m/d/Y`) when empty, matching preview behavior

## 16:25 — Memo PDF preview parity (scale, images, single page)

**Why:** Emailed memo PDF still did not match modal preview — wrong field positions, SAMPLE stamp overlap, signature/stamp showing as alt text, and a blank second page.

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Support/CompanyDocumentMemoDompdfPreparer.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/pdf/company-document-memo-field.blade.php`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- Scale designer coordinates from 640px preview canvas to A4 PDF width; auto-stack elements without saved positions like JS preview
- Write memo images to temp files and set Dompdf `basePath` so signature + SAMPLE stamp render (not alt-text placeholders)
- Fixed canvas width/height, z-index on overlay images, explicit img dimensions for Dompdf
- Verified on employee `23-LIB250200`: PDF is 1 page with 4 embedded images (was 2 pages before)

## 16:35 — Memo PDF 640px parity (margins + alignment)

**Why:** PDF still misaligned vs preview because coordinates were scaled down to 547px while preview modal rendered wider (~640–860px).

**Files:**
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `resources/views/pdf/company-document-memo-field.blade.php`
- `resources/js/company-document-designer.js`
- `resources/css/app.css`
- `tests/Unit/CompanyDocumentMemoPdfLayoutTest.php`

**Summary:**
- PDF canvas now **640px** with **no coordinate scaling** — same designer `pos_x`/`pos_y` as preview
- Preview canvas locked to 640px in JS + CSS so modal and PDF share one layout width
- Left-aligned canvas (removed centering), matched header/body padding and 14px/18px typography to preview
- Signature box height 120px like preview readonly signature

## 16:40 — Memo preview/PDF single shared canvas HTML

**Why:** Preview used JS layout while PDF used separate Blade — margins/alignments could never match exactly; Dompdf also clipped tall absolute layouts to a blank second page.

**Files:**
- `resources/views/shared/company-document-memo-canvas.blade.php`
- `resources/views/shared/company-document-memo-canvas-field.blade.php`
- `resources/views/timekeeping/memo/_preview-content.blade.php`
- `resources/views/pdf/company-document-memo-document.blade.php`
- `app/Support/CompanyDocumentMemoPdfLayout.php`
- `app/Services/CompanyDocumentMemoRenderService.php`
- `resources/js/timekeeping-memo.js`
- `tests/Feature/TimekeepingMemoTest.php`

**Summary:**
- Modal preview now renders **the same server-side canvas HTML** as the emailed PDF (no JS re-layout)
- `buildForOutput()` scales canvas uniformly to fit one A4 page when needed; preview and PDF always share identical layout numbers
- Improved paragraph height estimate for long absence lists; fixed Dompdf second-page clipping
