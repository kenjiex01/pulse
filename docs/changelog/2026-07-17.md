## 00:00 — Desktop app version badge in sidebar

**Why:** User requested visible installer version in the desktop sidebar header (next to ISKOLARIS Desktop).

**Files:**
- `resources/views/partials/sidebar.blade.php`

**Summary:**
- Shows `v{NATIVEPHP_APP_VERSION}` inline after **ISKOLARIS Desktop** in sidebar subtitle when running inside NativePHP desktop only.
- Reads from `config('nativephp.version')` — hidden on browser/dev `php artisan serve`.

## 00:10 — Desktop build v0.1.16 (10 AM backup + sidebar version)

**Why:** Production-ready build with 10 AM cloud backup schedule, sidebar version label, and recent backup fixes.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.16`)
- `resources/views/partials/sidebar.blade.php`

**Summary:**
- Built `Pulse-0.1.16-arm64.dmg` and `Pulse-0.1.16-setup.exe` with `DB_BACKUP_CLOUD_SCHEDULE_HOUR=10`.
- Sidebar shows `ISKOLARIS Desktop v0.1.16`; cloud backup marker reset button included.
- Installers: `pulse/dist/Pulse-0.1.16-arm64.dmg`, `pulse/dist/Pulse-0.1.16-setup.exe`.

## 00:22 — Hidden SQL restore upload on Database page

**Why:** Admin easter egg to restore a `.sql` backup on desktop without exposing the action in normal UI.

**Files:**
- `app/Services/DatabaseBackupService.php`
- `app/Http/Controllers/DatabaseController.php`
- `resources/views/database/index.blade.php`
- `resources/js/app.js`
- `routes/web.php`
- `tests/Feature/DatabaseSqlUploadTest.php`
- `tests/Unit/DatabaseBackupRestoreTest.php`

**Summary:**
- Hidden **Restore SQL Backup** panel on Database module — reveal by pressing `.` five times (5th held 5 seconds).
- Upload replaces SQLite DB; auto safety backup saved first; admin-only with confirmation checkbox.

## 00:28 — SQL restore on web (MySQL + SQLite)

**Why:** Hidden restore must work in browser dev (`php artisan serve`) as well as desktop, including MySQL local setups.

**Files:**
- `app/Services/DatabaseBackupService.php`
- `app/Http/Controllers/DatabaseController.php`
- `resources/views/database/index.blade.php`
- `resources/js/app.js`
- `tests/Unit/DatabaseBackupRestoreTest.php`

**Summary:**
- `restoreFromSqlFile()` supports **SQLite** (desktop) and **MySQL/MariaDB** (web dev) via `mysql` CLI import.
- Easter egg (`.` × 4 + 5th hold 5s) works on web; ignores key-repeat on first four presses.
- Panel copy notes web + desktop support.

## 00:32 — Fix database easter egg not revealing

**Why:** Hidden restore panel did not appear reliably in browser testing.

**Files:**
- `resources/js/app.js`
- `resources/views/database/index.blade.php`
- `scripts/test-database-easter-egg.mjs`

**Summary:**
- Added bottom-right progress toast while entering the `.` sequence (dot count + 5s hold bar).
- Capture-phase `window` key listeners, numpad decimal support, auto-focus on Database page.
- Clear "released too early" feedback when 5th dot is not held long enough.

## 00:35 — Remove blue focus outline on Database page

**Why:** Easter egg auto-focus on the page wrapper showed an unwanted blue browser focus ring.

**Files:**
- `resources/views/database/index.blade.php`
- `resources/js/app.js`

**Summary:**
- Removed `tabindex` and programmatic `focus()` from the Database content wrapper; keyboard listener stays on `window`.

## 00:36 — Remove easter egg progress indicator

**Why:** Hidden restore must stay invisible — no on-screen hints during the dot sequence.

**Files:**
- `resources/views/database/index.blade.php`
- `resources/js/app.js`

**Summary:**
- Removed amber bottom-right progress toast; easter egg runs silently until the restore panel appears.

## 10:04 — Desktop build v0.1.17 (SQL restore easter egg + full feature set)

**Why:** Ship hidden SQL restore, web/MySQL restore support, and all recent fixes in a new production installer.

**Files:**
- `.env.example` (`NATIVEPHP_APP_VERSION=0.1.17`)
- `pulse/dist/Pulse-0.1.17-arm64.dmg`
- `pulse/dist/Pulse-0.1.17-setup.exe`

**Summary:**
- Built macOS + Windows installers at **v0.1.17** with 10:00 AM cloud backup schedule, sidebar version label, cloud backup marker reset, and silent `.` easter egg SQL restore.
- Desktop data lives in OS **userData** (`~/Library/Application Support/.../storage/app/pulse.sqlite` on macOS) — survives reinstall as long as app data is not manually deleted.
- Reinstall only runs migrations + module sync; does **not** re-seed when existing database has users.
