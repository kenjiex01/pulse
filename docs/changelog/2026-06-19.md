# Code Changelog — 2026-06-19

> Daily log of `pulse/` code changes. Append-only.

---

## 10:00 — Port Time Capturing Settings from paths-mvc

**Why:** User requested Time Capturing Settings tab logic ported from paths-mvc (Time Code + Time Capture Format).

**Files:**
- `database/migrations/2026_06_19_100000_create_time_capturing_settings_tables.php`
- `config/time_capturing_settings.php`
- `app/Models/TimeCode.php`, `TimeCaptureFormat.php`, `TimeCaptureField.php`
- `app/Support/TimeCode.php`, `TimeCaptureFormat.php`
- `app/Http/Controllers/TimeCapturingSettingsController.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `routes/web.php`
- `resources/views/timekeeping/time-capturing-settings/_sub-tabs-nav.blade.php`
- `resources/views/timekeeping/time-codes/_list.blade.php`, `_results.blade.php`, `_form.blade.php`
- `resources/views/timekeeping/time-capture-formats/_list.blade.php`, `_results.blade.php`, `_form.blade.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `resources/js/app.js`

**Summary:**
- New tables: `tbl_time_codes`, `tbl_timecapture_formats`, `tbl_timecapture_fields` (all soft deletes).
- Timekeeping Policy tab `time-capturing-settings` with sub-tabs: Time Code | Time Capture Format.
- Time Code CRUD: code, description, is_billable, pass_out; unique code; in-use check stub.
- Time Capture Format CRUD: device name, description, standard column mappings (employee, date, reason, time in/out, indicator), custom other fields.
- Validation mirrors paths-mvc: min 4 mappings, unique columns, indicator identifiers when same-column mode.
- Modal-first CRUD, live datatable, sys_logs on all actions.

---

## 11:00 — Remove Time Code sub-tab from Time Capturing Settings

**Why:** User requested removal of the Time Code tab; only Time Capture Format should remain.

**Files:**
- `app/Http/Controllers/TimeCapturingSettingsController.php`
- `app/Support/TimeCaptureFormat.php`
- `app/Support/TimeCode.php` (deleted)
- `routes/web.php`
- `config/timekeeping_policy.php`, `config/time_capturing_settings.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `resources/views/timekeeping/time-capture-formats/_list.blade.php`
- `resources/views/timekeeping/time-codes/*` (deleted)
- `resources/views/timekeeping/time-capturing-settings/_sub-tabs-nav.blade.php` (deleted)

**Summary:**
- Removed Time Code sub-tab and sub-tab navigation.
- Time Capturing Settings now shows Time Capture Format list directly.
- Removed time code CRUD routes, controller methods, support class, and views.
- `tbl_time_codes` model/table retained for future upload/reason mapping use.

---

## 11:30 — Employee ID fixed to Employee Number only

**Why:** User requested Employee ID field show only Employee Number (no Temporary Employee Number dropdown).

**Files:**
- `resources/views/timekeeping/time-capture-formats/_form.blade.php`
- `app/Support/TimeCaptureFormat.php`
- `config/time_capturing_settings.php`

**Summary:**
- Replaced Employee ID dropdown with fixed "Employee Number" label + hidden input.
- Removed `tmp_employee_number` option from config and mapping load logic.
- Excluded other format mapping selects from searchable-select to avoid mixed option lists.

---

## 12:00 — Employee ID dropdown: Employee Number or Biometric ID

**Why:** User requested Employee Biometric ID as a dropdown option alongside Employee Number (not a separate checkbox row).

**Files:**
- `resources/views/timekeeping/time-capture-formats/_form.blade.php`
- `app/Support/TimeCaptureFormat.php`
- `config/time_capturing_settings.php`

**Summary:**
- Employee ID is a select: Employee Number | Employee Biometric ID.
- Selected type maps to `employee_number` or `biometric_id` with one column input.
- Edit load restores the correct dropdown value from saved mappings.

---

## 12:30 — Inline column inputs beside field dropdowns

**Why:** User requested column textbox placed next to its corresponding field dropdown (paths-mvc layout).

**Files:**
- `resources/views/timekeeping/time-capture-formats/_form.blade.php`

**Summary:**
- Employee ID, Date, and Time In: select and column number on one row.
- Logout Reason and Time Out: checkbox label with column input aligned on the same row.

## 10:15 — Biometric time capture format defaults and indicator mode

**Why:** Dapat supported ang biometric upload na hiwalay na row ang time in/time out; indicator column (1=in, 0=out) ang nagdedetermine.

**Files:**
- `config/time_capturing_settings.php`
- `app/Support/TimeCaptureFormat.php`
- `resources/views/timekeeping/time-capture-formats/_form.blade.php`
- `resources/js/app.js`
- `database/seeders/TimeCaptureFormatSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `public/build/assets/app-CGztEqma.js`

**Summary:**
- Default new format: Biometric ID col 1, Date col 2, Time col 3, Indicator col 4 (1=in, 0=out).
- Added sample row preview and clearer biometric indicator UI.
- Seeded `BIOMETRIC` format with same mappings as paths-mvc same-column mode.

## 10:45 — Clear Time In row when biometric same-column mode is on

**Why:** Kapag naka-tick ang "Same column for Time In / Time Out", dapat walang laman ang Time In row (type + column) — shared time column lang sa indicator section.

**Files:**
- `app/Support/TimeCaptureFormat.php`
- `resources/views/timekeeping/time-capture-formats/_form.blade.php`
- `resources/js/app.js`
- `public/build/assets/app-B2qx0c4w.js`

**Summary:**
- Fixed syntax error in `validateMappingRules()` (missing closing brace).
- When same-column indicator mode is enabled: Time In type/column cleared, disabled, and grayed out; Time Out row cleared/disabled.
- Added `worktime_column` field in indicator section; validation and mapping use it instead of `time_in_column` in biometric mode.
- JS `syncTimeCaptureFormatPanels()` toggles fields on checkbox change and on form load.

## 11:30 — Port Timekeeping Templates (email notifications for filed forms)

**Why:** User requested Templates tab logic from paths-mvc — email notification bodies for filed/approved/cancelled/disapproved forms.

**Files:**
- `database/migrations/2026_06_19_110000_create_timekeeping_template_tables.php`
- `config/timekeeping_templates.php`
- `app/Models/LuTemplate.php`, `TimekeepingTemplate.php`
- `app/Support/TimekeepingTemplate.php`
- `app/Http/Controllers/TimekeepingTemplateController.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `database/seeders/LuTemplateSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `routes/web.php`
- `resources/views/timekeeping/templates/_list.blade.php`, `_results.blade.php`, `_form.blade.php`
- `resources/views/timekeeping/policy/index.blade.php`
- `resources/js/app.js`
- `public/build/assets/app-BQFKyI-5.js`

**Summary:**
- New tables: `lu_template` (6 notification types) and `tbl_timekeeping_templates` (content + is_active).
- Templates tab CRUD with modal-first UI; list shows ID, type, content, active status.
- Validation mirrors paths-mvc: required content (max 1000, no HTML), one active template per type, block delete of active rows.
- Placeholder tokens per type (e.g. `[EMPLOYEE_NAME]`, `[FORM_TYPE]`, `[LINK]`) with click-to-insert in form.
- Added `TimekeepingTemplate::fetchActiveContent()` for future form-filing email integration.

## 11:35 — Fix Templates syntax error

**Why:** Templates tab crashed with ParseError on load.

**Files:**
- `app/Support/TimekeepingTemplate.php`

**Summary:**
- Fixed misplaced quote in `placeholdersForType()` config key concatenation.

## 11:40 — Fix empty template placeholders panel

**Why:** Placeholder list stayed blank after selecting template type.

**Files:**
- `resources/views/timekeeping/templates/_form.blade.php`
- `resources/js/app.js`

**Summary:**
- Moved placeholder map onto the form as `data-tkt-placeholder-map` (was outside `<form>`, so JS could not find it and skipped init).

## 11:45 — Templates active/inactive toggle actions

**Why:** User requested row actions matching other tables — toggle active/inactive; delete only when inactive.

**Files:**
- `app/Support/TimekeepingTemplate.php`
- `app/Http/Controllers/TimekeepingTemplateController.php`
- `routes/web.php`
- `resources/views/timekeeping/templates/_results.blade.php`

**Summary:**
- Added toggle switch action (green = active, gray = inactive) like payroll maintenance tables.
- Delete button visible only for inactive templates; activating still blocked if another active template exists for the same type.

## 12:00 — Port Holiday Settings from paths-mvc

**Why:** User requested Holiday Settings tab with paths-mvc logic for holidays, groups, and year schedules.

**Files:**
- `database/migrations/2026_06_19_120000_create_holiday_settings_tables.php`
- `config/holiday_settings.php`
- `app/Models/TimekeepingHoliday.php`, `TimekeepingHolidayGroup.php`, `TimekeepingHolidayGroupList.php`, `TimekeepingYear.php`, `TimekeepingHolidayYear.php`
- `app/Support/HolidaySettings.php`
- `app/Http/Controllers/HolidaySettingsController.php`
- `app/Http/Controllers/TimekeepingPolicyController.php`
- `routes/web.php`
- `resources/views/timekeeping/holiday-settings/**`
- `resources/views/timekeeping/policy/index.blade.php`
- `resources/js/app.js`

**Summary:**
- Three sub-tabs: Holidays (master), Groups (dual-list assignment), Year (calendar year + per-year holiday list).
- Tables: `tbl_timekeeping_holidays`, `tbl_timekeeping_holiday_groups`, `tbl_timekeeping_holiday_group_list`, `tbl_timekeeping_years`, `tbl_timekeeping_holiday_years`.
- Validation/delete guards mirror paths-mvc: block delete if holiday in group, group on employee, year ≤ current year.
- New year auto-seeds recurring master holidays; year manage modal supports add/edit/remove schedule entries.

## 12:15 — Holiday group multi-select list boxes

**Why:** Select Holidays was rendered as searchable dropdowns instead of paths-mvc-style multi-select lists.

**Files:**
- `resources/views/timekeeping/holiday-settings/groups/_form.blade.php`
- `resources/js/searchable-select.js`

**Summary:**
- Excluded `multiple` selects from searchable-select wrapper; added `data-no-searchable-select` on available/selected lists.
- Restored tall list boxes with Available / Selected labels and >> / << transfer buttons.

## 12:20 — Show Add Holiday in Manage Year modal

**Why:** User could not add holidays inside Manage Year — add form was gated on create permission and hidden when no options.

**Files:**
- `resources/views/timekeeping/holiday-settings/year/_manage.blade.php`
- `resources/views/timekeeping/holiday-settings/year/_results.blade.php`
- `app/Http/Controllers/HolidaySettingsController.php`

**Summary:**
- Add Holiday section always visible for users with update permission.
- `storeYearEntry` uses update permission (same as edit/delete in manage modal).
- Empty states link to Holidays tab when no master holidays or all already assigned.

## 12:25 — Fix duplicate year holiday on re-add after delete

**Why:** Adding a holiday to a year failed with unique constraint when a soft-deleted row still existed for the same year + holiday.

**Files:**
- `app/Support/HolidaySettings.php`
- `app/Http/Controllers/HolidaySettingsController.php`

**Summary:**
- `syncYearEntryFromHoliday()` restores and updates soft-deleted year entries instead of inserting duplicates.
- Recurring year seed uses the same sync helper.

## 12:30 — Hard delete year holiday entries with audit log

**Why:** User requested hard delete for holidays removed from a year schedule, with sys_logs entry.

**Files:**
- `app/Models/TimekeepingHolidayYear.php`
- `database/migrations/2026_06_19_123000_hard_delete_timekeeping_holiday_years.php`
- `app/Support/HolidaySettings.php`
- `app/Http/Controllers/HolidaySettingsController.php`

**Summary:**
- Removed soft deletes from `tbl_timekeeping_holiday_years`; purge legacy soft-deleted rows via migration.
- Delete from Manage Year now permanently removes the row and writes `sys_logs` (action: delete, old values preserved).

## 12:51 — Add Time Logs submodule under Timekeeping

**Why:** User requested Time Logs as a Timekeeping sub-module, matching paths-mvc structure.

**Files:**
- `database/migrations/2026_06_19_130000_create_time_logs_tables.php`
- `database/seeders/SubModuleSeeder.php`
- `config/time_logs.php`
- `app/Models/RawTimekeepingTransaction.php`, `RawTimekeepingInandout.php`, `RawTimekeepingTimeLog.php`
- `app/Support/TimeLogs.php`
- `app/Policies/TimeLogsPolicy.php`
- `app/Http/Controllers/TimeLogsController.php`
- `app/Providers/AppServiceProvider.php`
- `routes/web.php`
- `resources/views/timekeeping/time-logs/index.blade.php`, `_results.blade.php`
- `resources/views/partials/sidebar-icon.blade.php`

**Summary:**
- New sidebar sub-module **Time Logs** (`timekeeping.time-logs.*`) with permissions synced for admin role.
- Tables: `raw_timekeeping_transactions`, `raw_timekeeping_inandout`, `raw_timekeeping_time_logs` (includes `filename` and `timecapture_format_id` on transactions).
- Initial UI: **Time In / Time Out** tab listing upload batches (batch no, record count, uploader, date, filename) — upload/detail flows to follow.

## 13:06 — Fix employee salary save after employment sync

**Why:** Employee salary was not persisting because employment records are recreated on save, leaving stale `employment_info_id` values in memory and breaking salary sync.

**Files:**
- `app/Services/EmployeeEmploymentSync.php`
- `app/Services/EmployeeSalarySync.php`
- `app/Http/Controllers/EmployeeController.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Http/Requests/Employee/UpdateEmployeeRequest.php`
- `resources/js/app.js`

**Summary:**
- Clear cached `employmentInformations` after employment sync; salary sync now reloads fresh employment rows before `updateOrCreate`.
- Wrapped employment + salary sync in DB transactions on create/update.
- Normalized blank salary numeric/FK fields before validation; validation redirect returns to the active tab.
- On submit, re-enable the correct salary panel and relax `required` on hidden tabs so browser validation does not block save.

## 13:18 — Employee salary Hourly Rate (read-only, auto-computed)

**Why:** User requested a read-only Hourly Rate at the bottom of each employee salary panel, computed from Basic Income and Hours Per Day.

**Files:**
- `app/Models/EmployeeSalary.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `resources/js/app.js`

**Summary:**
- Formula: `(Basic Income taxable + non-taxable) ÷ Hours Per Day` using the BASC / default basic income row.
- Read-only field at the bottom of each salary panel; updates live when income amounts or hours change.
- Employee view page shows the same computed Hourly Rate.

## 13:22 — Fix Rate Definition day-type rates grid layout

**Why:** Rate group / ND rate group forms showed overlapping sidebar and table columns (labels outside the bordered panel).

**Files:**
- `resources/views/payroll/rate-definitions/_rates-grid.blade.php`
- `resources/css/app.css`
- `resources/js/app.js`

**Summary:**
- Replaced flex layout with explicit CSS grid (`11rem` day-type nav + fluid content column).
- Wrapped rates table in `datatable-skolaris-table-wrap` + `table-skolaris` for consistent alignment.
- Disabled searchable-select on dense rate grid dropdowns; tab panels now use `hidden` attribute + class for reliable toggling.

## 13:31 — Hourly Rate computation uses Pay Type (Days Per Period)

**Why:** User requested hourly rate to follow pay type, matching PATHS payroll formula.

**Files:**
- `app/Models/PayType.php`
- `app/Models/EmployeeSalary.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/js/app.js`

**Summary:**
- Formula updated to `Basic Income ÷ Days Per Period ÷ Hours Per Day` (PATHS payroll style).
- Added pay-type defaults for Days Per Period: Daily=1, Weekly=6, Semi-Monthly=13.04167, Monthly=26.
- Changing Pay Type auto-updates Days Per Period and recalculates Hourly Rate live.

## 13:40 — Days Per Period rules by Pay Type

**Why:** Daily should auto-set Days Per Period to 1; Weekly/Semi-Monthly/Monthly require manual Days Per Period input for hourly rate.

**Files:**
- `app/Models/PayType.php`
- `app/Models/EmployeeSalary.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/js/app.js`

**Summary:**
- Daily pay type: Days Per Period locked to 1 (read-only, auto on save).
- Weekly, Semi-Monthly, Monthly: Days Per Period is required user input (no auto defaults).
- Hourly Rate uses entered Days Per Period for non-daily pay types; shows `—` until Days Per Period is filled.

## 13:57 — Time Logs file upload (paths-mvc style)

**Why:** User requested time logs uploading like paths-mvc Time In / Time Out batch upload.

**Files:**
- `app/Services/TimeLogsUploadService.php`
- `app/Http/Controllers/TimeLogsController.php`
- `app/Support/TimeLogs.php`
- `app/Models/RawTimekeepingInandout.php`
- `app/Models/RawTimekeepingTimeLog.php`
- `routes/web.php`
- `resources/views/timekeeping/time-logs/index.blade.php`
- `resources/views/timekeeping/time-logs/_results.blade.php`
- `resources/views/timekeeping/time-logs/_upload-form.blade.php`
- `resources/views/timekeeping/time-logs/_upload-preview.blade.php`
- `resources/views/timekeeping/time-logs/_show-content.blade.php`
- `resources/js/app.js`

**Summary:**
- Upload modal: pick Time Capture Format, download tab-delimited template, upload `.txt`/`.csv`.
- Two-step flow: parse + validation preview, then **Load to the Database** (paths-mvc style).
- Supports biometric (worktime + indicator), time in/out pairs, and reason/time-code formats.
- Batch list: view batch details, purge selected batches; auto batch numbering.

## 14:23 — Employee Profile submodule (Timekeeping)

**Why:** User requested Employee Profile under Timekeeping — list all employees with setup status, configurable per employee like paths-mvc `employee_settings`.

**Files:**
- `database/migrations/2026_06_19_140000_create_timekeeping_employee_setup_tables.php`
- `app/Models/TimekeepingEmployeeSetup.php`
- `app/Models/TimekeepingEmployeeRestDay.php`
- `app/Models/Employee.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `app/Policies/TimekeepingEmployeeProfilePolicy.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `routes/web.php`
- `app/Providers/AppServiceProvider.php`
- `database/seeders/SubModuleSeeder.php`
- `resources/views/timekeeping/employee-profile/index.blade.php`
- `resources/views/timekeeping/employee-profile/_results.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `resources/views/partials/sidebar-icon.blade.php`
- `resources/js/app.js`

**Summary:**
- New sidebar sub-module **Employee Profile** (`timekeeping.employee-profile.*`) with admin permissions synced.
- Lists all employees; badge **Needs Setup** until row exists in `tbl_timekeeping_employee_setup`.
- Setup modal: rest days (with paid toggle), holiday group, shift code, policy group, leave/populate flags, team viewing limit.
- Tables: `tbl_timekeeping_employee_setup`, `tbl_timekeeping_employee_rest_days`.
- Approval Settings and Attendance View tabs deferred (paths-mvc tabs 2–3).

## 14:30 — Employee Profile setup modal — 3 tabs (paths-mvc)

**Why:** User requested the setup modal to use the same 3-tab layout as paths-mvc (Timekeeping Settings, Approval Settings, Attendance View).

**Files:**
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-tabs-nav.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-tab-timekeeping.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-approval-settings.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-attendance-view.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `routes/web.php`
- `resources/js/app.js`

**Summary:**
- Setup modal now has 3 tabs matching paths-mvc labels; Save Settings stays on Timekeeping Settings tab only.
- Approval Settings tab lazy-loads via AJAX (paths-style layout shell; full approval matrix port pending).
- Attendance View tab lazy-loads recent raw time in/out logs for the employee.
- View modal also uses the same 3-tab navigation.

## 14:48 — Enforce unique employee number on Employment tab

**Why:** User requested that employee number must be unique in the Human Resource Employment tab.

**Files:**
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Http/Requests/Employee/StoreEmployeeRequest.php`
- `app/Http/Requests/Employee/UpdateEmployeeRequest.php`
- `app/Http/Requests/Employee/WizardDetailsRequest.php`
- `app/Http/Controllers/EmployeeController.php`
- `resources/views/employees/partials/_employment-information.blade.php`

**Summary:**
- Unique validation on active employees (`deleted_at` null) with clear error message.
- Trims employee number before validation/save.
- Wizard final submit re-checks uniqueness before create; redirects to step 1 on duplicate.
- Edit validation failure opens Employment tab when employee number conflicts.
- Field helper text: must be unique across all active employees.

## 15:30 — Sidebar accordion — one module open at a time

**Why:** User requested that opening one sidebar dropdown closes the other expanded module groups.

**Files:**
- `resources/js/app.js`

**Summary:**
- Replaced unreliable `<details>` `toggle` handler with summary click accordion logic.
- On page load, collapses extra open groups so only the active module (or first open) stays expanded.

## 15:35 — Employee Profile Approval Settings — Form Type dropdown

**Why:** Form Type on Approval Settings tab was disabled placeholder; user could not click or change it.

**Files:**
- `database/migrations/2026_06_19_153000_create_approval_settings_tables.php`
- `database/seeders/UserRequestTypeSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `app/Models/LuUserRequestType.php`
- `app/Models/ApprovalStep.php`
- `app/Models/ApprovalStepMember.php`
- `app/Support/EmployeeApprovalSettings.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/TimekeepingEmployeeProfile.php`
- `routes/web.php`
- `resources/views/timekeeping/employee-profile/_tab-approval-settings.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-approval-routes.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `resources/js/app.js`

**Summary:**
- Ported lookup/approval tables from paths-mvc: `lu_user_request_types`, `tbl_approval_steps`, `tbl_approval_steps_members`.
- Seeded employee form types (Overtime, OBT, FTO, Work Schedule, Leave, etc.; excludes Transfer types).
- Form Type dropdown is now active; changing it reloads approval routes for that type via AJAX.
- Route step editing (add/remove approvers) deferred to a follow-up; view-only grid for now.

## 17:40 — Approval Settings form type list cleanup

**Why:** User requested removing Cost Centers, HR Forms, TOIL Credit, and Work Schedule from Form Type dropdown; rename Shift Schedule to Shift Code.

**Files:**
- `app/Models/LuUserRequestType.php`
- `database/seeders/UserRequestTypeSeeder.php`

**Summary:**
- Employee Profile Approval Settings dropdown excludes IDs 4, 6, 9, 11 (plus existing transfer types 10, 13).
- Renamed form type 8 from **Shift Schedule** to **Shift Code** in seeder/DB.

## 17:42 — Remove auto-forwarding fields from Approval Settings

**Why:** User requested removal of automatic-forwarding UI from Employee Profile Approval Settings tab.

**Files:**
- `resources/views/timekeeping/employee-profile/_tab-approval-routes.blade.php`
- `resources/views/timekeeping/employee-profile/_tab-approval-settings.blade.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`
- `app/Support/EmployeeApprovalSettings.php`

**Summary:**
- Removed **Allow Automatic-Forwarding of Un-Acted Forms?** and **Hours before forwarding** from Approval Routes section.
- Approval Routes table (steps/approvers) remains.

## 17:45 — Hide Approval Settings tab (approval matrix)

**Why:** User requested to hide approval matrix until full route editing is ready.

**Files:**
- `app/Support/TimekeepingEmployeeProfile.php`
- `resources/views/timekeeping/employee-profile/_setup-tabs-nav.blade.php`
- `resources/views/timekeeping/employee-profile/_setup-form.blade.php`
- `resources/views/timekeeping/employee-profile/_show-content.blade.php`
- `app/Http/Controllers/TimekeepingEmployeeProfileController.php`

**Summary:**
- Added `showApprovalMatrix()` flag (currently `false`) — hides **Approval Settings** tab in setup/view modals.
- Approval routes endpoints return 404 while hidden; set flag to `true` when matrix editing ships.

## 17:54 — Sidebar dropdown slide animation

**Why:** User requested smooth slide animation when opening/closing sidebar module dropdowns.

**Files:**
- `resources/views/partials/sidebar.blade.php`
- `resources/css/app.css`
- `resources/js/app.js`

**Summary:**
- Wrapped sub-menu lists in `.sidebar-sub-panel` for height transition.
- Open/close animates with 300ms slide; accordion still closes other groups with animation.
- Chevron rotation timing matched to panel slide.

## 18:17 — Payroll Calendar submodule (port from paths-legacy)

**Why:** User requested Payroll Calendar under Payroll module, ported from paths-legacy.

**Files:**
- `database/migrations/2026_06_19_180000_create_payroll_calendar_tables.php`
- `app/Models/PayrollCalendar.php`, `PayrollCalendarDeduction.php`, `PayrollCalendarLoan.php`, `DeductionLoanPriority.php`, `PayrollSettingOther.php`
- `app/Support/PayrollCalendarModule.php`
- `app/Services/PayrollCalendarGeneratorService.php`, `PayrollCalendarScheduleService.php`
- `app/Http/Controllers/PayrollCalendarController.php`
- `app/Policies/PayrollCalendarPolicy.php`
- `resources/views/payroll/calendar/*`
- `routes/web.php`, `database/seeders/SubModuleSeeder.php`, `PayrollCalendarSeeder.php`
- `app/Providers/AppServiceProvider.php`, `resources/js/app.js`

**Summary:**
- New **Payroll Calendar** submodule (sidebar under Payroll) with pay period CRUD per pay type (Daily/Weekly/Semi-Monthly/Monthly), year filter, auto-fill generate, loan/deduction schedule panel, and Deduction & Loan Priority tab.
- Tables: `tbl_payroll_calendar`, junction tables, `tbl_deduction_loan_priority`, `tbl_payroll_setting_others`.
- All CRUD writes to `sys_logs`; permissions via `payroll-calendar.*` gates.

## 18:19 — Hide Auto-Fill button on Payroll Calendar

**Why:** User requested removal of Auto-Fill button from Pay Periods UI.

**Files:**
- `resources/views/payroll/calendar/index.blade.php`
- `app/Http/Controllers/PayrollCalendarController.php`

**Summary:**
- Removed Auto-Fill button and modal from Payroll Calendar index; backend autofill route retained for future use.

## 18:23 — Payroll Calendar split layout alignment

**Why:** User reported misaligned table vs schedule panel on Pay Periods view.

**Files:**
- `resources/views/payroll/calendar/_results.blade.php`
- `resources/views/payroll/calendar/_schedule-panel.blade.php`

**Summary:**
- Matched left/right panels as equal-height cards with aligned headers (Pay Periods / Loan & Deduction Schedule).
- Moved Delete Selected into panel header; 5/7 column split on xl screens.

## 18:28 — Loan & Deduction Schedule as modal

**Why:** User requested schedule panel open as pop-up modal when clicking View on a pay period.

**Files:**
- `resources/views/payroll/calendar/_results.blade.php`
- `resources/views/payroll/calendar/_schedule-form.blade.php`
- `app/Http/Controllers/PayrollCalendarController.php`
- Removed `resources/views/payroll/calendar/_schedule-panel.blade.php`

**Summary:**
- Removed inline right-side schedule panel; pay periods table is now full width.
- View button opens **Loan & Deduction Schedule** modal per row; reopens after Save Schedule via `?view=` query param.

## 18:43 — Pay period Colleges and Category multi-select

**Why:** User requested multi-select fields for Colleges and Category (User Type) on pay period creation.

**Files:**
- `database/migrations/2026_06_19_190000_create_payroll_calendar_scope_tables.php`
- `app/Models/PayrollCalendarCollege.php`, `PayrollCalendarUserType.php`
- `app/Models/PayrollCalendar.php`
- `app/Services/PayrollCalendarScopeService.php`
- `app/Support/PayrollCalendarModule.php`
- `app/Http/Controllers/PayrollCalendarController.php`
- `resources/views/partials/dual-list-select.blade.php`
- `resources/views/payroll/calendar/_form.blade.php`
- `resources/views/payroll/calendar/index.blade.php`
- `resources/views/payroll/calendar/_results.blade.php`
- `resources/views/timekeeping/holiday-settings/groups/_form.blade.php`
- `resources/js/app.js`

**Summary:**
- New pivot tables: `tbl_payroll_calendar_colleges`, `tbl_payroll_calendar_user_types`.
- Create/Edit Pay Period modal now has dual-list multi-select for **Colleges** (active colleges) and **Category (User Type)** (Faculty, Staff, Admin).
- Both fields required (min 1 selection); values sync on store/update and cascade on delete.
- Reusable `dual-list-select` partial + generic JS init; holiday group form migrated to same partial.

## 18:46 — Stack Category below Colleges on pay period form

**Why:** User requested Category (User Type) multi-select placed below Colleges instead of side-by-side.

**Files:**
- `resources/views/payroll/calendar/_form.blade.php`

**Summary:**
- Changed Colleges and Category dual-list fields from two-column grid to vertical stack (`space-y-4`).

## 18:49 — Colleges optional when Category is Admin only

**Why:** User requested Colleges not be required when only Admin is selected in Category.

**Files:**
- `app/Support/PayrollCalendarModule.php`
- `app/Http/Controllers/PayrollCalendarController.php`
- `resources/views/payroll/calendar/_form.blade.php`
- `resources/views/partials/dual-list-select.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `isAdminOnlyCategory()` / `requiresColleges()` helpers; server skips college min:1 validation when only Admin is selected.
- Form toggles Colleges required asterisk and hint live when Category selection changes to Admin-only.
- Colleges remain required for Faculty, Staff, or mixed selections.

## 18:55 — Group Colleges list by college in pay period form

**Why:** User requested the Colleges multi-select be grouped by college instead of a flat list.

**Files:**
- `app/Http/Controllers/PayrollCalendarController.php`
- `resources/views/partials/dual-list-select.blade.php`
- `resources/views/payroll/calendar/_form.blade.php`
- `resources/views/payroll/calendar/index.blade.php`
- `resources/views/payroll/calendar/_results.blade.php`
- `resources/js/app.js`

**Summary:**
- Available Colleges list now uses `<optgroup>` per college (e.g. `CAS — College of Arts & Sciences`) with campus rows underneath.
- Selected list shows campus name + campus code; transfer back restores options into the correct college group.
- `dual-list-select` partial supports optional `optionGroups` for grouped lists.

## 18:57 — Dedupe Colleges list to unique college codes

**Why:** User requested only colleges in the list (no per-campus duplicates), grouped uniquely by college.

**Files:**
- `app/Http/Controllers/PayrollCalendarController.php`
- `app/Services/PayrollCalendarScopeService.php`
- `resources/views/payroll/calendar/_form.blade.php`

**Summary:**
- Colleges multi-select now shows one row per college code (e.g. `CAS — College of Arts & Sciences`) across all campuses.
- Form submits `college_codes[]`; save expands each code to all active campus college records in `tbl_payroll_calendar_colleges`.
- Edit loads unique college codes from saved period colleges.

## 18:59 — Scrollable dual-list selects for long college names

**Why:** College names were truncated in the Available/Selected list boxes and could not be read in full.

**Files:**
- `resources/css/app.css`
- `resources/views/partials/dual-list-select.blade.php`
- `resources/views/payroll/calendar/_form.blade.php`

**Summary:**
- Wrapped dual-list `<select>` elements in scrollable panels with vertical and horizontal overflow.
- Select width expands to content (`width: max-content`) so long labels scroll instead of clipping.
- Colleges list uses 10 visible rows for easier browsing.

## 19:12 — Use Basic Income as Hourly Rate checkbox

**Why:** User requested a checkbox on Employee Salary so Hourly Rate can display Basic Income directly when enabled.

**Files:**
- `database/migrations/2026_06_19_200000_add_use_basic_income_as_hourly_rate_to_employee_salaries.php`
- `app/Models/EmployeeSalary.php`
- `app/Services/EmployeeSalarySync.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/views/employees/partials/_employee-salary.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `use_basic_income_as_hourly_rate` on `tbl_employee_salaries`.
- Checkbox **Use Basic Income as Hourly Rate** above Hourly Rate field; when checked, Hourly Rate shows Basic Income total (taxable + non-taxable) instead of computed ÷ days ÷ hours.
- Live UI update on toggle/income change; persisted on save; reflected on employee view page.

## 19:15 — Remove COLA Rate per Hour from Employee Salary

**Why:** User requested removal of the COLA Rate per Hour field from Employee Salary.

**Files:**
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/views/employees/partials/_employee-salary.blade.php`
- `resources/views/employees/_show-sections.blade.php`
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Services/EmployeeSalarySync.php`

**Summary:**
- Removed COLA Rate per Hour input from salary form and employee view.
- Stopped validating and syncing the field on save (DB column retained for existing data).

## 19:22 — Swap Category and Colleges order on pay period form

**Why:** User requested Category (User Type) above Colleges in the pay period create/edit modal.

**Files:**
- `resources/views/payroll/calendar/_form.blade.php`

**Summary:**
- Category (User Type) multi-select is now first; Colleges multi-select is below it.

## 19:23 — Clear Colleges when Category is Admin only

**Why:** User requested selected colleges move back to Available when only Admin is selected in Category.

**Files:**
- `resources/js/app.js`

**Summary:**
- When Category selection becomes Admin-only, all colleges in Selected are automatically moved back to Available and hidden inputs are cleared.

## 19:37 — Payroll Transaction submodule (3 tabs)

**Why:** User requested new Payroll submodule with Payroll Batches, Upload Transactions, and Unpost Batches tabs (paths-legacy Payroll Processing).

**Files:**
- `database/migrations/2026_06_19_210000_create_payroll_transaction_tables.php`
- `database/seeders/PayrollBatchStatusSeeder.php`
- `database/seeders/SubModuleSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `config/payroll_transaction.php`
- `app/Models/PayrollBatch.php`, `PayrollBatchDetail.php`, `PayrollBatchStatus.php`
- `app/Support/PayrollTransactionModule.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `app/Policies/PayrollTransactionPolicy.php`
- `app/Providers/AppServiceProvider.php`
- `resources/views/payroll/transaction/*`
- `resources/views/partials/sidebar-icon.blade.php`
- `routes/web.php`

**Summary:**
- New sidebar submodule **Payroll Transaction** under Payroll with tabs: Payroll Batches, Upload Transactions, Unpost Batches.
- Tables: `lu_payroll_batch_status`, `trn_payroll_batches`, `trn_payroll_batch_details` (soft deletes on batch rows).
- Batches and Unpost tabs list batches (Unpost filters Posted status); Upload tab includes 8 legacy sub-tabs (Incomes, Deductions, etc.) with placeholder content for follow-up port.
- Permissions via `payroll-transaction.*` gates; admin role synced via seeders.

## 19:39 — Fix duplicate import in PayrollTransactionController

**Why:** Payroll Transaction page crashed with fatal error on duplicate `use` statement.

**Files:**
- `app/Http/Controllers/PayrollTransactionController.php`

**Summary:**
- Removed duplicate `PayrollTransactionModule` import that caused "name is already in use" fatal error.

## 19:24 — Disable Colleges selection for Admin-only Category

**Why:** User requested that colleges cannot be selected when only Admin is chosen in Category.

**Files:**
- `resources/js/app.js`
- `resources/views/payroll/calendar/_form.blade.php`

**Summary:**
- Colleges dual-list (Available/Selected and >> / << buttons) is disabled when Category is Admin-only.
- Hint updated to state colleges are not used for Admin-only pay periods.

## 19:47 — Add Payroll Batch create modal

**Why:** User requested ability to add payroll batches from Payroll Transaction.

**Files:**
- `database/migrations/2026_06_19_220000_add_payroll_batch_form_support.php`
- `database/seeders/WithholdingTaxComputationSeeder.php`
- `database/seeders/ReferenceDataSeeder.php`
- `app/Models/WithholdingTaxComputation.php`
- `app/Models/PayrollSettingOther.php`
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/index.blade.php`
- `resources/views/payroll/transaction/_form.blade.php`
- `resources/views/payroll/transaction/_create-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Added **Add Payroll Batch** button and create modal on Payroll Batches tab.
- Form fields: Batch No., Pay Type, Pay Year, Pay Period, Tax Computation, optional Include all employees.
- Cascading pay period selects; batch no. auto-suggests from `last_batch_no`.
- Creates `trn_payroll_batches` row (status Pending) and optionally attaches eligible employees.
- Lookup table `lu_withholding_tax_computations` and `last_batch_no` on payroll settings.

## 19:52 — Pay Year dropdown from Payroll Calendar only

**Why:** Pay Year in Add Payroll Batch must list unique years from created pay periods only, not duplicates or fallback current year.

**Files:**
- `app/Services/PayrollBatchService.php`
- `resources/views/payroll/transaction/_form.blade.php`
- `resources/views/payroll/transaction/_create-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Built `yearsByPayType` from `tbl_payroll_calendar` with `unique()` per pay type.
- Removed `date('Y')` fallback; default pay type prefers first type that has calendar years.
- JS uses dedicated years list with `Set` dedupe; validation requires pay year exists for selected pay type.

## 19:58 — Fix empty Pay Year dropdown in Add Payroll Batch

**Why:** Pay Year showed "No options found" even with payroll calendar data because searchable select initialized before dynamic options were populated.

**Files:**
- `resources/js/app.js`

**Summary:**
- Call `refreshSearchableSelect()` after cascading Pay Year/Pay Period options are filled.
- Harden pay type/year key lookup for JSON string vs integer keys.
- Re-sync batch form on modal reopen without duplicate event listeners.

## 20:01 — Server-render Pay Year options; disable searchable on batch cascades

**Why:** Pay Year still empty because searchable select wrapped dynamic selects before options loaded; JSON in script tags was unreliable inside the form.

**Files:**
- `resources/views/payroll/transaction/_create-form.blade.php`
- `resources/views/payroll/transaction/_form.blade.php`
- `resources/js/app.js`

**Summary:**
- Pay Year and Pay Period options now render server-side from payroll calendar (e.g. 2026, 001 Jun 1–15).
- Calendar JSON moved to `data-pb-years` / `data-pb-periods` on the form element.
- Pay Type, Pay Year, Pay Period use plain `select` (`data-no-searchable-select`) — no searchable wrapper.

## 20:03 — Fix Pay Year/Period not clearing on Pay Type change

**Why:** Changing Pay Type (e.g. to Weekly with no calendar) still showed Semi-Monthly 2026 because cascade change listeners were not reliably attached.

**Files:**
- `resources/js/app.js`

**Summary:**
- Pay Type / Pay Year changes use document-level event delegation to refresh dependent dropdowns.
- Empty pay type shows "No pay year defined" and "No pay period defined" instead of stale options.

## 20:04 — Clear Pay Period when Pay Type or Pay Year changes

**Why:** Pay Period kept stale Semi-Monthly values when switching to a pay type with no payroll calendar.

**Files:**
- `resources/js/app.js`

**Summary:**
- Pay Period clears immediately when Pay Type changes, before Pay Year reloads.
- Pay Period also refreshes when Pay Year changes; empty states drop `required` until options exist.

## 20:12 — Payroll batch view modal with add/remove employees

**Why:** User requested ability to view a payroll batch and add employees (paths-mvc batch detail flow).

**Files:**
- `app/Models/PayrollBatch.php`
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/index.blade.php`
- `resources/views/payroll/transaction/_batches-results.blade.php`
- `resources/views/payroll/transaction/_show-content.blade.php`
- `resources/views/payroll/transaction/_add-employees-form.blade.php`
- `resources/views/payroll/transaction/_add-employees-modal.blade.php`
- `resources/js/app.js`

**Summary:**
- Batch list: View action opens modal with batch header (pay period, tax computation, status) and employee list.
- Add Employees nested modal: searchable eligible employees, select-all, optional include-all checkbox.
- Remove Selected soft-deletes batch detail rows; editable only for Pending/Locked/Processed batches.
- Routes: show redirect, POST/DELETE employees on batch; sys_logs on view/add/remove.

## 20:18 — Fix Employee Salary Save Changes button

**Why:** Save on Edit Employee (Employee Salary tab) appeared to do nothing due to browser validation blocking submit silently.

**Files:**
- `resources/js/app.js`
- `resources/views/employees/partials/_employee-salary.blade.php`
- `resources/views/employees/partials/_employee-salary-panel.blade.php`
- `resources/views/employees/partials/_employee-salary-income-row.blade.php`

**Summary:**
- Prep form on Save click (before validation): strip `required` from hidden tabs, salary subtabs, and hybrid panels.
- Show validation on first invalid field (focus + reportValidity); switch to the tab/subtab that contains it.
- Salary selects use plain `select` (`data-no-searchable-select`) so browser validation is visible.
- Load `rate_group_id` and `use_basic_income_as_hourly_rate` when editing existing salary; show Days Per Period server error.

## 20:28 — Fix employee salary not persisting on save

**Why:** Employee Salary changes (pay type, income amounts) reverted after reload because employment rows were force-deleted/recreated each save, breaking salary linkage; disabled panel fields could also be omitted from POST.

**Files:**
- `app/Services/EmployeeEmploymentSync.php`
- `app/Http/Controllers/EmployeeController.php`
- `resources/js/app.js`

**Summary:**
- Employment sync now updates existing rows in place (same `employment_info_id`); only removes extras when hybrid toggled off.
- Before submit, explicitly re-enables fields on the active salary/employment panel so values are posted.
- After update, redirect back to Edit Employee on the same tab instead of the view page.

## 20:33 — Fix salary Basic Income / Days Per Period reverting on save

**Why:** Duplicate salary panels (single + hybrid) both posted `employee_salaries[0]`; the hidden hybrid panel’s stale values overwrote user edits for taxable income and days per period.

**Files:**
- `resources/views/employees/partials/_employee-salary.blade.php`
- `resources/views/employees/partials/_employment-information.blade.php`
- `resources/js/app.js`
- `resources/css/app.css`

**Summary:**
- Wrapped single/hybrid salary and employment panels in disabled `<fieldset>` so inactive panel fields are excluded from POST.
- On submit, strip `name` from inactive panel fields as a second safeguard against duplicate array keys.
- Panel toggle JS now sets `fieldset.disabled` instead of only per-field disabled.

## 20:39 — Fix salary save blocked by hidden validation errors

**Why:** Save from Employee Salary tab silently failed when required fields on other tabs were invalid (e.g. missing Campus on Assignment); user saw old values again after reload.

**Files:**
- `app/Http/Requests/Employee/Concerns/EmployeeFormRules.php`
- `app/Http/Requests/Employee/UpdateEmployeeRequest.php`
- `resources/views/employees/edit.blade.php`
- `resources/views/employees/_form.blade.php`
- `resources/views/employees/partials/_employment-information.blade.php`
- `resources/js/app.js`
- `app/Http/Controllers/EmployeeController.php`

**Summary:**
- Render only one salary/employment panel in HTML (`@if/@else`) so duplicate `employee_salaries[0]` cannot overwrite edits.
- Hybrid toggle on edit reloads page with `?is_hybrid=` to swap panels server-side.
- Validation redirect opens the tab that contains the first error (e.g. Assignment when Campus is missing).
- Edit page shows a red validation summary banner listing all errors.
- Simplified submit prep (no client `checkValidity` block); assets rebuilt.

## 20:51 — Payroll batch per-employee detail modal (Incomes / Deductions)

**Why:** User asked how to view per-employee payroll details inside a batch; legacy PATHS opens income/deduction tabs per batch detail row.

**Files:**
- `database/migrations/2026_06_19_230000_create_payroll_batch_detail_transaction_tables.php`
- `app/Models/PayrollIncome.php`, `PayrollDeduction.php`, `PayrollBatchDetail.php`
- `app/Services/PayrollBatchService.php`
- `app/Support/PayrollTransactionModule.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `routes/web.php`
- `resources/views/payroll/transaction/index.blade.php`
- `resources/views/payroll/transaction/_show-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-content.blade.php`
- `resources/views/payroll/transaction/_batch-employee-detail-tabs-nav.blade.php`

**Summary:**
- Added `trn_payroll_incomes` and `trn_payroll_deductions` tables (soft deletes) linked to batch detail rows.
- On add employee (or first view), seed income/deduction lines from the employee’s salary setup for the batch pay type.
- Batch employee list: View action opens nested modal with Incomes and Deductions tabs and totals.
- Route `payroll.transaction.employees.show` redirects back to batch modal with employee detail open; sys_logs read on detail view.

## 21:05 — Payroll upload transactions (PATHS legacy port)

**Why:** User requested full Upload Transactions feature from paths-legacy — 8 upload types with tab-delimited file import to raw staging tables.

**Files:**
- `database/migrations/2026_06_19_240000_create_raw_payroll_transaction_tables.php`
- `app/Models/RawPayrollTransaction.php` + 6 raw detail models
- `app/Services/PayrollTransactionUploadService.php`
- `app/Support/PayrollTransactionModule.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `config/payroll_transaction.php`
- `routes/web.php`
- `resources/views/payroll/transaction/index.blade.php`
- `resources/views/payroll/transaction/_upload-transactions-results.blade.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`
- `resources/views/payroll/transaction/_upload-preview.blade.php`
- `resources/views/payroll/transaction/_upload-show-content.blade.php`
- `resources/js/app.js`

**Summary:**
- Added `raw_payroll_transactions` header + detail tables for incomes, deductions, hours worked, leaves, loans, resigned employees.
- Config-driven upload types (8 tabs) with template download, parse → preview → commit flow (mirrors Time Logs).
- Upload form requires pay type / year / period; validates employee no., lookup codes, decimals, dates per legacy columns.
- Batch list with search, view modal (detail lines), purge selected; sys_logs on read/create/delete.

## 21:09 — Fix upload modal pay year/period staying disabled

**Why:** Pay Year and Pay Period stayed disabled after selecting Pay Type in Upload modal — searchable-select UI was not refreshed after dynamic option fill.

**Files:**
- `resources/js/app.js`

**Summary:**
- Call `refreshSearchableSelect()` after `fillPayrollBatchSelect` updates options/disabled state.
- Re-init payroll batch forms inside auto-opened modals on page load.

## 21:10 — Payroll upload max file size 15 MB

**Why:** User requested larger upload limit for payroll transaction files.

**Files:**
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`

**Summary:**
- Increased payroll upload limit from 2 MB to 15 MB (validation, service check, UI hint).

## 21:14 — PHP upload limits for 15 MB payroll uploads

**Why:** App allowed 15 MB but local Homebrew PHP was still capped at 2M/8M; user asked to fix PHP config.

**Files:**
- `config/php-upload-limits.ini`
- `scripts/check-php-upload-limits.sh`

**Summary:**
- Added reference ini (`upload_max_filesize=15M`, `post_max_size=20M`) and shell check script.
- Installed Homebrew drop-in: `/opt/homebrew/etc/php/8.5/conf.d/99-pulse-uploads.ini`.

## 21:18 — Desktop build PHP upload limits (15 MB)

**Why:** Bundled NativePHP desktop app must allow 15 MB payroll uploads without relying on host PHP ini.

**Files:**
- `config/uploads.php`
- `app/Providers/NativeAppServiceProvider.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `config/php-upload-limits.ini`
- `scripts/check-php-upload-limits.sh`

**Summary:**
- Centralized upload limits in `config/uploads.php` (`max_file_kb`, `php_ini` directives).
- `NativeAppServiceProvider::phpIni()` returns `upload_max_filesize=15M` and `post_max_size=20M` for bundled PHP on desktop builds.
- App validation/service reads from same config; dev check script loads limits via Laravel bootstrap.

## 21:46 — Upload modal: unposted pay periods only

**Why:** Pay Period dropdown in Upload Transactions should exclude periods with posted payroll batches.

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`

**Summary:**
- `formData(forUpload: true)` filters out calendar periods that have a batch with status Posted.
- Server-side guard on upload process and commit rejects posted periods.
- UI hint notes unposted periods only.

## 21:53 — Payroll upload template as CSV

**Why:** Download template should be a CSV file for Excel-friendly editing.

**Files:**
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`
- `resources/views/payroll/transaction/index.blade.php`

**Summary:**
- Template download is now `.csv` with `text/csv` content type (comma-separated rows).
- Upload parser auto-detects comma vs tab delimiter from the first line.
- Upload form instructions updated to prefer CSV.

## 21:58 — Payroll upload template aligned with paths-legacy

**Why:** Template format and field hints should match legacy upload (3-row CSV with suggestions).

**Files:**
- `config/payroll_transaction.php`
- `app/Services/PayrollTransactionUploadService.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`

**Summary:**
- CSV template now has 3 rows: aliases, column labels, and per-field hints (legacy `template.php` pattern).
- Hints mirror legacy: employee lookup text, decimal/date/string rules, lookup code lists from DB.
- Parser skips caption/hint rows when present; accepts files with aliases + data only after rows 2–3 removed.
- Upload type descriptions and how-to steps updated in the modal.

## 22:02 — Remove tmp_emp_num from payroll upload templates

**Why:** Pulse has no temporary employee number; upload templates should only use Employee No.

**Files:**
- `config/payroll_transaction.php`
- `app/Services/PayrollTransactionUploadService.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`

**Summary:**
- Dropped `tmp_emp_num` from shared upload field definitions and CSV templates.
- Validation requires `emp_num` only; updated hint text in upload modal.

## 22:40 — Pre-fill upload template with batch employee numbers

**Why:** When a pay period is selected, downloaded CSV should list Employee No. from that period's payroll batch(es).

**Files:**
- `app/Services/PayrollBatchService.php`
- `app/Services/PayrollTransactionUploadService.php`
- `app/Http/Controllers/PayrollTransactionController.php`
- `resources/views/payroll/transaction/_upload-form.blade.php`
- `resources/js/app.js`

**Summary:**
- Template download accepts `?payroll_calendar_id=` and adds one row per batch employee (Employee No. only; other columns blank).
- No pay period selected → template stays header rows only (legacy blank template).
- Download link updates when Pay Period changes in the upload modal.

## 13:54 — Lock default income and leave types

**Why:** System-seeded BASC/OVRT/13TH income types and AWOL/LATE/UNDT leave types must stay active and unchanged.

**Files:**
- `config/payroll_maintenance.php`
- `app/Support/PayrollMaintenance.php`
- `app/Http/Controllers/PayrollMaintenanceController.php`
- `resources/views/payroll/maintenance-table/_results.blade.php`

**Summary:**
- `protected_codes` config for income-types and leave-types; `PayrollMaintenance::isProtectedRecord()` helper.
- Update, toggle status, and delete blocked server-side for protected records.
- UI shows "Default" badge and "Locked" instead of edit/deactivate/delete actions; edit modals omitted.
